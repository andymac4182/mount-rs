# W08 TiDB workstream progress ledger

Status snapshot: **2026-09-22 11:09 AEST**
Repository: `andymac4182/mount-rs`  
Publication snapshot: W08.36 implementation commit `0855e2ab` was reconciled
with concurrent mainline work and pushed in merge tip `e0ab07d6`. The W08.36
implementation was qualified at hosted source `e0ab07d6`; the latest W08 ledger
pointer synchronization before the live production-boundary audit was
`f1362442`. The audit documentation was committed as `fd29a297`, reconciled
with concurrent mainline work and pushed in public merge tip `a40f5b8d`.
The current tested source base for this checkpoint is merged tip
`4e0d0d73b671a0a1b85a8b9315e31e27cabb42c4`, which passed a fresh full locked
workspace test and strict Clippy run after concurrent N-API/session-parity,
chunked-durability, FUSE, S3-test, WebDAV, lockfile, NFS, 9P and provider
updates. This is source-health evidence only; provider/native rows remain
explicit opt-in skips. Changed N-API JavaScript files and package JSON also
passed static checks, while full local N-API runtime remains blocked by absent
optional binding artifacts. The earlier public `9b2dabd7` added only a WebDAV
N-API parity test file whose syntax is clean, while its local runtime is blocked
by absent optional N-API binding artifacts. This does not close any production
gate.
The ledger checkpoint commit `2a99acdd` was reconciled with concurrent
mainline work and published in public merge tip `056e7a15`. The latest FUSE
qualification commit `052dc184` was reconciled and published in public merge
tip `a921fe76`; the subsequent pointer synchronization was published in
`ff29dad7`. The current source qualification is merged tip `4e0d0d73` and is
pending publication with this ledger update.
Earlier source-health results at `4b9787f9`, `7addaf12`,
`77114d4d`, `229a9cd5`,
`b7d77432`, `be721a20`,
`0ab9bf41`, `b27dd2bb`,
`2d459e15`, `6e27d46c`,
`49778fde` and
`6a19ff6555635b20385acdc30d7aa2da143f6931` remain recorded below for
reconciliation history. The W08.35 implementation and
hosted-result ledger history remain in the same public ancestry; subsequent
unrelated mainline merges do not change any W08 production-gate decision.
W08.34 implementation commit `59149b95`, hosted-boundary documentation commit
`25ff75c1` and hosted-success documentation commit `aec7fbdd` remain in the
same public ancestry.
The W08.33 implementation and evidence-boundary commits `16ae685d` and
`969339b8` remain in the same public ancestry.
The W08.32 hosted-policy evidence documentation remains in the same public
ancestry; its final pointer-sync history includes `7dbe0ce3`, which was
verified at **05:36 AEST**.
W08.30 source-verification documentation commit
`8b4fac35` was reconciled into public merge tip
`d4de2e75623ebfe730166bec696f4f43b25ea4fc` (the ref verified at **04:51
AEST**). W08.29 evidence commit `ad45bf06` was previously reconciled into
public merge tip `d68f14de3c13c0510237e9a88c615c1a8e74c9a3`; the subsequent
pointer-synchronized ledger and rollout notes were published in merge tip
`3cd43779a8f440b47278e5112f9ec48b12f43e23` (the ref verified at **04:47
AEST**). The next verified public ref was
`76c2b1a863c23afe71c0591d0a480433e1b9078d` at **04:51 AEST**, where the
locked workspace test and strict Clippy gates both passed. These public tips
contain the published W08.28 implementation
(`dff55858`, merge tip `6abc0535`) and the source ancestry used by W08.29.
Hosted W08.29 run
`35638433010` qualified source `9d3a6e502eccec9ba54c00e80c98e6e1da175177`;
the target artifacts, manifests, SBOMs and attestations were independently
reverified after download.
The latest W08.31 manual target qualification run `35641555767` completed
successfully from current-main-at-dispatch source
`2bbd0266a094b06bf97d51baafe0d3a8800bfec5`; both target builds, both
download-verification jobs and both provenance/SBOM-attestation jobs passed,
and the downloaded artifacts were independently reverified below.
The protected production-candidate admission workflow remains implementation
evidence only: no candidate tag, protected-environment approval, published
production-candidate release, registry acceptance, canary, rollback or owner
approval was executed in this session.
Authoritative W08 hosted evidence includes CI runs `35624385556`, source
`2ab3cf1`, `35627761501`, source `50a33ac`, `35631063978`, source `cf75835`,
and `35633841116`, source `56aae0c`, plus `35638005702`, source `ad0dd81`,
and `35638433010`, source `9d3a6e5`, plus `35641555767`, source
`2bbd0266`, with terminal Linux/macOS build,
download-verification and
provenance/SBOM-attestation jobs, plus the functional, HTTP-contract,
release-policy, SBOM and asset-integrity jobs recorded below. Earlier candidate
runs cancelled before a job started are not used as evidence.

This ledger is the detailed working record for W08 in `WORK_TRACKER.md`. It
separates implementation completion from real provider, durable-service,
native-mount, and hosted-platform acceptance. A checked tracker item is not
treated as proof for a broader unchecked gate. Percentages and time estimates
are provisional planning values, not release-readiness measurements.

## Summary

W08 is **100% complete for the defined workstream acceptance scope**. The
implementation, real durable TiDB/PD/TiKV restart sequence, provider
time/fencing and ambiguous-commit semantics, live Linux TiDB/RustFS consumer
composition, and current Linux/macOS native-platform rows are all evidenced
by terminal hosted jobs. The aggregate workflow was later cancelled by
`main`-branch concurrency and had unrelated FoundationDB/Windows/macOS Rust
failures; those are separate workstreams and are not converted into W08
failures. Local Docker capacity and local native-device/credential gaps remain
environment boundaries, not open W08 acceptance actions.
W08.31 adds a fresh hosted Linux x86_64/macOS arm64 target-package,
download, runtime, checksum, SBOM and attestation qualification; it does not
change the separate production rollout decision.
W08.32 adds a fail-closed repository consistency check for the tracker,
production-rollout decision, detailed ledger and operations runbook, and runs
both the current NO-GO state and simulated decision/gate transitions in the
dedicated hosted W08 policy workflow. This is an implementation control, not
production evidence.
W08.33 adds a `--require-go` admission mode and places it before every
production-candidate target build, so a candidate tag cannot reach artifact
generation while the authoritative rollout decision is NO-GO or P01–P09 is
open. This is a release safety control, not provider, native, canary, rollback
or approval evidence.
W08.34 adds a machine-readable nine-gate evidence packet and fail-closed
validator. The checked-in packet remains NO-GO with explicit remaining actions
and empty evidence arrays; GO admission additionally requires complete records
for revision, provider versions, topology, environment, terminal run/status,
owner, cleanup, rollback and evidence reference.
W08.35 hardens the real TiDB harness preflight by reading Docker architecture,
CPU and memory from one formatted server-info response before creating any
isolated resource. A daemon-side failure now exits with a clear status-2
environment boundary and cannot emit `TIDB_ACCEPTANCE` from partial setup.
W08.36 hardens the GO evidence packet by rejecting placeholder-shaped values
in the required provider-version, topology, environment, run, owner, cleanup,
rollback and evidence-reference fields. The real packet remains NO-GO with
zero evidence records.

The headline percentage uses this deliberately simple weighting of the five
top-level tracker items; it is not a line-count metric:

| Item | Provisional completion | Weight | Weighted contribution |
| --- | ---: | ---: | ---: |
| W08.1 implementation | 100% | 15% | 15% |
| W08.2 durable TiDB/PD/TiKV harness and restart | 100% | 25% | 25% |
| W08.3 fencing, concurrency, ambiguous commit and durability assumptions | 100% | 20% | 20% |
| W08.4 Node/CLI/native platform acceptance | 100% | 20% | 20% |
| W08.5 bounded TiDB metadata + RustFS composition | 100% | 20% | 20% |
| **Workstream view** |  | **100%** | **100%** |

The W08.4 number includes W08.4a and W08.4b; those nested rows are shown
separately below and are not additional weight in the summary calculation.

## Production rollout readiness — new scope

The demo is not a production gate. W08 is **100% complete for its defined
functional acceptance scope**, but the production rollout decision is currently
**NO-GO**. The hosted jobs prove a bounded, real TiDB/RustFS topology and
consumer path; they do not prove that a production deployment has an approved
topology, secret-management process, backup/restore capability, upgrade or
rollback path, SLOs, capacity headroom, security sign-off, on-call readiness or
release provenance.

The provisional production-readiness baseline is **15%**. This is a planning
indicator showing that the functional foundation exists; it is deliberately not
a release-readiness measurement and must not be used to approve a rollout. Each
production item below remains open until its required evidence is produced in a
production-like environment. “Implementation” rows are repository work; the
hosted/provider/native rows require external systems or platform evidence.
The executable deployment contract and rollout sequence are maintained in
[`docs/W08-production-rollout.md`](W08-production-rollout.md); operator
procedures, rollback steps and timed drills are maintained in
[`docs/W08-operations-runbook.md`](W08-operations-runbook.md).

| ID | Production work item | Gate class | Status and completion | Evidence currently available | Remaining actions / exit evidence | Provisional engineering time | External blockers / dependency |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **P01** | Production deployment contract, topology and configuration | Implementation + hosted/provider | **Open — 25%** | W08 proves a pinned TiDB `v8.5.7` durable 3PD/3TiKV test topology and a pinned RustFS service in hosted Linux CI. Public SDK, CLI and N-API consumers now expose an opt-in TLS client build. `scripts/verify-w08-production-config.mjs` passed the positive contract fixture and the public CLI schema accepted it; its insecure fixture failed closed. These are qualification foundations, not an approved production architecture. | Select managed or self-hosted TiDB/PD/TiKV and object storage; document regions, quorum/HA, network policy, TLS endpoints, resource limits, tenant isolation, supported versions, config ownership and IaC; deploy a staging topology and pass a production-like smoke/restart gate. | **1–2 engineer-days** | Platform/provider owners, target regions, DNS/network/TLS and production-sized capacity are not yet supplied. |
| **P02** | Secrets, IAM, credential rotation and audit | Implementation + provider | **Open — 20%** | W08 keeps live provider credentials environment-injected and does not commit them; hosted rows used real credential-gated services. The production-config policy gate rejects inline secret strings and requires `MOUNT_RS_TIDB_TLS_URL`, `R2_ACCESS_KEY_ID` and `R2_SECRET_ACCESS_KEY` as external references; this is not proof that a secret manager or IAM policy is configured. | Bind production credentials through the approved secret manager; define least-privilege TiDB/RustFS policies, rotation/revocation, bootstrap and break-glass procedures, audit events and redaction checks; prove rotation without data loss. | **0.5–1 engineer-day** | Secret manager, IAM roles/policies and credential-rotation owner are external gates. |
| **P03** | Backup, restore, disaster recovery and data-retention policy | Hosted/provider | **Open — 10%** | Restart/reopen and RustFS fault-recovery markers passed, but no backup, restore, corruption, region-loss or measured RPO/RTO evidence exists. | Define RPO/RTO and retention; configure TiDB and object-store backups/versioning; run an isolated restore drill, point-in-time or snapshot recovery, metadata/block consistency checks, corruption/partial-object handling and documented recovery sign-off. | **2–4 engineer-days** | Backup/restore facilities, retention policy, second environment/region and data-owner approval are required. |
| **P04** | Upgrade, compatibility and rollback rehearsal | Hosted/provider + implementation | **Open — 10%** | The acceptance matrix is pinned to TiDB `v8.5.7` and RustFS `1.0.0`; no production-version upgrade or rollback rehearsal has been run. | Test the supported TiDB/RustFS/client version matrix in staging; rehearse schema/config migration, rolling provider upgrades, client/package rollback, interrupted upgrade recovery and downgrade/forward-fix policy; retain logs and compatibility sign-off. | **1–2 engineer-days** | Target upgrade versions, staging snapshots and provider maintenance windows are external inputs. |
| **P05** | Observability, SLOs, health checks and alerting | Implementation + hosted/provider | **Open — 15% gate weight; local and hosted implementation slices passed** | The HTTP transport exposes unauthenticated `GET`/`HEAD /healthz` for listener/process liveness and `/readyz` for a non-empty configured drive registry; an empty registry returns `503 not_ready`. Responses include `Cache-Control: no-store` and `X-Content-Type-Options: nosniff`. The default locked `mount-rs-http` suite passed 8 unit tests and 10 integration tests, while the all-features suite passed 8 unit tests and 13 integration tests including application telemetry and OTLP propagation; both strict-Clippy gates passed on reconciled source `2159976`. Baseline hosted CI run `35599817215`, source `b26819e`, job `http-observability` `106333141914`, passed before the response-header hardening. Terminal hosted CI run `35601990956`, source `f09fbe9`, job `http-observability` `106340034907`, passed the hardening-bearing all-features tests and lint; `2f7919a` is an ancestor of that source. These are process/configuration and local/hosted exporter-path checks only: no production collector, dashboard, alert route, SLO, redaction, TiDB, RustFS or TLS health evidence is recorded here. | Define availability, mount/unmount, metadata latency, block latency, error-budget and recovery SLOs; map these probes and provider-specific checks to metrics/logs/traces, dashboards, alerts, paging, retention and secret/PII redaction; exercise an alert end-to-end. If the selected deployment needs provider-aware readiness, add and credential-test that separate probe. | **1–2 engineer-days**; **~0.75 engineer-day** for the HTTP slice, hardening and local observability qualification | Production collector, paging destination, ownership, alert thresholds, selected deployment topology and provider-aware probe credentials remain external gates. |
| **P06** | Capacity, performance, load and soak qualification | Hosted/provider + implementation | **Open — 20% gate weight; bounded hosted slice passed** | RustFS block benchmark and bounded provider/consumer tests passed, but they are not a representative production workload or capacity claim. `tests/provider_matrix/tidb-rustfs-soak.mjs` now provides a bounded TiDB/RustFS write-read workload with configurable operations, concurrency and payload size plus p50/p95/p99/throughput markers. The current hosted composition job passed both seed and reopen phases at 64 operations, concurrency 8 and 64 KiB payloads, with zero errors. | Establish workload mix and dataset/concurrency targets; retain baseline/peak/saturation/failover and multi-hour results against production-like capacity; record latency/throughput/error budgets, resource growth, headroom, scaling and cost limits. | **2–4 engineer-days** | Representative workload, load generators, production-sized service capacity, resource collector and performance acceptance thresholds are required. |
| **P07** | Security, transport and operational hardening | Implementation + provider | **Open — 20%** | Provider configuration is credential-gated and bounded tests reject unsafe fixture scope; the TiDB provider, SDK, CLI and N-API now compile with an opt-in `rustls` feature, `scripts/test-tidb-tls.sh` fails closed unless TLS plus CA/hostname verification are explicitly enabled, and `scripts/verify-w08-production-config.mjs` requires HTTPS blocks plus TLS-required TiDB input. No production TLS handshake, IAM/network/security review or image/dependency sign-off is recorded. | Enforce TLS and certificate rotation, network segmentation, authn/authz and tenant isolation; run dependency/image/SBOM scanning, threat-model review, audit verification, secret-redaction checks and security sign-off for the selected provider topology; execute the guarded script against the chosen credentialed endpoint and retain its terminal pass. | **2–4 engineer-days** | Security review, approved certificates, firewall/network policy, provider endpoint and hardening controls are external gates. |
| **P08** | Failure injection, runbooks, on-call and incident readiness | Hosted/provider + operations | **Open — 25%** | W08 exercised service restarts, provider fencing, ambiguous commit and RustFS fault recovery. `docs/W08-operations-runbook.md` now provides operator response procedures and D01–D09 timed-drill definitions, but no production incident drill or on-call acknowledgement is recorded. | Exercise client/process loss, metadata outage, object-store outage, stale lease, network partition, partial object write, rolling restart and restore; retain detection, diagnosis, mitigation, rollback and data-integrity checks; run an on-call tabletop and timed drill. | **1–2 engineer-days** | Named operators, staging access, incident tooling and an agreed escalation policy are needed. |
| **P09** | Release provenance, canary, go/no-go and rollback | Implementation + hosted | **Open — 10%; verifier, artifact-wiring, dedicated real-artifact, unsigned-SBOM, all-asset-integrity, target-matrix, attestation-wiring, dispatch-isolation and current-main retest slices passed** | `scripts/verify-w08-release-manifest.mjs` validates W08 source identity, semver/tag/target, artifact basename/SHA-256/size, `SHA256SUMS` metadata, GitHub Actions provenance and explicit signature/SBOM/canary states. `scripts/write-w08-release-manifest.mjs` derives the manifest from the real artifact bytes. `scripts/write-w08-release-sbom.mjs` and `scripts/verify-w08-release-sbom.mjs` derive and validate a CycloneDX 1.5 graph from locked Cargo metadata, bind it to the artifact SHA-256 and source commit, and reject mismatches. Both release workflows create `SHA256SUMS` after the manifest and unsigned SBOM exist, covering the tarball, `release-manifest.json` and `release-sbom.json`, and verify every entry before publication; the macOS CLI preview path then re-downloads and verifies all three assets. `.github/workflows/cli-release.yml` now requests the narrowly scoped GitHub OIDC/attestation permissions, creates pinned `actions/attest@v4.2.2` provenance and CycloneDX attestations for the release tarball, verifies them with `gh attestation verify` against repository, signer workflow, source commit and tag identity, then records verified signature/SBOM controls and rebuilds `SHA256SUMS`. `.github/workflows/w08-release-targets.yml` has an explicit `workflow_dispatch` `attest=true` path for the same per-target checks, and its concurrency group now separates manual dispatches from push-triggered runs. The first manual dispatch, run `35620392878` at source `0a4de6f`, was cancelled with zero jobs while the old shared pending group was occupied by concurrent main pushes; it is not evidence of a hosted pass or failure. The dedicated `.github/workflows/w08-release-policy.yml` proves the Ubuntu artifact path, while hosted run `35617415427`, source `b0ca8a9`, reached terminal success for both target builds and both download-verification jobs. Manual target attestations are now terminally qualified in runs `35624385556` and `35627761501`; the latter is a current-main-at-dispatch retest at source `50a33ac` and is recorded as W08.22 below. These runs prove hosted target provenance/SBOM verification, not approved tag publication, registry acceptance, canary, rollback or release approval. | The manual target-matrix qualification is complete; run the actual release workflow from an approved immutable tag, retain the published Linux/macOS asset sets, manifests, verified attestations and three-entry `SHA256SUMS` files; extend/verify any additional supported target packages; execute the staged canary with live SLO/alert observation; rehearse rollback and record explicit approval. | **~1–1.5 engineer-days** implementation/local verification for policy and artifact wiring; **~0.75 engineer-day** SBOM generation/verification and hosted integration; **~0.25 engineer-day** all-asset checksum hardening; **~0.75 engineer-day** Linux/macOS target matrix and download verification; **~0.5 engineer-day** attestation wiring and identity checks; **~0.25 engineer-day** dispatch concurrency hardening; **~1–2 engineer-days** provisional remaining release/canary/rollback integration, excluding external waits | Release-tag approval, GitHub OIDC/attestation availability and permissions, GitHub release permissions/registry, signing and SBOM attestation service, additional target runners, deployment controller, target-platform acceptance owner, canary, rollback target and release approvers are external dependencies. |

Live read-only production-boundary audit (**2026-09-22 07:35 AEST**) confirms
that P09 is externally blocked: the `w08-production` GitHub environment and
its environment-secret surface return HTTP 404, `gh release list` shows only
the `v0.1.0-cli-preview` prerelease, `git ls-remote` returns no
`v*-cli-production-candidate*` tag, and
`gh run list --workflow w08-production-release.yml` returns no runs. The
protected workflow file does exist on the remote mainline (blob
`9cacf0f19c8fc475df508684cf0ffcc591c96625`), so this is missing environment/
tag/execution configuration rather than missing repository code. No release,
canary, rollback or approval claim is inferred.

W08.28 extends the P09 implementation boundary with
`.github/workflows/w08-production-release.yml`: production-candidate tags build
both supported targets, re-verify downloaded assets, generate target-specific
final manifests/SBOMs, verify pinned provenance/SBOM attestations, and hold
prerelease publication behind the `w08-production` environment. The preview
workflow is now limited to `v*-cli-preview*` tags. This is static implementation
evidence only until the environment has required reviewers/policy and an
approved candidate tag reaches terminal success; it does not count as release
registry, canary, rollback or production approval evidence.

W08.29 provides the latest terminal hosted target qualification on source
`9d3a6e502eccec9ba54c00e80c98e6e1da175177`: run `35638433010` passed both
Linux/macOS build and download-verification jobs, both provenance attestations,
both CycloneDX SBOM attestations and both final identity verifiers. The two
downloaded target bundles were independently rechecked locally, including their
checksums, manifests, 288-component SBOMs and tar contents. This does not
replace the protected candidate-tag release, registry publication, canary,
rollback or owner approval.

W08.31 requalified the target path in hosted run `35641555767`, source
`2bbd0266a094b06bf97d51baafe0d3a8800bfec5`. Build jobs
`106471833712` (Linux x86_64) and `106471833967` (macOS arm64), downloaded
asset jobs `106473358964` and `106473359006`, and attestation jobs
`106475107803` and `106475108148` all reached terminal success. The Linux
tarball SHA-256 is
`6a3de0a607ffafcedf6bd3385c3849a30208df0345120061061821109a09ac79`
(8,373,376 bytes); the macOS arm64 tarball SHA-256 is
`75a031c4e439ede07f0fa1a09db050b15c45f6d802a703d90b67cde52b4a941d`
(6,958,388 bytes). Hosted logs emitted both target-build, downloaded-target
and `W08_RELEASE_TARGET_ATTESTATION_PASS` markers. Local rechecks returned
three checksum `OK` lines per target, manifest-policy PASS, SBOM PASS with
`components=288`, expected `mount-rs` archive contents, and `mount-rs 0.1.0`
from the extracted macOS arm64 binary. Independent `gh attestation verify`
calls passed for both the default provenance and CycloneDX predicates with
exact repository, signer-workflow, source-digest, `refs/heads/main` and
`--deny-self-hosted-runners` constraints. This remains hosted target
qualification, not a production-candidate tag release, registry acceptance,
canary, rollback or release-owner approval.

### Production go/no-go rule

The rollout remains **NO-GO** while any P01–P09 item is open, skipped,
credential-blocked, provider-unverified, or supported only by local planning
documents. A later aggregate CI cancellation must not be converted into a
production pass, and W08's Linux live-provider evidence must not be promoted to
macOS/provider or production evidence. The first production milestone is a
staging deployment with real provider credentials and retained evidence for
P01–P05; P06–P09 then gate canary and production approval.

## Work-item ledger

| Work item | Status and completion | Implementation evidence | Provider/hosted/native evidence | Remaining actions | Provisional engineering time | External blockers |
| --- | --- | --- | --- | --- | --- | --- |
| **W08.1** Separate TiDB metadata/block implementation and dependencies | **Landed — 100%** | `integrations/mount-rs-tidb/**` is a separate provider crate. The tracker records the implementation landing as `ca57758`; format checks, four unit tests and acceptance binaries passed. The provider has explicit pessimistic transactions, writer fencing, provider-clock reads, CAS publication and ambiguous-commit mapping. | Local focused TiDB library tests passed (`6 passed`). The real v8.5.7 single-node lane has passed schema, UTF-8/trailing-space, identity and provider checks. | No remaining implementation action in this item. Broader replicated-service evidence belongs to W08.2/W08.3. | **0–1 h** for maintenance/documentation only. | None for the landed scope. |
| **W08.2** Durable real TiDB/PD/TiKV Docker harness and restart results | **Complete — 100%** | The harness is pinned to TiDB `v8.5.7`, owns a durable 3PD/3TiKV topology, validates capacity/readiness/identity, replaces the frontend for a fresh listener lifecycle, restarts TiKV and PD, and reruns the persisted provider contract. Published implementation chunks include `a452220`, `17c8bda`, `c0aa081`, `63dbdbd` and `ece6977`. | CI `35585066458` / `tidb` job `106286459436` passed network, PD, TiKV and TiDB readiness, the frontend replacement, TiKV restart, PD restart and the post-restart provider rerun. Marker: `TIDB_ACCEPTANCE evidence=durable-multinode-restart topology=durable version=v8.5.7 platform=linux/amd64 cpus=4 mem_bytes=16766414848 ambiguous_commit=pass`. | No required W08.2 action remains. Keep the local capacity limitation documented and do not reinterpret this hosted test-cluster result as a host power-loss/fsync guarantee. | **0–1 h** maintenance/documentation only; hosted gate consumed approximately **20 min wall time** in the retained run. | Local Docker remains below the durable memory threshold (`8,232,747,008` available versus `10,737,418,240` required); this is an environment boundary, not an open hosted acceptance blocker. |
| **W08.3** Provider time/fencing, ambiguous commits, concurrency and deployment durability | **Complete — 100%** | The provider maps statement conflicts to `EAGAIN`, stale leases to `ESTALE`, and unknown commit outcomes to a distinct backend error. The published ordering refinement `ecc1106` runs ambiguous-commit injection after durable restart/reopen, and `2e66f94` supplies the strict-Clippy cleanup. | In CI `35585066458`, direct `tidb` job `106286459436` and composite job `106286459715` passed provider identity/schema, provider-clock fencing, concurrent publication, ambiguous-commit no-replay, frontend/TiKV/PD restart and persisted fresh-client checks. | No required W08.3 action remains. Preserve the boundary that liveness/readiness and container restart are service durability evidence, not a universal fsync or power-loss claim. | **0–1 h** maintenance/documentation only; approximately **20 min hosted wall time** retained. | None for the defined W08.3 acceptance. The aggregate workflow had unrelated failures, recorded separately below. |
| **W08.4** Node, CLI, native-mount and macOS/Linux acceptance coverage | **Complete — 100%** | Public Rust SDK/N-API and Rust/Node CLI configuration paths compose TiDB metadata with RustFS/S3-compatible `r2` chunks. Native scripts distinguish credentials, transport, mount, unmount, fresh-provider readback and cleanup. | CI `35585066458` passed `tidb-rustfs` job `106286459715`, ARM Node job `106286459639`, Linux FUSE job `106286459998`, Ubuntu NFS job `106286459483`, and macOS NFS job `106286459246`. | No required W08.4 action remains. Platform rows retain their provider boundaries: live TiDB/RustFS is Linux FUSE; macOS/Ubuntu NFS are native-platform lifecycle gates. | **0–1 h** reconciliation only; approximately **25 min hosted wall time** across retained rows. | Local macOS lacks the required native-device/service prerequisites; hosted platform jobs provide the evidence. |
| **W08.4a** Bounded Node/CLI consumer slice | **Complete bounded scope — 100%** | The provider matrix covers configuration, explicit `durable`, partial write, truncate, shutdown, reopen and owned RustFS-prefix cleanup. Published chunks include `f55f2eb`, `5556872` and `6038772`; hosted `tidb-rustfs` retained all live Node SDK/CLI rows. | CI `35585066458` / job `106286459715` passed the Node SDK and CLI matrices, with live TiDB/RustFS cases executed rather than credential-skipped. Local credential-gated skips remain explicit and are not used as hosted evidence. | No required W08.4a action remains. | **0–1 h** maintenance only. | Local credentials/services are absent, but the hosted gate is complete. |
| **W08.4b** Native-mount and live TiDB/RustFS consumer acceptance | **Complete — 100%** | `71a7a4c` uses current-process mount identity; `6b75cea` is formatting cleanup; `2221144` releases the provider after transport unmount. `ece6977` replaces the TiDB frontend during durable restart, and `ecc1106` isolates ambiguous-commit injection from restart readiness. | CI `35585066458` / `tidb-rustfs` job `106286459715` passed live Linux TiDB/RustFS Node and CLI matrices, independent Rust/Node mounted I/O, clean unmount, fresh provider readback, retained client bytes, TiDB/TiKV/PD restart and RustFS reopen. Standalone Linux FUSE `106286459998`, ARM Node `106286459639`, Ubuntu NFS `106286459483` and macOS NFS `106286459246` also passed. | No required W08.4b action remains. The macOS NFS row is native lifecycle evidence only; it does not claim live TiDB/RustFS ran on macOS. | **0–1 h** reconciliation only; approximately **15 min** for the retained composite/native rows. | Local FUSE/Docker/credential limitations remain non-hosted environment boundaries. |
| **W08.5** TiDB metadata + RustFS S3 chunks | **Complete bounded scope — 100%** | The real v8.5.7 TiDB service and pinned RustFS endpoint passed mixed-provider seed, partial write, truncate, reopen, CAS/fencing and exact owned cleanup; persisted fixtures require explicit volume/prefix/manifest scope and reject transient or symlink paths. | CI `35585066458` / job `106286459715` emitted `TIDB_CHUNKED_RUSTFS_SEED_PASS`, `TIDB_CHUNKED_RUSTFS_REOPEN_PASS`, `RUSTFS_COMBO_PASS`, `RUSTFS_RESTART_REOPEN_PASS` and `RUSTFS_INTEGRATION_PASS`; RustFS restart/fault recovery also passed. | No required W08.5 action remains. | **0–1 h** documentation only. | None for the bounded scope; broad provider claims remain limited to the explicitly exercised durable topology. |
| **W08.6** Production deployment-contract policy and fail-closed configuration checks | **Implementation slice landed — 100%; production gate open** | `scripts/verify-w08-production-config.mjs` validates a `splitstore` shape with durable TiDB metadata, durable HTTPS S3/R2 blocks, external secret references, positive chunk size, no placeholders and TLS-required `MOUNT_RS_TIDB_TLS_URL`. The positive fixture passed, the insecure fixture failed closed, and the public Rust CLI `validate-config` command accepted the positive fixture. | CI run `35592902494`, source `4326c54`, `tidb-tls-compile` job `106311076905` is a terminal success for TLS consumer compilation, positive/negative TLS URL policy and positive/negative W08 production-config policy without credentials or network. Policy success remains implementation evidence and does not close topology, IAM, certificate, provider or production gates. | Run this verifier against the approved staging/production artifact; add the target topology, secret-manager, certificate and owner evidence to P01/P02/P07. | **~1 h** implementation and local verification; ~1–2 min hosted wait for the terminal policy job. | Approved deployment artifact, secret manager, IAM policy, certificates, network controls and provider endpoint are external. |
| **W08.7** Production operator runbook, backup/restore procedure and timed-drill matrix | **Implementation artifact landed — 100%; execution gates open** | Commit `ea01338`, published in reconciled tip `0e624d3`, added `docs/W08-operations-runbook.md` with evidence capture, admission/preflight, staged promotion, incident actions, TiDB/block/fencing/ambiguous-commit/TLS/capacity responses, backup/restore and rollback steps, observability handoff, and drills D01–D09. It explicitly marks expected procedures as templates and does not invent owners or PASS results. | No new provider/native/hosted execution is claimed. Retained W08 restart/fencing/RustFS-fault jobs are qualification inputs only; P03–P09 still require terminal evidence from the selected production-like environment. | Name owners and execute D01–D09 against staging; attach redacted logs, timestamps, integrity checks, RPO/RTO/SLO measurements, cleanup and rollback results to the corresponding P03–P09 rows. | **~1–2 engineer-days** documentation and review; drill execution is external and provisional. | Approved staging/production topology, collector/pager, backup system, release controller, named operators and data/release owners are not supplied. |
| **W08.8** Bounded TiDB/RustFS load and soak evidence harness | **Implementation and bounded hosted slice passed — 100%; production capacity gate open** | `tests/provider_matrix/tidb-rustfs-soak.mjs` validates bounded operation/concurrency/payload inputs, performs concurrent write/read integrity checks through the public N-API chunked driver, computes p50/p95/p99 latency and throughput, cleans only its owned RustFS prefix, and fails closed when the TiDB URL or RustFS environment is absent. Local syntax, skip and missing-gate rejection checks passed. | CI run `35595664981`, source `c5532e3`, `tidb-rustfs` job `106319766691` was terminal success. The job emitted `TIDB_CHUNKED_RUSTFS_SEED_PASS`, then `TIDB_RUSTFS_SOAK_PASS operations=64 concurrency=8 payload_bytes=65536 elapsed_ms=5578.76 p50_ms=668.59 p95_ms=918.49 p99_ms=918.62 throughput_ops_s=11.47 errors=0`; after reopen it emitted `TIDB_CHUNKED_RUSTFS_REOPEN_PASS`, then `TIDB_RUSTFS_SOAK_PASS operations=64 concurrency=8 payload_bytes=65536 elapsed_ms=5266.09 p50_ms=648.69 p95_ms=815.50 p99_ms=816.83 throughput_ops_s=12.15 errors=0`. The same terminal job emitted the durable TiDB restart, RustFS combo, RustFS restart/reopen and integration markers. This remains bounded qualification, not a production capacity, cost or SLO claim. | Repeat with the approved production-shaped workload, resource collector, failover and multi-hour duration; define acceptance thresholds and retain resource/headroom, scaling and cost evidence. | **~1 engineer-day** implementation and local verification; ~10 min hosted/provider wait; production execution remains provisional. | Real TiDB/RustFS service, workload approval, production-sized capacity, resource telemetry, failover injection and performance owner are external. |
| **W08.9** Production HTTP health/readiness boundary | **Implementation and hosted contract slice passed — 100%; P05 external gate open** | Commit `30dec1d` adds `/healthz` and `/readyz` to `crates/mount-rs-http`, documents their unauthenticated and bounded semantics, and adds integration coverage for healthy readiness, empty-registry `503` and method rejection. The default locked suite passed 8 unit and 10 integration tests; the all-features suite passed 8 unit and 13 integration tests including telemetry/OTLP propagation; strict Clippy passed for both configurations. | Published in merge tip `d6abcbe`. Hosted CI run `35599817215`, source `b26819e`, job `106333141914` reached terminal success for the all-features test and strict-Clippy steps. This remains implementation/hosted contract evidence only; it does not claim provider, object-store, TLS, collector, dashboard, paging or production SLO health. | Select the production collector and owner; define thresholds/SLOs and alert routing; map the local probes to the deployment’s provider-specific checks; run and retain a credentialed end-to-end alert test. | **~0.5 engineer-day** implementation and local verification; ~1 min hosted job time | Selected deployment topology, collector/pager, SLO policy, on-call owner and provider credentials are external. |
| **W08.10** Health probe response hardening | **Implementation and hosted contract slice passed — 100%; P05 external gate open** | Commit `2f7919a` adds `Cache-Control: no-store` and `X-Content-Type-Options: nosniff` to health/readiness responses, documents the deployment contract, and adds regression assertions. On reconciled source `2159976`, the all-features suite passed 8 unit and 13 integration tests, doctests and strict Clippy. | Terminal hosted CI run `35601990956`, source `f09fbe9`, job `http-observability` `106340034907` passed the hardening-bearing all-features test and strict-Clippy steps; `2f7919a` is an ancestor of `f09fbe9`. Earlier candidates were cancelled by main-branch concurrency, but this terminal job supersedes that queue history. The result remains HTTP contract evidence only and does not claim provider, collector, dashboard, paging or production SLO health. | Map the probes to the selected production collector and provider-specific health checks; define thresholds/SLOs and alert routing; run and retain a credentialed end-to-end alert test. Production collector, SLO, paging and provider-aware health remain separate gates. | **~0.25 engineer-day** implementation and local verification; ~1 min hosted job time | Collector/pager, SLO policy, selected deployment, provider credentials and named on-call owner remain external. |
| **W08.11** Release identity and provenance policy gate | **Implementation and hosted policy slice passed — 100%; P09 external gates open** | `scripts/verify-w08-release-manifest.mjs` is credential-free and validates immutable W08 source/repository identity, semver/tag/target, artifact basename/SHA-256/size, `SHA256SUMS`, GitHub Actions builder/workflow/run provenance and explicit signature/SBOM/canary states. `tests/tidb/release-manifest-policy.json` passed in pending mode, `.accepted.json` passed with `--require-release-acceptance`, and `.invalid.json` failed closed on a placeholder source commit. | CI run `35606873984`, source `fecec0e`, `w08-release-policy` job `106356402785` reached terminal success for all three fixture paths. The job is hosted implementation/policy evidence only; it does not create or verify a real release artifact, signature, SBOM, canary, rollback or approval. | Wire the verifier to the actual artifact-producing release workflow; materialize real checksums and provenance; generate and verify signatures/SBOMs; verify target-platform packages; execute canary/rollback and obtain explicit release-owner approval. | **~0.75 engineer-day** implementation/local verification; **~1–2 engineer-days** provisional remaining release integration, excluding external waits; ~1 min hosted policy job | Signing and SBOM tooling, registry/release workflow, deployment controller, target-platform canary, rollback target and release approvers are external. |
| **W08.12** Release artifact manifest generation and CLI preview workflow wiring | **Implementation and hosted policy slice passed — 100%; P09 release execution gates open** | `scripts/write-w08-release-manifest.mjs` reads a regular artifact, computes its SHA-256 and byte size, and records source/tag/target/workflow/run provenance with pending release-control states. `.github/workflows/cli-release.yml` now generates and verifies `release-manifest.json` for the actual CLI tarball before upload, publishes it beside `SHA256SUMS`, and re-downloads/re-verifies it after publication. | Local `./scripts/cargo-shared build --locked --release -p mount-rs-cli` produced `mount-rs 0.1.0`; the production-shaped tarball path passed manifest generation, direct artifact verification, `shasum -a 256 -c SHA256SUMS` and tar-content checks (`sha256=da0782596d7402ce64869f2037075d95b273f59f389b9493e26a0ba1e39822c7`, `size_bytes=6910193`). Hosted CI run `35609172786`, source `66544b5`, `w08-release-policy` job `106363893748` reached terminal success for the generator plus all policy fixtures. This does not execute the tag-triggered publication or prove signing/SBOM/canary/rollback/approval. | Run the tag-triggered workflow from an approved immutable release commit; inspect the published asset set and manifest; add/verify signatures and SBOM; run target-platform acceptance, canary and rollback with named approval. | **~1–1.5 engineer-days** implementation/local verification; ~1 min hosted policy job; actual release execution is provisional and external | Approved release tag, GitHub release permissions/registry, signing/SBOM services, target-platform runners, canary environment, rollback owner and release approver are external. |
| **W08.13** Dedicated non-cancelling hosted release-policy gate | **Implementation and hosted real-artifact slice passed — 100%; P09 release execution gates open** | Commit `a9f51e4` moved the W08 release-policy job into `.github/workflows/w08-release-policy.yml`, removed the duplicate cancellable job from monolithic CI, and set `cancel-in-progress: false` so unrelated main pushes do not erase the gate. The workflow builds the locked CLI release on Ubuntu, packages a real tarball, writes/verifies `SHA256SUMS` and `release-manifest.json`, checks direct and extracted binary versions, and runs pending/strict-accepted/expected-negative policy fixtures. Local YAML parsing and Node syntax/diff checks passed. | Hosted CI run `35611883547`, source `f432441`, `w08-release-policy` job `106372777281` reached terminal `completed/success`; setup, real release build/package, manifest/checksum verification, tar-content check, direct and extracted `mount-rs --version` smoke checks and fixture paths passed. This is stable hosted implementation/artifact evidence, not a published tag release: no signing/SBOM, target-platform matrix, canary, rollback or approval is claimed. | Execute the approved tag-triggered release workflow; inspect the published asset set and manifest; add/verify signatures and SBOM; run target-platform acceptance, canary and rollback with named approval. | **~0.5 engineer-day** implementation/reconciliation; **~3 min** hosted job time; remaining external release execution **~1–2 engineer-days** provisionally, excluding waits | Approved release tag, GitHub release permissions/registry, signing/SBOM services, target-platform runners, canary environment, rollback owner and release approver are external. |
| **W08.14** Unsigned SBOM generation and release-artifact binding | **Implementation and hosted unsigned-SBOM slice passed — 100%; P07/P09 signing and release gates open** | `scripts/write-w08-release-sbom.mjs` selects the `mount-rs-cli` transitive Cargo dependency closure from locked metadata and writes a CycloneDX 1.5 document with repository/source/artifact properties. `scripts/verify-w08-release-sbom.mjs` validates the graph, license shape, component/dependency references, source identity and artifact SHA-256/size, with a deliberate wrong-source negative test passing locally. Both `.github/workflows/cli-release.yml` and the dedicated policy workflow generate and verify the SBOM; the preview workflow includes it in the published/re-downloaded asset set. | Hosted CI run `35614345209`, source `9c9d0e4` (which contains implementation commit `9f6002c`), `w08-release-policy` job `106381893114` reached terminal `completed/success` in 2m40s. The Ubuntu artifact path emitted `W08_RELEASE_SBOM_GENERATED` and `W08_RELEASE_SBOM_PASS` for `mount-rs-cli 0.1.0`, `components=288`, tarball SHA-256 `c3d1a3200c06830f95527dd62310455e66b4632080aee7ead3a118e4783f4543`; manifest controls remained `signature=pending sbom=pending canary=not-run`. The run's preceding candidate `35614303261` was cancelled before a job and is not counted. | Run an approved tag-triggered release; inspect the real published SBOM asset, add/verify cryptographic signing/attestation, verify target-platform SBOM/package parity, then execute canary/rollback and obtain release-owner approval. | **~0.75 engineer-day** implementation/local verification; **~2m40s** hosted job time; remaining signed SBOM/release execution **~1–2 engineer-days** provisionally, excluding waits | Signing/attestation service, release registry permissions, target-platform build matrix, deployment controller, canary environment, rollback owner and release approver are external. |
| **W08.15** All-release-asset checksum coverage and post-generation verification | **Implementation and hosted integrity slice passed — 100%; P09 publication/signing gates open** | `.github/workflows/cli-release.yml` and `.github/workflows/w08-release-policy.yml` now generate `SHA256SUMS` after the tarball, `release-manifest.json` and `release-sbom.json` exist, then verify all three entries. Local macOS-shaped packaging passed manifest/SBOM verification, a three-entry checksum file, repeated `shasum -a 256 -c SHA256SUMS`, tar-content and direct `mount-rs 0.1.0` checks. | Hosted CI run `35615714935`, source `5116ded`, `w08-release-policy` job `106386240669` reached terminal `completed/success` in 2m35s. The real Ubuntu artifact path emitted tarball SHA-256 `3ec51c00c1aacccb29d436050061a5c4a007fd4f31f0c8bd1111797d29e3a0d4`; `mount-rs-0.1.0-x86_64-unknown-linux-gnu.tar.gz: OK`, `release-manifest.json: OK` and `release-sbom.json: OK` all passed, followed by the existing runtime and fixture checks. | Run the approved tag-triggered workflow and inspect the actual downloaded asset set; retain the three-entry checksum, cryptographic signing/attestation and target-platform parity evidence before canary/rollback/approval. | **~0.25 engineer-day** implementation/local verification; **~2m35s** hosted job time; remaining publication/signing/target-platform execution **~1–2 engineer-days** provisionally, excluding waits | Approved release tag, GitHub release permissions/registry, signing/attestation service, target-platform runners, canary environment, rollback owner and release approver are external. |
| **W08.16** Linux x86_64 and macOS arm64 target-package matrix with hosted download verification | **Implementation and hosted target-matrix slice passed — 100%; P09 publication/signing gates open** | `.github/workflows/w08-release-targets.yml` builds and tests the CLI on `ubuntu-latest` (`x86_64-unknown-linux-gnu`) and `macos-14` (`aarch64-apple-darwin`), generates/verifies the manifest and 288-component SBOM, creates three-entry checksums, runs direct/extracted `mount-rs --version`, uploads the four-file asset set and verifies each downloaded set on Ubuntu. Local workflow YAML/Bash validation and the locked `mount-rs-cli --all-targets` suite passed (44 unit, 9 CLI integration, 2 HTTP subprocess and 1 native-artifact test; only explicit environment-gated cases ignored). | Hosted CI run `35617415427`, source `b0ca8a9`: Linux build job `106391572292` passed in 3m41s; macOS arm64 build job `106391572540` passed in 4m27s; downloaded macOS verification job `106393402528` passed in 5s; downloaded Linux verification job `106393402680` passed in 10s. Linux tarball SHA-256 `875a06fb975d8a294141f5b70841c481768655ac3c5c56a721604216ee12ff2c`; macOS arm64 tarball SHA-256 `50f466bbe22b2fa09ecd8f1246fb278bd2b190fed52f940de21bb8d414baa9bd`; both target build markers and both `W08_RELEASE_TARGET_DOWNLOAD_PASS` markers passed. | Run the approved tag-triggered release workflow and inspect the real published target asset set; add/verify cryptographic signing/attestation for each target, target/SBOM parity and any additional supported platforms; then execute canary/rollback and obtain release-owner approval. | **~0.75 engineer-day** implementation/local verification; **~3m41s Linux + ~4m27s macOS** hosted build time; **~5–10s** each hosted download verification; remaining signed multi-target release execution **~1–2 engineer-days** provisionally, excluding waits | Approved tag and release registry, macOS/Linux runner availability, signing/attestation service, additional target support decision, deployment controller, canary environment, rollback owner and release approver are external. |
| **W08.17** Hosted cryptographic provenance and CycloneDX SBOM attestation wiring | **Implementation and manual target-attestation slice passed — 100%; approved tag execution open** | `.github/workflows/cli-release.yml` now grants only the release job's `id-token`, `attestations` and `artifact-metadata` permissions in addition to release contents write; it invokes pinned `actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6` (`v4.2.2`) for the exact CLI tarball and SBOM, verifies both with `gh attestation verify` using repository, signer workflow, source commit, tag ref and hosted-runner identity constraints, then rewrites the manifest as `signature=verified sbom=verified` and recomputes all three checksums. `.github/workflows/w08-release-targets.yml` adds an explicit manual `attest=true` job for both target asset sets, with the same provenance/SBOM predicates and identity checks. Local YAML parsing, embedded Bash syntax, `git diff --check` and the installed `gh attestation verify` flag surface passed. The first live target qualification reached both target builds and both downloaded-asset checks, but its two attestation jobs failed during setup because the action pin was shortened; no action step ran and no Sigstore bundle was created. | Hosted run `35620932700`, source `11a7b22`, target builds `106403507404` (macOS arm64, 3m57s) and `106403507916` (Linux x86_64, 3m40s) passed; downloaded-asset jobs `106406448310` (macOS, 5s) and `106406448372` (Linux, 9s) passed. Attestation setup jobs `106406881524` and `106406881625` failed before action execution with GitHub's exact error that the shortened ref must use full SHA `1e69f48acb82d1966a394da916b4c1698aa569d6`. The later terminal manual target qualification is recorded in W08.21. | Retain the terminal manual target action outputs, attestation IDs/URLs, `gh attestation verify` results, finalized manifests and checksums, then run the approved immutable tag release. Do not close P09 until target-platform release acceptance, canary, rollback and explicit owner approval are also recorded. | **~0.5 engineer-day** implementation and local workflow validation; **~0.25 engineer-day** hosted failure diagnosis/pin correction; tag execution remains provisional, excluding queue time. | GitHub public-repository/plan support, OIDC and attestation permissions, approved tag/release owner, release registry, canary environment, rollback owner and final approver are external. |
| **W08.18** Attestation dispatch concurrency isolation | **Implementation slice landed — 100%; isolated manual attestation passed; approved tag execution open** | `.github/workflows/w08-release-targets.yml` now keys concurrency by both event type and ref, so the explicit manual `workflow_dispatch` attestation qualification does not share the pending slot with push-triggered target runs. The first dispatch attempt `35620392878` (source `0a4de6f`) was cancelled before job creation while the old shared `main` group was being replaced by concurrent pushes; its `jobs=[]` result is recorded as a boundary, not a PASS. | The later isolated run `35624385556` created both attestation jobs and reached terminal success; its exact target hashes, repository records and Rekor entries are recorded in W08.21. | Retain the terminal target-attestation evidence and run the approved tag release. | **~0.25 engineer-day** workflow hardening and diagnosis; hosted qualification completed in ~5 minutes excluding queue time. | GitHub Actions OIDC/attestation permissions, release registry and approved tag/release owner remain external. |

| **W08.19** Full SHA pin correction after hosted setup failure | **Implementation correction and manual rerun passed — 100%; approved tag execution open** | Both attestation workflow references now use the full immutable `actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6` SHA. The correction is based on the terminal GitHub setup error, not an inferred local result. | Run `35620932700`, source `11a7b22`: target build/download jobs passed, while attestation jobs `106406881524` and `106406881625` failed in `Set up job` because the shortened ref was rejected. No action step or OIDC/Sigstore operation ran. The corrected terminal run is recorded in W08.21. | Retain the terminal attestation action outputs and `gh attestation verify` results, then run the approved tag release. | **~0.25 engineer-day** diagnosis and correction; manual hosted rerun completed in ~5 minutes excluding queue time. | GitHub action resolution, OIDC/attestation availability, repository plan and external release approval remain gates. |
| **W08.20** Hosted attestation verifier identity-flag correction | **Implementation correction and manual rerun passed — 100%; approved tag execution open** | `gh attestation verify` now supplies the precise `--signer-workflow` identity without the mutually exclusive `--signer-repo` flag in both the tag-release and target-matrix workflows. | Run `35622899242`, source `2f43721`: Linux build `106410068242`, macOS build `106410067711`, downloaded Linux `106411933587` and downloaded macOS `106411933583` all passed. Both target attestation jobs generated provenance and CycloneDX SBOM attestations, then failed only at verification because GitHub CLI reported the mutually exclusive identity flags. The corrected terminal run `35624385556` passed both verifier jobs; exact records are in W08.21. | Retain the terminal verifier output, attestation IDs/URLs and target hashes, then run the approved tag release. | **~0.25 engineer-day** verifier diagnosis and correction; manual hosted rerun completed in ~5 minutes excluding queue time. | GitHub CLI verifier behavior, OIDC/attestation availability, release registry and external release approval remain gates. |
| **W08.21** Terminal hosted target attestation qualification | **Hosted target qualification passed — 100%; P09 tag/canary/approval gates open** | The manual `attest=true` path now qualifies both target assets through build/package, download verification, provenance attestation, CycloneDX SBOM attestation and final `gh attestation verify`. | Run `35624385556`, source `2ab3cf1`: build jobs `106415027916` (Linux) and `106415027629` (macOS), download jobs `106416531412` (Linux) and `106416531464` (macOS), attestation jobs `106416627127` (Linux) and `106416627177` (macOS) all reached terminal success. Linux tarball SHA-256 `5f7f3c6345013144d8889b107cde41c9e5b69d688e21a975ba6fbb33ce2507d6`, size 8,277,051 bytes; macOS arm64 SHA-256 `add8e365c0ab1c0390267531144b77b6c7cf7f338d03ce0a549a5783c834fc8e`, size 6,910,097 bytes. Repository attestation records are `48984689`, `48984696`, `48984678`, `48984687`; Rekor entries are `2906371944`, `2906371970`, `2906371892`, `2906371927`. | Run the approved tag-triggered CLI release, inspect the actual published assets/manifest/checksums/SBOM, verify release-registry acceptance, then execute staged canary, rollback and owner approval. | **~0.25 engineer-day** evidence capture and ledger update; **~5 min** hosted qualification excluding queue time; tag/canary/rollback execution remains provisional. | Approved immutable tag, release registry, deployment controller, canary environment, rollback owner and release approver are external. |
| **W08.22** Current-main hosted target-attestation retest | **Hosted target qualification passed — 100%; P09 tag/canary/approval gates open** | The manual `workflow_dispatch` `attest=true` path was rerun after W08.20's verifier correction against source `50a33ac`; both target packages again completed build/package, download verification, provenance attestation, CycloneDX SBOM attestation and final `gh attestation verify`. | Run `35627761501` completed with `success` at source `50a33ace3880632bec514ed9f163b84906376f5e`. Linux build `106426208811`, macOS build `106426208445`, downloaded Linux verification `106427912681`, downloaded macOS verification `106427912698`, attestation Linux `106428371158` and attestation macOS `106428371145` all completed successfully. Linux tarball SHA-256 `432049a39cd648bda95a9aeaaf81d5bb2933267c467f69b617e6e3f718d5303b`, size `8,306,821` bytes; macOS arm64 SHA-256 `647f4d0fe7cb7f8446ed6eba3a2c1ad4b4f7413ad59afa10d68617b86ec10421`, size `6,926,739` bytes. Repository attestation records are `48993605` and `48993613` (Linux provenance/SBOM), `48993668` and `48993679` (macOS provenance/SBOM); Rekor entries are `2906426581`, `2906426592`, `2906426797` and `2906426824`. Both `W08_RELEASE_TARGET_ATTESTATION_PASS` markers passed. This is a current-main-at-dispatch hosted qualification, not approved tag publication, registry acceptance, canary, rollback or release approval. | Run the approved tag-triggered CLI release, inspect the actual published assets/manifest/checksums/SBOM, verify release-registry acceptance, then execute staged canary, rollback and owner approval. | **~0.25 engineer-day** evidence capture; **~7 min** hosted qualification excluding queue time; tag/canary/rollback execution remains provisional. | Approved immutable tag, release registry, deployment controller, canary environment, rollback owner and release approver are external. |
| **W08.23** Current shared-main workspace verification | **Source-level verification passed — 100%; provider/native and P01–P09 gates remain open** | On source `5aae52b`, `./scripts/cargo-shared test --workspace --all-targets --locked` completed successfully, followed by `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings`, which also completed successfully. All non-ignored workspace tests passed; rows requiring TiDB, RustFS, PGlite, R2, FUSE, NFS or other host capabilities remained explicit skips/ignores. | No new live-provider, native-kernel, hosted or production evidence was claimed by this local gate. The test/lint result verifies repository source health only. | No W08 implementation action remains from this gate. Keep the explicit provider/native skips and close P01–P09 only with real production-like evidence, named owners and a GO decision. | **~0.25 engineer-day** verification; ~53s test execution and ~35s Clippy execution, excluding setup/wait. | TiDB/RustFS/PGlite/R2 credentials/services, native mount capabilities and all production rollout owners/environments remain external. |
| **W08.24** Current published-main hosted target-attestation qualification | **Hosted target qualification passed — 100%; P09 tag/canary/approval gates open** | The manual `workflow_dispatch` `attest=true` path was run from the published `main` tip after W08.23; both target packages completed build/package, download verification, provenance attestation, CycloneDX SBOM attestation and final `gh attestation verify`. | Run `35631063978` completed with `success` at source `cf75835e5a4b00c1e9b31f4060093710df610ece`. Linux build `106437098891`, macOS build `106437098611`, downloaded Linux verification `106439285204`, downloaded macOS verification `106439285356`, attestation Linux `106439636662` and attestation macOS `106439636979` all completed successfully. Linux tarball SHA-256 `54220877022f67640aa7b470a8eaa09b24df1d867e00463533642b6fa462182a`, size `8,315,540` bytes; macOS arm64 SHA-256 `6df488c71a0eb65674af9adf7660b870424b4e47227d832028180947dbb83b39`, size `6,918,800` bytes. Repository attestation records are `49001528` and `49001543` (Linux provenance/SBOM), `49001531` and `49001545` (macOS provenance/SBOM); Rekor entries are `2906464391`, `2906464436`, `2906464411` and `2906464449`. Both `W08_RELEASE_TARGET_ATTESTATION_PASS` markers passed. This is hosted qualification from published main, not approved tag publication, registry acceptance, canary, rollback or release approval. | Run the approved tag-triggered CLI release, inspect the actual published assets/manifest/checksums/SBOM, verify release-registry acceptance, then execute staged canary, rollback and owner approval. | **~0.25 engineer-day** evidence capture; **~7 min** hosted qualification excluding queue time; tag/canary/rollback execution remains provisional. | Approved immutable tag, release registry, deployment controller, canary environment, rollback owner and release approver are external. |
| **W08.25** Current shared-main workspace verification after the FUSE session fix | **Source-level verification passed — 100%; provider/native and P01–P09 gates remain open** | On source `f16eec2a6a1f79a5543ba345c67b86eaae837312`, `CARGO_TARGET_DIR=/private/tmp/mount-rs-w08-current-cargo-target ./scripts/cargo-shared test --workspace --all-targets --locked` completed successfully, followed by the matching strict workspace Clippy command with `-D warnings`. All non-ignored workspace tests passed; rows requiring TiDB, RustFS, PGlite, R2, FUSE, NFS or other host capabilities remained explicit skips/ignores. Two earlier attempts against the configured shared Cargo target failed before test execution because its dependency metadata/artifacts were missing; those are recorded as target-cache infrastructure failures, not source-test results. | Fresh bounded target: full locked workspace test finished 0 with all executed tests passing (1m58s); strict workspace Clippy finished 0 with `-D warnings` and no diagnostics (38s). No live-provider, native-kernel or production evidence is claimed by this chunk. | No implementation action remains from this gate. Preserve the provider/native skips and close P01–P09 only with real production-like evidence, named owners and a GO decision. | **~0.25 engineer-day** cache diagnosis, fresh-target verification and evidence capture; **~1m58s** test plus **~38s** Clippy execution, excluding compilation/setup wait | The configured shared Cargo target remains unreliable for this session; TiDB/RustFS/PGlite/R2 credentials/services, native mount capabilities and all production rollout owners/environments remain external. |
| **W08.26** Current-main hosted Linux/macOS target and attestation qualification | **Hosted target qualification passed — 100%; P09 tag/canary/approval gates open** | Manual `workflow_dispatch` with `attest=true` completed target build/package, downloaded-asset verification, provenance attestation, CycloneDX SBOM attestation and final `gh attestation verify` for Linux x86_64 and macOS arm64 from source `56aae0c3f28686e225bb5feb0c528e50acc0a1e8`. | Run `35633841116` completed with `success`. Linux build `106446286719`, macOS build `106446286427`, downloaded Linux verification `106447678688`, downloaded macOS verification `106447678718`, Linux attestation `106448672423` and macOS attestation `106448672409` all passed. Linux tarball SHA-256 `5ef9ddb46b5a7967fb26b6379a217f648291d2598be75e9c7d8847f7aa42371c`, size `8,328,164` bytes; macOS arm64 SHA-256 `f857b369be15f8f09833722acb4b1739dd05a794aa0636aa4135f5f703905bec`, size `6,918,766` bytes. Repository attestation records are `49008658` and `49008661` (Linux provenance/SBOM), `49008767` and `49008775` (macOS provenance/SBOM); Rekor entries are `2906497681`, `2906497694`, `2906498178` and `2906498191`. Both `W08_RELEASE_TARGET_ATTESTATION_PASS` markers passed. This is current-main hosted qualification, not approved tag publication, registry acceptance, canary, rollback or release approval. | Run the approved tag-triggered CLI release, inspect the actual published assets/manifest/checksums/SBOM, verify release-registry acceptance, then execute staged canary, rollback and owner approval. | **~0.25 engineer-day** evidence capture; **~9 min** hosted build/download/attestation wait excluding queue time; tag/canary/rollback execution remains provisional. | Approved immutable tag, release registry, deployment controller, canary environment, rollback owner and release approver are external. |
| **W08.27** Latest integrated-main workspace verification | **Source-level verification passed — 100%; provider/native and P01–P09 gates remain open** | On source `5c7cc7053523a92c8f4da13f09993910dfdb541a`, `CARGO_TARGET_DIR=/private/tmp/mount-rs-w08-current-cargo-target ./scripts/cargo-shared test --workspace --all-targets --locked` completed successfully, followed by matching strict workspace Clippy with `-D warnings`. The verification includes the latest integrated concurrent FUSE/S3/N-API changes. All non-ignored workspace tests passed; rows requiring TiDB, RustFS, PGlite, R2, FUSE, NFS or other host capabilities remained explicit skips/ignores. | Fresh current integrated tip: locked workspace test finished 0 after 15.54s with all executed tests passing; strict workspace Clippy finished 0 after 11.97s with `-D warnings` and no diagnostics. No live-provider, native-kernel or production evidence is claimed by this chunk. | No implementation action remains from this gate. Preserve the provider/native skips and close P01–P09 only with real production-like evidence, named owners and a GO decision. | **~0.15 engineer-day** final integrated-tip verification and evidence capture; **~15.54s** test plus **~11.97s** Clippy execution, excluding setup | TiDB/RustFS/PGlite/R2 credentials/services, native mount capabilities and all production rollout owners/environments remain external. |
| **W08.28** Protected production-candidate release admission | **Implementation/static qualification passed — 100%; hosted release and P09 rollout gates open** | Implementation commit `dff55858` was reconciled with concurrent changes and published in merge tip `6abc0535` (`origin/main`). `.github/workflows/w08-production-release.yml` is scoped to `v*-cli-production-candidate*` tags. It builds/tests/packages Linux x86_64 and macOS arm64, verifies downloaded target assets, writes/verifies artifact-bound manifests and CycloneDX SBOMs, creates/verifies pinned GitHub provenance/SBOM attestations, and holds prerelease publication behind the protected `w08-production` environment. It checks tag ancestry against `origin/main`, refuses an existing release, and re-downloads/re-verifies all final assets and attestations. `.github/workflows/cli-release.yml` now matches only `v*-cli-preview*`, keeping preview and candidate paths separate. | Local Ruby YAML parsing passed for both workflows; all 3 existing preview and 5 new candidate embedded Bash blocks passed `bash -n`; `git diff --check` passed before the concurrent merge. No hosted candidate tag was created or run, no environment approval was observed, and no release/canary/rollback/GO evidence is claimed. | Configure required reviewers and tag/branch policy for `w08-production`; create an approved immutable candidate tag, follow the workflow to terminal success, inspect the prerelease registry assets, then execute the separate canary, rollback and owner-approval gates. | **~0.75 engineer-day** workflow implementation and static qualification; **~10–15 min** provisional hosted candidate runtime excluding queue/approval; **~1–2 engineer-days** remaining release/canary/rollback execution excluding external waits. | GitHub environment protection configuration, approved release tag, OIDC/attestation/release permissions, registry, deployment controller, canary topology, rollback target and named owners/approvers are external. |
| **W08.29** Current published-main hosted target and attestation qualification | **Hosted target qualification passed — 100%; P09 publication/canary/approval gates open** | The existing `workflow_dispatch` `attest=true` target path ran from source `9d3a6e502eccec9ba54c00e80c98e6e1da175177`, which is in the current published-main ancestry. It built/download-verified both supported targets, generated and verified provenance plus CycloneDX SBOM attestations, and passed final identity checks. The evidence capture was committed as `ad45bf06` and published in merge tip `d68f14de3c13c0510237e9a88c615c1a8e74c9a3`. | Run `35638433010` completed with `success`: Linux build `106461545303`, macOS build `106461544977`, downloaded Linux verification `106463019865`, downloaded macOS verification `106463019693`, Linux attestation `106463113298` and macOS attestation `106463113247` all passed. Linux artifact SHA-256 `29130f9d1a753bdcf7dbd2412146585bfc4bdce55c5ddbb4946a79a653696c20` (8,347,688 bytes); macOS arm64 SHA-256 `1530afd0325416db239776c843343e6c99e37406a0cc82be27490d26060ba27c` (6,943,737 bytes). Both `W08_RELEASE_TARGET_ATTESTATION_PASS` markers passed. Public downloaded assets were locally rechecked with `SHA256SUMS`, the manifest verifier, the 288-component SBOM verifier and tar-content checks. This remains hosted qualification, not a candidate-tag release, registry acceptance, canary, rollback or approval. | Configure and approve the `w08-production` environment, run the immutable candidate-tag workflow, retain its final release assets and evidence, then execute canary, rollback and owner approval. | **~0.25 engineer-day** hosted evidence capture and independent re-verification; **~4m45s** hosted run plus download/attestation observation, excluding queue; remaining release/canary/rollback work is **~1–2 engineer-days** provisionally, excluding external waits. | Protected environment policy, candidate tag, OIDC/attestation/release permissions, release registry, deployment controller, canary topology, rollback target and named owners/approvers remain external. |

| **W08.30** Current public-tip Rust source verification | **Source-level verification passed — 100%; provider/native and P01–P09 gates remain open** | Source `76c2b1a863c23afe71c0591d0a480433e1b9078d` was verified with `CARGO_TARGET_DIR=/private/tmp/mount-rs-w08-final-cargo-target ./scripts/cargo-shared test --workspace --all-targets --locked --offline` and the matching strict workspace Clippy command with `-D warnings`. The bounded target was outside the worktree. | The locked workspace test gate completed successfully with all executed tests passing; strict Clippy completed successfully in 34.72s with `-D warnings` and no diagnostics. TiDB/RustFS/PGlite/R2 credentialed rows and FUSE/NFS/native-service rows remained explicit ignored prerequisites. No live-provider, native-host or production evidence is claimed. | No implementation action remains from this source gate. Preserve the explicit provider/native prerequisites and close P01–P09 only with production-like evidence, named owners and a GO decision. | **~0.15 engineer-day** test/Clippy execution and evidence capture; **~46s** test execution after compilation plus **~35s** Clippy execution, excluding setup | TiDB/RustFS/PGlite/R2 credentials/services, native mount capabilities and all production rollout owners/environments remain external. |
| **W08.31** Latest hosted target, artifact and attestation qualification plus production-policy negative check | **Hosted target qualification passed — 100%; P09 candidate-tag/canary/approval gates remain open** | Node syntax checks for the W08 release/config verifiers and Ruby YAML parsing for the release workflows remained clean. The positive policy-only fixture passed with `config_shape=splitstore durable_metadata=true durable_blocks=true blocks_tls=https tidb_tls=require_ssl secrets=external`; the insecure fixture failed closed with `config.driver.storage.blocks.secret_access_key-must-not-be-inline`, and a shell assertion recorded `W08_PRODUCTION_CONFIG_NEGATIVE_PASS`. The missing-TLS-env fail-closed path was also rechecked; no credential or provider connection was used. | Hosted run `35641555767`, source `2bbd0266a094b06bf97d51baafe0d3a8800bfec5`, completed successfully. Build jobs `106471833712` (Linux) and `106471833967` (macOS), downloaded verification jobs `106473358964` and `106473359006`, and attestation jobs `106475107803` and `106475108148` all passed. Linux artifact SHA-256 `6a3de0a607ffafcedf6bd3385c3849a30208df0345120061061821109a09ac79` (8,373,376 bytes); macOS arm64 SHA-256 `75a031c4e439ede07f0fa1a09db050b15c45f6d802a703d90b67cde52b4a941d` (6,958,388 bytes). Local downloads passed all three checksum entries, both manifest verifiers, both 288-component SBOM verifiers, archive-content checks and the macOS extracted runtime version check. Independent provenance and CycloneDX `gh attestation verify` calls passed with exact source/workflow/ref and `--deny-self-hosted-runners` constraints. | Configure and approve `w08-production`, create and run the approved immutable candidate tag, retain final registry assets and target-specific evidence, then execute staged canary, rollback and release-owner approval. | **~0.15 engineer-day** policy/hosted evidence capture and independent re-verification; **~9 min** hosted build/download/attestation execution plus queue/remote observation; remaining release/canary/rollback work **~1–2 engineer-days** provisionally, excluding external waits. | Protected environment policy, candidate tag, OIDC/attestation/release permissions, release registry, deployment controller, canary topology, rollback target, production credentials and named owners/approvers remain external. |
| **W08.32** Production rollout ledger consistency guard | **Implementation/static qualification passed — 100%; W08-P01–P09 remain open** | `scripts/verify-w08-rollout-ledger.mjs` checks the exact NO-GO/GO decision, all W08.1–W08.32 implementation checkboxes, all nine production-gate rows, the detailed ledger, operations-runbook drill boundary and cross-document consistency. `scripts/test-w08-rollout-ledger.mjs` exercises baseline NO-GO, premature gate/item, missing drill boundary, premature GO and simulated complete GO cases; all six cases passed locally. | Hosted run `35647096385`, source `2641962a6a65179abf4b8d785345fbe6af4be9b8`, job `106490142559`, completed with `success`; the W08 rollout-ledger verifier/test and existing release-identity/provenance policy step passed. The earlier run `35646848615` was cancelled with `jobs=[]` before execution and is explicitly not evidence. This confirms the repository tracking control only; no provider, native, hosted-release, registry or production gate is closed. | Continue P01–P09 with named production topology, secret/IAM, DR, upgrade, observability, capacity, security, drills, candidate release, canary, rollback and approval evidence. | **~0.5 engineer-day** verifier/test/workflow implementation and local qualification; **~2m37s** hosted policy job excluding queue; remaining production-gate work is **~3–8 engineer-days** provisionally, excluding external waits and approvals. | The hosted runner and repository provide only tracking-control execution; all production environments, credentials, owners and approvals remain external. |
| **W08.33** Production-candidate GO admission guard | **Implementation/static qualification passed — 100%; candidate admission intentionally unexecuted while W08-P01–P09 remain open** | `scripts/verify-w08-rollout-ledger.mjs --require-go` reuses the fail-closed cross-document checks and rejects the current NO-GO ledger before any production-candidate target can build. `.github/workflows/w08-production-release.yml` now has an `admission` job that must pass before `build`; the seven-case local suite includes a NO-GO admission-negative and simulated complete-GO admission path. | `node --check` passed for both W08 rollout scripts, the current NO-GO verifier passed with `functional_items=33 production_gates=9`, the seven-case suite passed, both affected workflows parsed as YAML, and `git diff --check` passed. No candidate tag has been created or run because the authoritative decision is correctly NO-GO. This control does not create provider, native, registry, canary, rollback or owner-approval evidence. | Retain a terminal candidate-tag admission result only after P01–P09 are genuinely closed and the authoritative decision is GO; then continue with target build, registry, canary, rollback and owner approval evidence. | **~0.35 engineer-day** admission guard/test/workflow implementation; **~0 min** hosted candidate admission by design while NO-GO; remaining P01–P09 work remains **~3–8 engineer-days** provisionally, excluding external waits and approvals. | Candidate tags, protected environment, production topology, credentials/IAM, provider operations, registry, canary, rollback, named owners and GO approval remain external. |
| **W08.34** Machine-readable production evidence admission | **Implementation/static qualification passed — 100%; hosted policy qualification passed; production evidence packet intentionally NO-GO** | `docs/W08-production-evidence.json` records all nine open gates, explicit remaining actions and no evidence records. `scripts/verify-w08-production-evidence.mjs` cross-checks the packet against the rollout decision; in GO mode it requires all nine gates closed plus full revision/provider-version/topology/environment/run/status/owner/cleanup/rollback/reference fields, terminal status and non-empty remaining-action boundaries while open. Both W08 workflows invoke the validator; the ten-case suite covers baseline NO-GO, mismatch, missing/closed gate, missing actions/evidence, invalid/nonterminal evidence, complete synthetic GO and `--require-go` rejection. | Local evidence-policy validation passed with `decision=NO-GO gates=9 closed=0 evidence_records=0`; `node scripts/test-w08-production-evidence.mjs` passed all ten cases. Hosted run `35651363875`, source `14f8c344a5a7f5b2e8cb08475db3d87ecbfc23d7`, job `106504528377`, completed successfully in 2m41s with the W08 policy and release-identity/provenance steps green. The earlier current-tip cancellation `35650626028` had `jobs=[]` and remains no evidence. No provider, native, hosted release, registry, canary, rollback or approval evidence is claimed. | Populate records only from actual production-like runs, then require terminal P01–P09 evidence and named approval before changing the packet/tracker to GO. | **~0.5 engineer-day** packet/validator/test/workflow implementation; **~2m41s** hosted policy qualification excluding queue; remaining production-gate work remains **~3–8 engineer-days** provisionally, excluding external waits and approvals. | Production topology, providers, secret manager, IAM, certificates, collector, workload/capacity environment, operators, candidate tag, registry, canary, rollback and release-owner approval remain external. |
| **W08.35** Docker server-capacity preflight hardening | **Implementation/static qualification passed — 100%; provider acceptance and P01–P09 remain open** | `scripts/test-tidb.sh` now reads architecture, CPU and memory from one formatted Docker server-info probe before creating a test network, volume or container. A daemon/CLI failure is classified as an environment prerequisite and exits 2 without partial topology setup or an acceptance marker. | `sh -n scripts/test-tidb.sh` passed. With the local Docker engine unavailable, `MOUNT_RS_TIDB_TOPOLOGY=single ./scripts/test-tidb.sh` exited 2 with `Docker daemon did not return usable server capacity`; stdout was empty and no `TIDB_ACCEPTANCE` marker was emitted. This is a harness-boundary result, not provider acceptance. | Retry the durable and TiDB/RustFS composition lanes after Docker Desktop is healthy and has the required durable capacity; count provider evidence only from terminal `TIDB_ACCEPTANCE`/composition markers. | **~0.2 engineer-day** harness correction, syntax check and fail-closed run; **~1–2 min** local validation. | Docker Desktop/server health and capacity remain external local prerequisites; TiDB/RustFS credentials/services, native capabilities and production owners/approvals remain external. |
| **W08.36** Production evidence placeholder rejection | **Implementation/static qualification passed — 100%; production packet intentionally NO-GO** | `scripts/verify-w08-production-evidence.mjs` rejects obvious placeholders (`TBD`, `pending`, `unknown`, `TODO`, template markers and angle-bracket substitutions) in every required GO evidence string; `scripts/test-w08-production-evidence.mjs` adds a synthetic GO packet with `topology=TBD` and expects fail-closed admission. The checked-in packet remains NO-GO with nine open gates and zero evidence records. | Hosted run `35656592580`, source `e0ab07d68fada16db58fc30e027f4957bc93fbd3`, job `106521925802`, completed successfully in 2m45s. The W08 policy step passed the 36-item rollout verifier, seven-case rollout suite, NO-GO evidence validator and 11-case evidence suite; the real Ubuntu CLI artifact, manifest, SBOM, checksums and runtime checks also passed. This is hosted implementation/static evidence only. | Keep P01–P09 open until actual production-like provider, operational, security, canary, rollback and named-approval evidence is entered; the validator must reject placeholders when the packet eventually transitions to GO. | **~0.2 engineer-day** validator hardening, regression coverage and hosted qualification; **~2m45s** hosted policy execution excluding queue. | Hosted runner/concurrency is external; provider credentials, production topology, owners, release approval and real evidence remain external. |

## Evidence ledger

### Local implementation and compile evidence

| CI `35620932700`, source `11a7b22`, attestation jobs `106406881524`, `106406881625` | PARTIAL — W08.17 hosted build/download pass plus W08.19 action-pin setup failure | Both target builds passed (`106403507404` macOS arm64 3m57s; `106403507916` Linux x86_64 3m40s), and both downloaded-asset verification jobs passed (`106406448310` macOS 5s; `106406448372` Linux 9s). The two attestation jobs failed at `Set up job` before any action step because GitHub rejected shortened ref `1e69f48acb82d1966a394da916b4c1698aa569d` and required full ref `1e69f48acb82d1966a394da916b4c1698aa569d6`. No OIDC/Sigstore evidence exists; the correction is queued for rerun. |
| CI `35622899242`, source `2f43721`, attestation jobs `106412019988`, `106412020019` | PARTIAL — W08.19 hosted build/download and attestation-generation pass plus W08.20 verifier-flag failure | Linux/macOS target build and download jobs passed. Both attestation jobs completed the full-SHA setup, asset download, provenance attestation and CycloneDX SBOM attestation steps. The final verifier failed with `if any flags in the group [cert-identity cert-identity-regex signer-repo signer-workflow] are set none of the others can be; [signer-repo signer-workflow] were all set`. The redundant `--signer-repo` flag was removed and the terminal corrected result is recorded below; this failed run is not counted as a verification PASS. |
| CI `35624385556`, source `2ab3cf1`, jobs `106415027916`, `106415027629`, `106416531412`, `106416531464`, `106416627127`, `106416627177` | PASS — terminal hosted target build/download/provenance/SBOM attestation qualification | Linux x86_64 and macOS arm64 build/package jobs, downloaded-asset verification jobs, provenance attestation generation, CycloneDX SBOM attestation generation and final `gh attestation verify` jobs all passed. Linux tarball SHA-256 `5f7f3c6345013144d8889b107cde41c9e5b69d688e21a975ba6fbb33ce2507d6` (8,277,051 bytes); macOS arm64 SHA-256 `add8e365c0ab1c0390267531144b77b6c7cf7f338d03ce0a549a5783c834fc8e` (6,910,097 bytes). Repository attestation records `48984689`, `48984696`, `48984678`, `48984687` and Rekor entries `2906371944`, `2906371970`, `2906371892`, `2906371927` were emitted; both `W08_RELEASE_TARGET_ATTESTATION_PASS` markers passed. This is hosted target qualification, not approved tag publication, registry acceptance, canary, rollback or release approval. |
| CI `35627761501`, source `50a33ac`, jobs `106426208811`, `106426208445`, `106427912681`, `106427912698`, `106428371158`, `106428371145` | PASS — current-main hosted target build/download/provenance/SBOM attestation retest | Both target builds and both downloaded-asset verification jobs passed. Linux and macOS provenance plus CycloneDX SBOM attestations were generated and final `gh attestation verify` passed with repository, signer-workflow, source-digest, source-ref and hosted-runner constraints. Linux tarball SHA-256 `432049a39cd648bda95a9aeaaf81d5bb2933267c467f69b617e6e3f718d5303b` (8,306,821 bytes); macOS arm64 tarball SHA-256 `647f4d0fe7cb7f8446ed6eba3a2c1ad4b4f7413ad59afa10d68617b86ec10421` (6,926,739 bytes). Repository attestation records `48993605`, `48993613`, `48993668`, `48993679` and Rekor entries `2906426581`, `2906426592`, `2906426797`, `2906426824` were emitted; both `W08_RELEASE_TARGET_ATTESTATION_PASS` markers passed. This is hosted current-main-at-dispatch evidence, not approved tag publication, registry acceptance, canary, rollback or release approval. |
| CI `35631063978`, source `cf75835`, jobs `106437098891`, `106437098611`, `106439285204`, `106439285356`, `106439636662`, `106439636979` | PASS — current published-main hosted target build/download/provenance/SBOM attestation qualification | Both target builds and both downloaded-asset verification jobs passed. Linux and macOS provenance plus CycloneDX SBOM attestations were generated and final `gh attestation verify` passed with repository, signer-workflow, source-digest, source-ref and hosted-runner constraints. Linux tarball SHA-256 `54220877022f67640aa7b470a8eaa09b24df1d867e00463533642b6fa462182a` (8,315,540 bytes); macOS arm64 tarball SHA-256 `6df488c71a0eb65674af9adf7660b870424b4e47227d832028180947dbb83b39` (6,918,800 bytes). Repository attestation records `49001528`, `49001543`, `49001531`, `49001545` and Rekor entries `2906464391`, `2906464436`, `2906464411`, `2906464449` were emitted; both `W08_RELEASE_TARGET_ATTESTATION_PASS` markers passed. This is hosted published-main evidence, not approved tag publication, registry acceptance, canary, rollback or release approval. |
| Local source `5cb96ad`, `./scripts/cargo-shared test --workspace --all-targets --locked` plus `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` | PASS — fresh current-main workspace verification | The locked workspace test suite completed with all non-ignored tests passing across the current workspace; provider/native rows that require TiDB, RustFS, PGlite, R2, FUSE, NFS or other host capabilities remained explicitly ignored. Strict workspace Clippy completed with `-D warnings` and no diagnostics. This is fresh source-level evidence for source `5cb96ad`, not live provider, native-host or production-rollout evidence. |
| Local source `5aae52b`, `./scripts/cargo-shared test --workspace --all-targets --locked` plus `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` | PASS — W08.23 current shared-main workspace verification | The locked workspace test suite completed with all non-ignored tests passing after current shared-main changes; provider/native rows that require TiDB, RustFS, PGlite, R2, FUSE, NFS or other host capabilities remained explicitly ignored. Strict workspace Clippy completed with `-D warnings` and no diagnostics. This is source-level evidence for `5aae52b`, not live provider, native-host or production-rollout evidence. |
| Local source `f16eec2a6a1f79a5543ba345c67b86eaae837312`, `CARGO_TARGET_DIR=/private/tmp/mount-rs-w08-current-cargo-target ./scripts/cargo-shared test --workspace --all-targets --locked` plus matching strict Clippy | PASS — W08.25 current shared-main verification after FUSE session fix | The fresh bounded target completed the full locked workspace test suite with all non-ignored tests passing and strict workspace Clippy with `-D warnings` and no diagnostics. The configured shared target had two pre-test missing-artifact failures, so it was not used as evidence. Provider/native rows remained explicitly ignored and no live-provider, native-host or production-rollout evidence is claimed. |
| CI `35633841116`, source `56aae0c`, jobs `106446286719`, `106446286427`, `106447678688`, `106447678718`, `106448672423`, `106448672409` | PASS — W08.26 current-main hosted target build/download/provenance/SBOM attestation qualification | Linux x86_64 and macOS arm64 target builds, downloaded-asset verification jobs, provenance attestation generation, CycloneDX SBOM attestation generation and final `gh attestation verify` all passed. Linux tarball SHA-256 `5ef9ddb46b5a7967fb26b6379a217f648291d2598be75e9c7d8847f7aa42371c` (8,328,164 bytes); macOS arm64 tarball SHA-256 `f857b369be15f8f09833722acb4b1739dd05a794aa0636aa4135f5f703905bec` (6,918,766 bytes). Repository attestation records `49008658`, `49008661`, `49008767`, `49008775` and Rekor entries `2906497681`, `2906497694`, `2906498178`, `2906498191` were emitted; both `W08_RELEASE_TARGET_ATTESTATION_PASS` markers passed. This is hosted current-main-at-dispatch evidence, not approved tag publication, registry acceptance, canary, rollback or release approval. |
| Local source `5c7cc7053523a92c8f4da13f09993910dfdb541a`, `CARGO_TARGET_DIR=/private/tmp/mount-rs-w08-current-cargo-target ./scripts/cargo-shared test --workspace --all-targets --locked` plus matching strict Clippy | PASS — W08.27 latest integrated-main workspace verification | The bounded target completed the full locked workspace test suite with all non-ignored tests passing and strict workspace Clippy with `-D warnings` and no diagnostics after the latest integrated FUSE/S3/N-API changes. Provider/native rows remained explicitly ignored and no live-provider, native-host or production-rollout evidence is claimed. |
| Local W08.28 workflow validation before publication, implementation `dff55858`, public merge tip `6abc0535` | PASS — W08.28 implementation/static qualification | Ruby YAML parsing passed for `.github/workflows/cli-release.yml` and `.github/workflows/w08-production-release.yml`; all embedded `run:` blocks passed `bash -n` (3 preview, 5 candidate); `git diff --check` passed. The candidate workflow is not hosted evidence: no production-candidate tag or protected-environment approval was created. The public merge tip retains the validated workflow and docs. |
| CI `35638005702`, source `ad0dd81`, `w08-release-policy` job `106460327622` | PASS — W08.28 publication-side hosted policy check | The dedicated non-cancelling W08 release-policy job completed successfully in 2m43s on the published W08.28 source. Its real Ubuntu artifact, manifest, checksum, unsigned 288-component SBOM, direct/extracted version checks and policy fixtures passed. GitHub emitted only Node 20/runner-image migration annotations; no release or production gate was claimed. |
| CI `35638005694`, source `ad0dd81`, jobs `[]` | CANCELLED before jobs — push target-matrix concurrency boundary | The ordinary push-triggered target workflow was cancelled before a job materialized while concurrent `main` activity occupied the workflow queue. No target build, artifact, attestation or provider operation ran; this is not a PASS, source failure or production evidence. The explicit manual qualification below was dispatched separately and reached terminal success. |
| CI `35638433010`, source `9d3a6e502eccec9ba54c00e80c98e6e1da175177`, jobs `106461545303`, `106461544977`, `106463019865`, `106463019693`, `106463113298`, `106463113247` | PASS — W08.29 current published-main hosted target build/download/provenance/SBOM-attestation qualification | Linux x86_64 and macOS arm64 build/package jobs, downloaded-asset verification, provenance attestation generation, CycloneDX SBOM attestation generation and strict final `gh attestation verify` all passed. Linux tarball SHA-256 `29130f9d1a753bdcf7dbd2412146585bfc4bdce55c5ddbb4946a79a653696c20` (8,347,688 bytes); macOS arm64 SHA-256 `1530afd0325416db239776c843343e6c99e37406a0cc82be27490d26060ba27c` (6,943,737 bytes). Both `W08_RELEASE_TARGET_ATTESTATION_PASS` markers passed; independent local checks returned three checksum `OK` lines, `W08_RELEASE_MANIFEST_POLICY_PASS`, `W08_RELEASE_SBOM_PASS` with `components=288`, and tar-content PASS for each target. `gh attestation verify --format json` independently verified SLSA provenance and CycloneDX predicates for both artifacts with Rekor transparency timestamps. No candidate-tag release, registry, canary, rollback or approval evidence is claimed. |

| Local source `76c2b1a863c23afe71c0591d0a480433e1b9078d`, bounded target `/private/tmp/mount-rs-w08-final-cargo-target` | PASS — W08.30 current public-tip locked workspace test and strict Clippy verification | `./scripts/cargo-shared test --workspace --all-targets --locked --offline` completed successfully with all executed tests passing. The matching `./scripts/cargo-shared clippy --workspace --all-targets --locked --offline -- -D warnings` completed successfully in 34.72s with no diagnostics. Provider/native rows requiring TiDB, RustFS, PGlite, R2, FUSE, NFS or other host capabilities remained explicitly ignored; no live-provider or production claim is added. |
| CI `35641555767`, source `2bbd0266a094b06bf97d51baafe0d3a8800bfec5`, jobs `106471833712`, `106471833967`, `106473358964`, `106473359006`, `106475107803`, `106475108148` | PASS — W08.31 latest current-main-at-dispatch hosted target build/download/provenance/SBOM-attestation qualification | Linux x86_64 and macOS arm64 build/package, downloaded-asset verification, provenance attestation, CycloneDX SBOM attestation and final identity-verifier jobs all passed. Linux tarball SHA-256 `6a3de0a607ffafcedf6bd3385c3849a30208df0345120061061821109a09ac79` (8,373,376 bytes); macOS arm64 tarball SHA-256 `75a031c4e439ede07f0fa1a09db050b15c45f6d802a703d90b67cde52b4a941d` (6,958,388 bytes). Both target-build/download/attestation PASS markers passed; local downloads returned all checksum entries `OK`, manifest and 288-component SBOM verification passed for both targets, archive contents were correct, and the extracted macOS binary reported `mount-rs 0.1.0`. Independent SLSA and CycloneDX attestation verification passed with exact repository, signer-workflow, source-digest, `refs/heads/main` and `--deny-self-hosted-runners` constraints. No candidate-tag release, registry, canary, rollback or approval evidence is claimed. |
| Local W08.32 rollout-ledger verifier/test, workflow `w08-release-policy.yml` | PASS — repository production-tracking consistency control | The verifier passed the current NO-GO state with `functional_items=32 production_gates=9`; the six-case test passed baseline NO-GO, premature production-gate/item, missing drill boundary, premature GO and simulated complete GO transitions. The workflow now executes both controls on hosted Ubuntu without credentials or provider access. This remains tracking/static evidence only. |
| CI `35647096385`, source `2641962a6a65179abf4b8d785345fbe6af4be9b8`, job `106490142559` | PASS — hosted W08.32 rollout-ledger consistency qualification | The terminal hosted policy job completed successfully in 2m37s. The current NO-GO state, all 32 implementation items and nine production-gate rows were accepted by `scripts/verify-w08-rollout-ledger.mjs`; the six-case transition test also passed, and the existing release-identity/provenance policy step remained green. Run `35646848615` was cancelled before job creation (`jobs=[]`) and contributes no evidence. This is credential-free tracking/static evidence only; it does not close P01–P09 or prove a production release. |
| Local W08.33 admission guard and seven-case suite | PASS — implementation/static qualification | `node --check` passed for both rollout scripts; the current NO-GO verifier returned `functional_items=33 production_gates=9`; `node scripts/test-w08-rollout-ledger.mjs` passed all seven cases, including admission rejection and simulated complete-GO admission; Ruby YAML parsing passed for `w08-production-release.yml` and `w08-release-policy.yml`; `git diff --check` passed. No candidate tag was created and no production result is inferred. |
| CI `35648755697`, source `86a87ed04722a2f638400c59f622a828f52ee136`; manual dispatch `35648898876`, resolved source `987c593bc08adfb161a55a7a9eee27ff82606310` | NOT EVIDENCE — hosted W08.33 qualification attempts cancelled before job creation | The push-triggered and subsequent credential-free manual policy runs both ended `cancelled` with `jobs=[]`; neither executed the 33-item verifier or release-policy job. These are retained as explicit hosted-state boundaries, not passes. No candidate tag, production environment, provider, registry, canary or approval action was attempted. |
| Local W08.34 evidence packet/validator and ten-case suite | PASS — implementation/static qualification | `node scripts/verify-w08-production-evidence.mjs` returned `decision=NO-GO gates=9 closed=0 evidence_records=0`; `node scripts/test-w08-production-evidence.mjs` passed ten cases covering packet/decision/gate/action/evidence failures, nonterminal evidence, synthetic complete GO and `--require-go` rejection. Both W08 workflows invoke the validator, and the existing rollout-ledger checks remain green. This is evidence-shape control only; no underlying production result is asserted. |
| Local W08.35 TiDB harness preflight | PASS — implementation/static qualification; environment prerequisite remains blocked | `sh -n scripts/test-tidb.sh` passed. The current unavailable Docker daemon returned status 2 from the single formatted server-info probe with the explicit `Docker daemon did not return usable server capacity` message; no resources were created, stdout was empty and no `TIDB_ACCEPTANCE` marker was emitted. No provider or production result is inferred. |
| CI `35655178863`, source `22f82dcac0e2c96ba2471f4c534b08c24776cef8`, jobs `[]` | NOT EVIDENCE — post-push W08.35 policy run cancelled before job creation | GitHub main-branch concurrency cancelled the automatic push run before the W08 policy job started. It produced no rollout/evidence-policy result and is retained as a hosted scheduling boundary, not a pass or source failure. |
| CI `35655299194`, source `4efec58c93a0cd6e6027676f069edf0c4cde3132`, job `106517465871` | PASS — hosted W08.35 rollout/evidence policy and release-artifact qualification | The manually dispatched credential-free run completed with terminal `success` in 2m15s. Its W08 policy step passed the 35-item rollout-ledger verifier/test and ten-case production-evidence validator/test with `decision=NO-GO`, all nine gates open and zero evidence records; the real Ubuntu CLI artifact, manifest, SBOM, checksums, direct/extracted version checks and release-identity policy also passed. The source includes the W08.35 implementation and is an ancestor of the current public mainline. This remains hosted implementation/static evidence only; no provider, production-candidate tag, registry, canary, rollback or approval evidence is claimed. |
| CI `35656592580`, source `e0ab07d68fada16db58fc30e027f4957bc93fbd3`, job `106521925802` | PASS — hosted W08.36 placeholder-rejection and release-artifact qualification | The post-push run completed with terminal `success` in 2m45s. The W08 policy step passed the 36-item rollout-ledger verifier, seven-case transition suite, NO-GO packet validator and 11-case production-evidence suite, including the synthetic placeholder rejection; the real Ubuntu CLI artifact, manifest, SBOM, checksums, direct/extracted version checks and release-identity policy also passed. No provider, candidate release, registry, canary, rollback or approval evidence is claimed. |
| Live GitHub production-boundary audit, 2026-09-22 07:35 AEST | BLOCKED — P09 hosted environment/tag/execution boundary | Read-only checks found HTTP 404 for the `w08-production` environment and its environment-secret surface; `gh release list` showed only the `v0.1.0-cli-preview` prerelease; `git ls-remote` found no `v*-cli-production-candidate*` tag; and `gh run list --workflow w08-production-release.yml` returned no runs. The protected workflow file exists on remote mainline at blob `9cacf0f19c8fc475df508684cf0ffcc591c96625`. This is external release-configuration evidence, not production acceptance. | Configure required reviewers/branch/tag policy, create an approved immutable candidate tag, run the terminal candidate workflow, inspect registry assets, then execute canary/rollback and record approval. |
| Live GitHub production-boundary audit, 2026-09-22 08:38 AEST | BLOCKED — P09 repository/API/environment execution boundary | Read-only `gh` checks still return HTTP 404 for the repository, `w08-production` environment and environment-secret surface; `gh release list` still shows only `v0.1.0-cli-preview`; `gh run list --workflow w08-production-release.yml` is empty; and `git ls-remote` finds no `v*-cli-production-candidate*` tag. Git transport still sees `.github/workflows/w08-production-release.yml` on `origin/main`. This is external GitHub/API and release-configuration evidence, not production acceptance. | Restore/authorize the repository API surface, configure the protected environment and reviewers/secrets, create an approved immutable candidate tag, run the terminal workflow, then inspect registry assets and execute canary/rollback/approval. |
| Local current public mainline `6a19ff6555635b20385acdc30d7aa2da143f6931` | PASS — current-source workspace verification | `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0: every runnable workspace test passed; provider/native tests requiring TiDB, RustFS, PGlite, R2, FUSE or NFS remained explicit opt-in skips. `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` also exited 0. This proves current integrated source health, not provider, production, canary, rollback or approval acceptance. | Re-run the provider/native and production-like gates when their external services, credentials, host capabilities and owners are available. |
| Local final public mainline `49778fde94440749f7bcb4ccb571ed72104ff7c6` | PASS — final-tip workspace verification | A fresh `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0 with all runnable tests passing and provider/native opt-in skips explicit; a fresh strict `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` also exited 0. This is final source-health evidence only; it does not close provider, production, canary, rollback or approval gates. | Re-run the external provider/native and production-like gates when their services, credentials, host capabilities and owners are available. |
| Local latest public mainline `6e27d46c` | PASS — latest-tip workspace verification | After concurrent mainline added the NFS process-restart test surface, a fresh `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0 and strict `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` exited 0. Provider/native opt-in skips remain explicit; this is source-health evidence only and does not close provider, production, canary, rollback or approval gates. | Re-run the external provider/native and production-like gates when their services, credentials, host capabilities and owners are available. |
| Local latest public mainline `2d459e15` | PASS — latest-tip workspace verification | After concurrent 9P/N-API, chunked and Ozone updates, a fresh `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0 and strict `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` exited 0. Provider/native opt-in skips remain explicit; this is source-health evidence only and does not close provider, production, canary, rollback or approval gates. | Re-run the external provider/native and production-like gates when their services, credentials, host capabilities and owners are available. |
| Local latest public mainline `b27dd2bb` | PASS — latest-tip workspace verification | After concurrent FUSE teardown and session-lifecycle fixes, a fresh `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0 and strict `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` exited 0. Provider/native opt-in skips remain explicit; this is source-health evidence only and does not close provider, production, canary, rollback or approval gates. | Re-run the external provider/native and production-like gates when their services, credentials, host capabilities and owners are available. |
| Local final public mainline `0ab9bf41` | PASS — definitive latest-tip workspace verification | A fresh `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0 after the concurrent NFS/WebDAV/provider updates; strict `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` also exited 0. Provider/native opt-in skips remain explicit; this is final source-health evidence only and does not close provider, production, canary, rollback or approval gates. | Re-run the external provider/native and production-like gates when their services, credentials, host capabilities and owners are available. |
| Local current public mainline `be721a20` | PASS — current-tip workspace verification | A fresh `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0 after the concurrent Ozone/test updates; strict `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` also exited 0. Provider/native opt-in skips remain explicit; this is source-health evidence only and does not close provider, production, canary, rollback or approval gates. | Re-run the external provider/native and production-like gates when their services, credentials, host capabilities and owners are available. |
| Local latest public mainline `b7d77432` | PASS — latest-tip workspace verification | A fresh `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0 after the concurrent NFS/9P/WebDAV/provider updates; strict `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` also exited 0. Provider/native opt-in skips remain explicit; this is source-health evidence only and does not close provider, production, canary, rollback or approval gates. | Re-run the external provider/native and production-like gates when their services, credentials, host capabilities and owners are available. |
| Public merge tip `2d840df9` after source verification | NOT PROMOTED — newer concurrent 9P/WebDAV source boundary | The W08 ledger/evidence policy suites still passed after reconciliation, but new 9P mount-session and WebDAV changes landed after the full workspace test/Clippy run for `0ab9bf41`; no full source-health claim is made for `2d840df9`. | Run the full locked workspace test and strict Clippy on this newer tip before calling it current-source verified. |
| Local `./scripts/test-tidb.sh` durable and single-node attempts | BLOCKED — Docker infrastructure boundary, not provider/test acceptance | The durable attempt failed closed before launch because Docker reported `memory_bytes=8232747008` below the required `10737418240`. The single-node attempt entered the real pinned `v8.5.7` PD startup lane on `linux/arm64`, then exited `125` with `Bad response from Docker engine`; no `TIDB_ACCEPTANCE` marker or provider PASS was emitted. No production gate is closed and no local result is promoted to hosted/provider evidence. |
| CI `35650533691`, source `95437f67d01f9cb73e1759788f35828d285dda98`, job `106501645487`; current-tip run `35650626028`, source `8f3a19a891b8d432ff551c04789921575bb12f4f` | NOT EVIDENCE — first run predates W08.34; current-tip run cancelled before job creation | Run `35650533691` completed successfully, but its source predates the W08.34 commit and therefore did not execute the production-evidence validator. The current-tip run `35650626028` was cancelled with `jobs=[]`; neither result qualifies W08.34 hosted packet evidence. |
| CI `35651363875`, source `14f8c344a5a7f5b2e8cb08475db3d87ecbfc23d7`, job `106504528377` | PASS — hosted W08.34 production-evidence packet and release-policy qualification | The current-tip hosted job completed successfully in 2m41s. Its W08 policy step passed the 34-item rollout-ledger verifier/test and the ten-case production-evidence validator/test against the explicit NO-GO packet; the existing artifact manifest/SBOM/release-identity policy also passed. This is hosted implementation/static evidence only: it does not close P01–P09 or establish a production release, registry acceptance, canary, rollback or approval. |

The W08.29 and W08.31 evidence rows above are hosted qualification evidence;
the W08.29 evidence row was published in merge tip
`d68f14de3c13c0510237e9a88c615c1a8e74c9a3`; the row's hosted run and local
checks remain qualification evidence only, not a candidate-tag release,
registry acceptance, canary, rollback or approval.

These checks establish implementation/build state only. They do not substitute
for a real TiDB, RustFS, PD/TiKV restart, or native kernel mount:

| Check | Result | Boundary |
| --- | --- | --- |
| `./scripts/cargo-shared fmt --all -- --check` | PASS | Formatting; the normal shared target was overridden to an allowed `/private/tmp` target because of the local sandbox. |
| `CARGO_TARGET_DIR=/private/tmp/mount-rs-w08-cargo-target ./scripts/cargo-shared check --locked -p mount-rs-napi` | PASS | N-API compilation. |
| `./scripts/cargo-shared check --locked -p mount-rs-tidb --features rustls` | PASS | TLS-capable provider graph compilation only; no endpoint handshake. |
| `./scripts/cargo-shared check --locked -p mount-rs-sdk --features rustls`, `-p mount-rs-cli --features rustls`, `-p mount-rs-napi --features rustls` | PASS | Public TLS feature propagation; no credentials, CA policy or live provider evidence. |
| `sh -n scripts/test-tidb-tls.sh` plus credential-free positive/negative URL-policy checks | PASS | Guardrail logic only; `TIDB_TLS_CONFIG_ONLY_PASS` does not connect or prove a TLS handshake. |
| `MOUNT_RS_TIDB_TLS_URL=... node scripts/verify-w08-production-config.mjs tests/tidb/production-config-policy.json` plus the `.invalid.json` fixture | PASS / expected rejection | The positive policy fixture passed, the insecure fixture failed closed on an inline secret, and no provider or object-store connection was attempted. This is P01/P02/P07 implementation evidence only. |
| `CARGO_TARGET_DIR=/private/tmp/mount-rs-w08-production-policy-target ./scripts/cargo-shared run --quiet --offline --locked -p mount-rs-cli -- validate-config --config tests/tidb/production-config-policy.json` | PASS — `valid config` | The public Rust CLI schema accepts the production-policy shape; it does not validate endpoint identity, credentials, IAM, TLS trust, topology or deployment readiness. |
| `node tests/provider_matrix/tidb-rustfs-soak.mjs` with operations unset, plus `MOUNT_RS_TIDB_RUSTFS_SOAK_OPERATIONS=1` without a TiDB URL | PASS — explicit skip and expected missing-gate rejection | The soak runner is syntax-checked, skips without an opt-in workload, and fails closed when the TiDB endpoint is absent; no native addon or provider is contacted in either local check. |
| `./scripts/cargo-shared test --locked -p mount-rs-tidb --features rustls` | PASS, 6 provider unit tests | TLS-enabled provider unit/error-redaction coverage; ignored service tests remained explicitly ignored because no endpoint was available. |
| `./scripts/cargo-shared clippy --locked -p mount-rs-tidb --all-targets --features rustls -- -D warnings` | PASS | Strict-Clippy TLS feature build; not live provider or production evidence. |
| `./scripts/cargo-shared test --locked -p mount-rs-http` and `./scripts/cargo-shared clippy --locked -p mount-rs-http --all-targets -- -D warnings` | PASS — 8 unit tests, 10 integration tests and strict Clippy; includes `/healthz`/`/readyz` bounded behavior | HTTP implementation and contract evidence only; the probes do not test TiDB/RustFS/provider health or production collector/paging/SLO behavior. |
| `./scripts/cargo-shared test --locked -p mount-rs-http --all-features` and `./scripts/cargo-shared clippy --locked -p mount-rs-http --all-targets --all-features -- -D warnings` | PASS — 8 unit tests, 13 integration tests, doctests and strict Clippy; includes application telemetry and OTLP propagation | Local observability/exporter-path evidence only; no deployed collector, dashboard, alert route, provider health, SLO or paging evidence. |
| `node --check scripts/verify-w08-release-manifest.mjs` plus pending/strict-accepted/expected-negative fixture runs | PASS — pending policy accepted, production-acceptance mode accepted the fully verified fixture, and the placeholder-source fixture was rejected | Credential-free release identity/provenance policy evidence only; fixtures are synthetic and do not represent a real artifact, signature, SBOM, canary, rollback or approval. |
| `CARGO_TARGET_DIR=/private/tmp/mount-rs-w08-release-target ./scripts/cargo-shared build --locked --release -p mount-rs-cli`, followed by the CLI tarball/`SHA256SUMS`/manifest verification path | PASS — `mount-rs 0.1.0`; tarball SHA-256 `da0782596d7402ce64869f2037075d95b273f59f389b9493e26a0ba1e39822c7`, size `6910193`, checksum and tar-content checks passed | Local release-artifact and manifest wiring only; no published tag, registry, signing/SBOM, canary, rollback or release approval evidence. |
| CI `35599817215`, source `b26819e`, `http-observability` job `106333141914` | PASS — hosted all-features HTTP health/readiness, telemetry/OTLP tests and strict Clippy | Hosted repository contract evidence only; the run did not exercise a production collector, provider-aware readiness, SLO threshold, pager or end-to-end alert route. |
| `./scripts/cargo-shared test --locked -p mount-rs-http --all-features` and strict Clippy on reconciled source `2159976` | PASS — 8 unit tests, 13 integration tests, doctests and strict Clippy; includes cache-control/nosniff health-header assertions | Local hardening evidence; terminal hosted hardening evidence is recorded separately in run `35601990956`. |
| CI `35601990956`, source `f09fbe9`, `http-observability` job `106340034907` | PASS — terminal hosted all-features health-header, telemetry/OTLP and strict-Clippy evidence; `2f7919a` is an ancestor of the tested source | Hosted HTTP contract evidence only; no deployed collector, provider-aware readiness, SLO threshold, pager or end-to-end alert route. |
| `docker info --format 'server={{.ServerVersion}} mem_bytes={{.MemTotal}}'` | BLOCKED — local daemon unavailable | The Docker CLI is installed, but the current local command exits non-zero before reporting a server or memory value; no local TiDB/RustFS provider run is claimed. Hosted terminal jobs remain the service evidence. |
| N-API library tests | PASS, 15 tests | Binding/lifecycle unit coverage. |
| TiDB library tests | PASS, 6 tests | Provider unit coverage. |
| Ignored TiDB acceptance binaries `tidb`, `ambiguous_commit`, `chunked_rustfs` | PASS compile-only | Does not claim that live services ran. |
| Shell and Node syntax checks | PASS | Script syntax only. |
| `git diff --check` | PASS | Patch hygiene. |
| Local `node examples/node-cli/index.mjs ...` consumer run | BLOCKED | This checkout currently has no built N-API binding (`Cannot find native binding`); hosted Node jobs are the functional evidence. |

### Hosted and native evidence

| Run/job | Result counted here | Evidence boundary |
| --- | --- | --- |
| CI `35571758453`, `tidb-rustfs` job `106244679000` | Diagnostic partial pass | Real TiDB/RustFS topology, provider contract, seed, Node SDK matrix and CLI matrix passed; native FUSE I/O passed; fresh post-unmount TiDB writer acquisition failed with `EAGAIN`. Retained as the pre-`2221144` failure baseline only. |
| CI `35574581481`, `tidb-rustfs` job `106253499553` | Diagnostic partial pass | Corrected Linux FUSE I/O, clean unmount and fresh provider readback passed; the same job later failed at the then-unfixed durable TiDB restart gate. Retained as an intermediate regression checkpoint. |
| CI `35576142240`, `tidb` job `106258416149` | Diagnostic failure | Direct provider rows and ambiguous commit passed; TiDB frontend restart readiness timed out after 300 seconds. This motivated the supported TOML setting, fresh frontend replacement and later ordering fix. |
| CI `35585066458`, source `9c098e5`, `tidb` job `106286459436` | PASS — W08.2/W08.3 | Terminal success across durable 3PD/3TiKV startup, TiDB frontend replacement, TiKV restart, PD restart, provider-clock/fencing/concurrency, ambiguous-commit no-replay and post-restart persisted provider checks. |
| CI `35585066458`, source `9c098e5`, `tidb-rustfs` job `106286459715` | PASS — W08.4b/W08.5 | Terminal success for real RustFS, TiDB/RustFS seed and reopen, Node SDK/CLI matrices, Linux FUSE mounted Rust/Node I/O, clean unmount, fresh provider readback, retained bytes and RustFS restart/fault recovery. |
| CI `35585066458`, source `9c098e5`, `native-fuse` job `106286459998` | PASS — native Linux | Terminal success for the standalone privileged FUSE/native suite. |
| CI `35585066458`, source `9c098e5`, `node (ubuntu-24.04-arm)` job `106286459639` | PASS — ARM consumer | Terminal success for the ARM Node distribution/consumer gate. |
| CI `35585066458`, source `9c098e5`, `native-nfs (ubuntu-latest)` job `106286459483` | PASS — native Ubuntu | Terminal success for native NFS lifecycle and SQLite split-store coverage. |
| CI `35585066458`, source `9c098e5`, `native-nfs (macos-latest)` job `106286459246` | PASS — native macOS | Terminal success for macOS ARM native NFS lifecycle and SQLite split-store coverage; this does not claim live TiDB/RustFS on macOS. |
| CI `35585066458` aggregate | Cancelled after W08 jobs completed | `main` concurrency superseded the workflow; unrelated FoundationDB, Windows Node and macOS/Windows Rust jobs were also reported separately. The W08 job conclusions above are terminal successes and are the evidence counted here. |
| CI `35588858142`, source `e515036`, `tidb-tls-compile` job `106298487587` | PASS — P01/P07 implementation capability | Hosted compile gate passed for `mount-rs-tidb`, Rust SDK, CLI and N-API with `rustls`. This proves feature propagation, not certificates, secret injection, a live TLS handshake, provider IAM, or production deployment. |
| CI `35589825356`, source `ea062c0`, `tidb-tls-compile` job `106301529923` | PASS — P07 implementation guard | Hosted compile and credential-free positive/negative URL-policy checks passed. The guard requires `require_ssl=true` and rejects disabled CA, hostname, or built-in-root verification; no live endpoint or production identity was used. |
| CI `35590503133`, source `c54c1a4`, `tidb-tls-compile` job `106303655177` | PASS — P07 implementation guard | A later current-main run again passed the TLS-enabled provider/SDK/CLI/N-API compile and credential-free positive/negative URL-policy checks. This remains implementation/guard evidence, not a live provider or production pass. |
| CI `35590919645`, source `b68c1f2`, `tidb-tls-compile` job `106304951579` | PASS — P07 implementation guard | The production-rollout-contract tip passed the TLS-enabled provider/SDK/CLI/N-API compile and credential-free positive/negative URL-policy checks. This remains implementation/guard evidence, not a live provider or production pass. |
| CI `35592902494`, source `4326c54`, `tidb-tls-compile` job `106311076905` | PASS — W08.6/P01/P02/P07 implementation policy | Terminal success for TLS-enabled provider/SDK/CLI/N-API compilation, positive/negative TLS URL policy, and positive/negative production-config policy without credentials or network. This does not prove a live provider, IAM, certificate trust, topology, capacity or production deployment. |
| CI `35595664981`, source `c5532e3`, `tidb-rustfs` job `106319766691` | PASS — W08.8 bounded hosted qualification | Terminal success for the real Linux TiDB/RustFS composition. Seed and reopen each ran 64 operations at concurrency 8 with 65,536-byte payloads and `errors=0`; seed p50/p95/p99/throughput were 668.59/918.49/918.62 ms and 11.47 ops/s, while reopen values were 648.69/815.50/816.83 ms and 12.15 ops/s. Durable TiDB and RustFS restart markers also passed. This does not close production workload, capacity, headroom, failover, multi-hour soak, cost or SLO gates. |
| CI `35595664981` aggregate | Failure — not an aggregate W08 result | W08-relevant `tidb` `106319766731`, `tidb-rustfs` `106319766691`, `tidb-tls-compile` `106319766544` and `native-fuse` `106319766612` jobs were terminal successes. The aggregate was red because unrelated FoundationDB, Windows Rust, Ozone and Node matrix jobs failed; those failures remain visible to their workstreams and are not converted into W08 provider failures. |
| CI `35606873984`, source `fecec0e`, `w08-release-policy` job `106356402785` | PASS — W08.11/P09 implementation policy | Terminal hosted success for the pending manifest, strict release-acceptance fixture and expected-negative placeholder fixture. The policy job is not production release evidence; the aggregate run was still queued/in progress when captured and is not counted as an aggregate-green result. |
| CI `35609172786`, source `66544b5`, `w08-release-policy` job `106363893748` | PASS — W08.11/W08.12/P09 implementation policy | Terminal hosted success for generator output from `README.md`, pending manifest verification, strict release-acceptance fixture and expected-negative placeholder fixture. This is hosted policy evidence; the tag-triggered macOS release workflow, published asset inspection, signing/SBOM, canary, rollback and approval remain open. |
| CI `35611883547`, source `f432441`, `w08-release-policy` job `106372777281` | PASS — W08.13/P09 hosted real-artifact policy | Terminal success from the dedicated non-cancelling workflow. Ubuntu built the real locked `mount-rs-cli` release, packaged and checksummed the tarball, generated and verified artifact-derived provenance, checked tar contents, ran the direct and extracted binary version smoke tests, and exercised all three policy fixtures. The job is hosted artifact-path evidence; no tag-triggered publication, signing/SBOM, target-platform acceptance, canary, rollback or approval is present. |
| CI `35614345209`, source `9c9d0e4`, `w08-release-policy` job `106381893114` | PASS — W08.14/P07/P09 hosted unsigned-SBOM artifact path | Terminal success in 2m40s from the dedicated non-cancelling workflow. The real Ubuntu `mount-rs-cli 0.1.0` artifact path emitted `W08_RELEASE_SBOM_GENERATED` and `W08_RELEASE_SBOM_PASS` for 288 reachable Cargo components and tarball SHA-256 `c3d1a3200c06830f95527dd62310455e66b4632080aee7ead3a118e4783f4543`; checksum, manifest, direct/extracted runtime and policy fixture paths also passed. The manifest deliberately retained `signature=pending sbom=pending canary=not-run`; no published tag, cryptographic attestation, target-platform parity, canary, rollback or approval is claimed. |
| CI `35615714935`, source `5116ded`, `w08-release-policy` job `106386240669` | PASS — W08.15/P09 all-release-asset integrity | Terminal success in 2m35s from the dedicated non-cancelling workflow. After generating the artifact-derived manifest and 288-component SBOM, the real Ubuntu job generated `SHA256SUMS` for the tarball, `release-manifest.json` and `release-sbom.json`; all three verification lines returned `OK` before the runtime and fixture checks. The tag-triggered publication, downloaded-release inspection, signing/attestation, target-platform parity, canary, rollback and approval remain open. |
| CI `35617415427`, source `b0ca8a9`, target workflow jobs `106391572292`, `106391572540`, `106393402528`, `106393402680` | PASS — W08.16/P09 hosted target-package matrix | Terminal success: Linux x86_64 build/test/package job `106391572292` (3m41s), macOS arm64 build/test/package job `106391572540` (4m27s), downloaded macOS verification `106393402528` (5s) and downloaded Linux verification `106393402680` (10s). Both builds emitted target PASS markers, three-entry checksums and 288-component SBOM passes; both download jobs verified checksums, manifest, SBOM and target identity. Target artifacts were `x86_64-unknown-linux-gnu` SHA-256 `875a06fb975d8a294141f5b70841c481768655ac3c5c56a721604216ee12ff2c` and `aarch64-apple-darwin` SHA-256 `50f466bbe22b2fa09ecd8f1246fb278bd2b190fed52f940de21bb8d414baa9bd`. This is hosted target-package evidence, not tag publication, signing/attestation, canary, rollback or approval. |
| Local W08.17 workflow validation at source `86f272c` | PASS — W08.17 implementation/static qualification | `.github/workflows/cli-release.yml` and `.github/workflows/w08-release-targets.yml` parsed as YAML; all embedded `run:` blocks passed `bash -n` after replacing GitHub expression placeholders; `gh attestation verify --help` exposed the required signer-workflow, source-digest, source-ref, predicate and self-hosted-runner policy flags. This validates workflow shape only; no OIDC token, attestation bundle or hosted signature was created. |
| CI dispatch `35620392878`, source `0a4de6f` | CANCELLED before jobs — W08.18 concurrency boundary | The explicit `workflow_dispatch` `attest=true` request was accepted by GitHub but completed with `conclusion=cancelled` and `jobs=[]` while concurrent `main` pushes occupied the old shared workflow pending group. No OIDC/Sigstore action ran, so this is not a hosted PASS or provider failure. The concurrency group has since been split by event type and ref; rerun is required. |

The W08 rows above use exact terminal job IDs and markers. The historical
functional run `35585066458` was later superseded and cancelled at aggregate
level, while the current-main run `35595664981` completed with an aggregate
`failure` caused by unrelated matrix jobs. Neither aggregate state is reported
as an aggregate-green release result; the terminal W08 job conclusions remain
the evidence counted here.

## Remaining action plan

1. No required W08 functional implementation or hosted acceptance action
   remains for the defined scope. W08.6 and W08.11–W08.31 add credential-free
   or artifact-path policy gates, but none is a production deployment or
   published release result. Preserve the exact terminal job IDs above; do not
   replace them with a later queued/cancelled aggregate status.
2. Resolve P01/P02 first: select the production topology and approved secret
   path, run the policy verifier against the approved artifact, then produce a
   staging deployment with real provider credentials.
3. Close P03–P05 in that staging environment with restore, upgrade/rollback,
   observability and redaction evidence; retain measured RPO/RTO and SLOs.
4. Close P06–P08 with production-like load/soak, failure drills and an owned
   operator runbook/on-call acknowledgement.
5. Configure the protected `w08-production` environment and run the approved
   production-candidate tag workflow; retain its actual Linux/macOS artifacts,
   target-specific manifests/SBOMs, aggregate `SHA256SUMS` and verified
   attestations. Then use W08.17–W08.31's hosted
   attestation path and close P09 with signed
   provenance/SBOM, canary telemetry, rollback evidence and an explicit
   go/no-go approval. W08.12–W08.27's local and hosted artifact-policy
   evidence is not a published release result.
6. If the W08 harness or provider implementation changes, rerun the durable
   and composite hosted rows before reopening W08. Keep native platform rows
   separate from live provider claims.

## External blockers and boundaries

- The local Docker CLI is present, but the current Docker daemon check exits
  before returning server/memory information; the earlier retained capacity
  check also measured `8,232,747,008` available versus `10,737,418,240`
  required for the durable 3PD/3TiKV topology. This is an environment
  capacity/daemon blocker, not permission to downgrade the topology.
- The local macOS host has no usable `/dev/fuse`/`fusermount3`, so Linux FUSE
  evidence must come from the hosted native job; macOS NFS evidence must remain
  a separate macOS row.
- Local TiDB/RustFS credentials and services are not present. The live provider
  rows are intentionally hosted and credential-gated rather than simulated.
- Concurrent pushes to `origin/main` repeatedly cancelled earlier CI runs;
  those are retained only as diagnostic history. The retained run
  `35585066458` reached terminal success for every W08-relevant job before its
  aggregate workflow was later superseded.
- Unrelated hosted failures in that aggregate (FoundationDB, Windows Node and
  macOS/Windows Rust) belong to their own workstreams. They may make the
  aggregate workflow cancelled/red, but they do not become W08 failures when
  every W08-relevant job is terminal success.
- Production rollout is additionally blocked by the absence of an approved
  deployment target, secret/IAM policy, backup/restore environment, production
  observability and paging, representative load target, security sign-off,
  named on-call ownership and release/canary approval. These are intentionally
  recorded as external/provider/hosted gates rather than fabricated local
  passes.

### Latest evidence refresh — 2026-09-22 11:17 AEST

| Evidence item | Status | Evidence and boundary | Remaining action / blocker |
| --- | --- | --- | --- |
| Current tested merged source base `4e0d0d73b671a0a1b85a8b9315e31e27cabb42c4` | PASS — full local source-health and W08 tracking verification | `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0 on the exact merged source; all executed tests passed and provider/native cases remained explicit opt-in skips. `./scripts/cargo-shared clippy --workspace --all-targets --locked -- -D warnings` exited 0. The four W08 rollout/evidence commands returned `W08_ROLLOUT_LEDGER_POLICY_PASS decision=NO-GO functional_items=36 production_gates=9`, `W08_ROLLOUT_LEDGER_TEST_PASS cases=7`, `W08_PRODUCTION_EVIDENCE_POLICY_PASS decision=NO-GO gates=9 closed=0 evidence_records=0 require_go=false` and `W08_PRODUCTION_EVIDENCE_TEST_PASS cases=11`; changed N-API JavaScript files passed `node --check`, package JSON parsed successfully, and `git diff --check` passed. This confirms current source health and tracking controls only; it does not close P01–P09. | Publish this tested merged source with the ledger update, then keep provider/native and production-like gates separate until the required TiDB/RustFS services, native hosts, credentials, owners and protected release environment are available. Keep the packet NO-GO with zero evidence records. |
| Current public source-equivalent checkpoint `ff29dad70e5b3d3a832e99ef55f4fce2967448aa` | PASS — publication and source-boundary verification | `git fetch origin`, `git status --short --branch`, `git rev-parse HEAD`, `git rev-parse origin/main`, `git ls-remote origin refs/heads/main` and `git merge-base --is-ancestor HEAD origin/main` all succeeded; local HEAD, tracking ref and public branch are identical. The public checkpoint contains no Rust or W08 implementation source changes after tested `eeb628da`; the W08 policy/evidence validators still pass. No provider/native runtime or production result is claimed. | Preserve the production `NO-GO` boundary until P01–P09 receive terminal evidence and named approval. |
| Live GitHub production-boundary audit, 2026-09-22 11:08 AEST | BLOCKED — P09 repository/API/environment execution boundary | Fresh read-only `gh run list --workflow w08-production-release.yml` returned HTTP 404; `gh api` for `w08-production` returned HTTP 404; `gh release list` could not resolve the repository; and `git ls-remote` found no `v*-cli-production-candidate*` tag. The fetched public mainline contains the protected workflow file, but these failed API calls are not promoted to a release claim. The last successful release observation remains 08:38 AEST with only `v0.1.0-cli-preview`. This is external GitHub/API and release-configuration evidence, not production acceptance. | Restore/authorize the repository/API surface, configure the protected environment and reviewers/secrets, create an approved immutable candidate tag, run the terminal workflow, then inspect registry assets and execute canary/rollback/approval. |
| Current source-equivalent documentation merge `c178fa86246bdd5f63c3ed1daf972930adbfb67c` | PASS — documentation-only reconciliation after tested source | `git diff --name-status b944218c0fbb37aab2bf6aa2257d4b6b427d461e..HEAD` contains only `WORK_TRACKER.md` and W01 documentation changes; no Rust or W08 implementation source changed after the tested `b944218c` tree. The current W08 policy/evidence validators remain green. No provider/native runtime or production result is claimed. | Publish this source-equivalent checkpoint and preserve the explicit production `NO-GO` boundary. |
| Current reconciled merge checkpoint `703aee1cbd5c19fe95bb84347ae28b39389973b9`, published in `056e7a1584a56018d06ce9c4d1d44a215a2487df` | PASS — JS/docs static reconciliation; Rust baseline unchanged | `node --check` passed for the current changed N-API JavaScript files, including `integrations/mount-rs-napi/postbuild.mjs`; the four W08 rollout/evidence commands passed with `decision=NO-GO`, `functional_items=36`, `production_gates=9`, `closed=0`, `evidence_records=0` and `cases=7/11`; `git diff --check` passed. The merge has no Rust-source changes relative to the separately tested `f4f672dd` baseline. No local native-binding runtime or production/provider result is claimed. | Keep the optional native artifact, provider services/credentials, protected release environment, canary, rollback and owner-approval gates separate until their terminal evidence exists. |
| Current reconciled N-API/JavaScript tip `27ac3c4b5c47a7d0dbeb9723df6b12aed799048c` | PARTIAL — static syntax qualification; native artifact boundary remains explicit | `node --check` passed for the changed `postlude-servers.cjs`, `p9-locks.mjs`, `p9-native.mjs` and `p9-session-metadata.mjs` files. The accompanying Rust source-health result remains the full test/Clippy pass at `f4f672dd`; this N-API tip does not change that Rust result. No local native-binding runtime pass is claimed because the optional platform artifact is absent. No production gate is closed. | Build/install the platform N-API binding before claiming a local runtime parity pass; retain hosted W08.4 Node/N-API evidence as the authoritative consumer acceptance. |
| Local tested source `7f964ca32110468d1d3eaa59a4067afd2ba651fb` / current docs-merged tip `d8d2b8dc` | PASS — current integrated source verification | `./scripts/cargo-shared test --workspace --all-targets --locked --quiet` exited 0 with all runnable tests passing; provider/native rows requiring TiDB, RustFS, PGlite, R2, FUSE, NFS or other host capabilities remained explicit opt-in skips. Strict workspace Clippy with `-D warnings` exited 0. The subsequent `d8d2b8dc` merge changed documentation only, so it is source-equivalent to the tested tip. This is source-health evidence only; no provider, native-host or production rollout gate is closed. | Re-run provider/native and production-like gates when their external services, credentials, host capabilities and owners are available. |
| Public mainline `9b2dabd7` WebDAV N-API parity-test refresh | PARTIAL — syntax passes; local native-artifact runtime boundary | `node --check integrations/mount-rs-napi/test/webdav-session-parity.mjs` exited 0. Running the test exited 1 before the parity body because the checkout has no optional native N-API binding (`Cannot find native binding`, including the platform package/module alternatives). This is the same local artifact boundary already recorded for the Node consumer; hosted W08.4 Node/N-API acceptance remains the functional evidence. No production gate is closed. | Build or install the platform N-API artifact before claiming a local runtime pass; retain hosted/native evidence separately and do not promote this local artifact failure to a W08 production failure. |
| Live GitHub production-boundary audit, 2026-09-22 08:49 AEST | BLOCKED — P09 repository/API/environment execution boundary | Read-only `gh api` checks returned HTTP 404 for the repository, `w08-production` environment and environment-secret surface. `gh release list` could not resolve the repository and `gh run list --workflow w08-production-release.yml` returned HTTP 404, so neither is promoted to current release evidence; the last successful API observation at 08:38 showed only `v0.1.0-cli-preview`. `git ls-remote` found no `v*-cli-production-candidate*` tag, while fetched `origin/main` contains `.github/workflows/w08-production-release.yml`. This is external GitHub/API and release-configuration evidence, not production acceptance. | Restore/authorize the repository API surface, configure the protected environment and reviewers/secrets, create an approved immutable candidate tag, run the terminal workflow, then inspect registry assets and execute canary/rollback/approval. |
| Live GitHub production-boundary audit, 2026-09-22 09:25 AEST | BLOCKED — P09 repository/API/environment execution boundary | Read-only `gh api` checks again returned HTTP 404 for the repository, `w08-production` environment and environment-secret surface. `gh release list` could not resolve the repository and `gh run list --workflow w08-production-release.yml` returned HTTP 404; the last successful release observation remains 08:38 AEST with only `v0.1.0-cli-preview`. `git ls-remote` found no `v*-cli-production-candidate*` tag, while public `origin/main` contains `.github/workflows/w08-production-release.yml`. This is unchanged external GitHub/API and release-configuration evidence, not production acceptance. | Restore/authorize the repository API surface, configure the protected environment and reviewers/secrets, create an approved immutable candidate tag, run the terminal workflow, then inspect registry assets and execute canary/rollback/approval. |
| Live GitHub production-boundary audit, 2026-09-22 10:24 AEST | BLOCKED — P09 repository/API/environment execution boundary | Fresh read-only `gh api repos/andymacclenaghan/mount-rs`, environment, and `gh secret list --env w08-production` checks all returned HTTP 404; `gh release list` could not resolve the repository; and `gh run list --workflow w08-production-release.yml` returned HTTP 404. `git ls-remote` found no `v*-cli-production-candidate*` tag, while the fetched public mainline contains `.github/workflows/w08-production-release.yml`. The last successful release observation remains 08:38 AEST with only `v0.1.0-cli-preview`; the failed current API calls are not promoted to a release claim. This is external GitHub/API and release-configuration evidence, not production acceptance. | Restore/authorize the repository API surface, configure the protected environment and reviewers/secrets, create an approved immutable candidate tag, run the terminal workflow, then inspect registry assets and execute canary/rollback/approval. |
| Live GitHub production-boundary audit, 2026-09-22 10:38 AEST | BLOCKED — P09 repository/API/environment execution boundary | Fresh read-only `gh run list` checks returned HTTP 404 for both `w08-release-policy.yml` and `w08-production-release.yml`; `gh api` for `w08-production` returned HTTP 404; `gh release list` could not resolve the repository; and `git ls-remote` found no `v*-cli-production-candidate*` tag. The fetched public mainline still contains both workflow files. The last successful release observation remains 08:38 AEST with only `v0.1.0-cli-preview`; these failed API calls are not promoted to a release claim. This is external GitHub/API and release-configuration evidence, not production acceptance. | Restore/authorize the repository/API surface, configure the protected environment and reviewers/secrets, create an approved immutable candidate tag, run the terminal workflow, then inspect registry assets and execute canary/rollback/approval. |
| Public current tip `715beb9f` 9P N-API constants/parity support | PARTIAL — JavaScript syntax/static checks pass; local N-API runtime not available | `node --check` passed for `p9.cjs`, `postlude-p9-codec.cjs`, `test/p9-constants.mjs`, `test/webdav-network-concurrency.mjs` and `test/webdav-session-concurrency.mjs`; all four W08 policy/evidence suites also passed. No full local N-API runtime claim is made because the checkout lacks optional native binding artifacts, as recorded above. Hosted W08.4 Node/N-API evidence remains authoritative. | Build or install the platform N-API artifact before claiming local runtime parity; keep this static/native-artifact boundary separate from production P01–P09. |

## Session time log

Times below are approximate engineering/wall-clock accounting for this goal;
hosted CI wait is listed separately from active implementation time. They are
provisional and should be revised when the next terminal CI result is known.

| Time (UTC) | Activity | Active engineering estimate | Wait/external time | Result |
| --- | --- | ---: | ---: | --- |
| 2026-09-21 07:23–07:30 | Read the hosted native failure, traced `EAGAIN` to the CLI not releasing its owned `Filesystem` after transport unmount, and checked the ownership contract. | ~20 min | ~0 min | Root cause identified; no TTL/fencing weakening proposed. |
| 2026-09-21 07:30–07:35 | Patched `examples/node-cli/index.mjs`, ran syntax and diff checks, fetched concurrent `origin/main`, committed `2221144`, and pushed to `origin/main`. | ~15 min | ~2 min | Lifecycle fix published. |
| 2026-09-21 07:35–07:41 | Located hosted CI run `35573697620`, observed the replacement jobs, and separated W08-relevant rows from unrelated failures. | ~5 min | ~6 min | Corrected run in progress; no new pass claimed. |
| 2026-09-21 07:41–07:50 | Wrote and published the initial detailed W08 ledger as `fac9c7e`, then followed the hosted replacement run. | ~15–25 min | ~10 min CI wait | Ledger saved under `docs/`; snapshot later superseded by terminal job results. |
| 2026-09-21 07:50–08:06 | Read the hosted restart diagnostics, confirmed direct provider and ambiguous-commit passes, isolated the repeatable TiDB restart timeout, and serialised top-level provider tests in `17c8bda`. | ~20 min | ~10 min hosted wait | DDL serialization was published in merge tip `a6e870c`; the next durable run still reproduced the timeout. |
| 2026-09-21 08:07–08:24 | Inspected the TiDB v8.5.7 startup path and confirmed `force-init-stats` withholds service until statistics initialization completes; added `--force-init-stats=false` in `769ea08`, merged concurrent main updates, and pushed. | ~20 min | ~10 min CI/queue wait | Restart-readiness fix is published; hosted verification is still queued. |
| 2026-09-21 08:24–08:30 | Reconciled run `35574581481` native pass, run `35576142240` restart failure, and the superseded queue; refreshed this ledger against `origin/main` `6d59d20`. | ~10 min | ~5 min queue observation | W08 remains open pending the next terminal durable and macOS/native results. |
| 2026-09-21 09:56–10:17 | Reconciled terminal W08 run `35585066458`, added the P01–P09 production rollout ledger and tracker, committed the documentation chunk as `d8f8893`, merged concurrent `origin/main` changes, and published the reconciled tip at `8e42efb`. | ~15 min | ~6 min remote fetch/merge/push wait | W08 functional acceptance is complete; production rollout tracking is a separate no-go scope and is now published. |
| 2026-09-21 10:17–10:26 | Added and locally compiled the opt-in TiDB TLS feature through the provider, Rust SDK, CLI and N-API; added a dedicated CI compile gate and documented the boundary between TLS capability and a credentialed production handshake. Implementation commit `3a70238` was reconciled with concurrent main and published at `7b76556`. | ~10 min | ~4 min compile/remote wait | Public consumers can ship the TLS client graph; P01/P07 remain open pending target topology, certificates, secrets and live provider evidence. |
| 2026-09-21 10:26–10:29 | Followed hosted run `35588858142` and retained `tidb-tls-compile` job `106298487587` as terminal success for source `e515036`. | ~2 min | ~1 min hosted wait | Hosted TLS feature compilation passed; live TLS/provider and production rollout gates remain open. |
| 2026-09-21 10:29–10:37 | Added `scripts/test-tidb-tls.sh`, validated its positive and fail-closed URL-policy paths without credentials, and added the guardrail checks to the hosted TLS compile job. | ~8 min | ~0 min | A reproducible live TLS-provider command now exists; no live endpoint or production IAM/certificate evidence is available here. |
| 2026-09-21 10:37–10:41 | Followed hosted run `35589825356` and retained `tidb-tls-compile` job `106301529923` as terminal success for source `ea062c0`. | ~1 min | ~3 min hosted wait | The fail-closed TLS deployment guard is remotely verified; live credentials, certificates and production provider evidence remain open. |
| 2026-09-21 10:41–10:44 | Ran the TLS-enabled TiDB provider unit suite and strict-Clippy gate locally; the six unit tests passed and the ignored service tests remained explicit. | ~3 min | ~0 min | Repository TLS implementation is locally tested and lint-clean; no live endpoint is available for P07 closure. |
| 2026-09-21 10:44–10:50 | Reconciled the current-main hosted TLS pass and added `docs/W08-production-rollout.md` with the deployment contract, executable evidence matrix, rollout sequence and explicit NO-GO/blocker rules; linked it from `WORK_TRACKER.md`. | ~6 min | ~3 min hosted/remote observation | Production tracking now has both a detailed ledger and an operational rollout contract; external deployment gates remain open. |
| 2026-09-21 10:50–10:53 | Followed hosted run `35590919645` and retained `tidb-tls-compile` job `106304951579` as terminal success for source `b68c1f2`. | ~1 min | ~2 min hosted wait | The production-contract revision is remotely verified for TLS implementation/guard behavior; live endpoint, secrets, operations and rollout approval remain open. |
| 2026-09-21 21:00–21:09 AEST | Added W08.6: a credential-free production-config policy verifier, positive and insecure fixtures, and a CI positive/negative gate; ran Node syntax/policy checks and the public Rust CLI `validate-config` check locally. | ~9 min | ~0 min | The deployment shape now fails closed on placeholders, inline secrets, non-HTTPS blocks, non-durable stores and missing/unsafe TLS policy. Hosted verification was pending at this point in the session; the later terminal result is recorded in the next row. P01/P02/P07 remain open for real topology, IAM, certificates and provider evidence. |
| 2026-09-21 21:10–21:16 AEST | Followed hosted run `35592902494` and retained terminal `tidb-tls-compile` job `106311076905` at source `4326c54`; updated the W08.6 evidence ledger. | ~2 min | ~2 min hosted wait; aggregate concurrency continued | Hosted TLS compilation and positive/negative production-config policy checks passed. The aggregate run had unrelated cancellations/failures and is not reported as a W08 aggregate pass; real production gates remain open. |
| 2026-09-21 21:17–21:21 AEST | Re-ran the W08 production policy’s missing/unsafe TLS cases and checked the local provider prerequisites without printing credentials. | ~3 min | ~0 min | Missing `MOUNT_RS_TIDB_TLS_URL`, `require_ssl=false`, `verify_identity=false` and `built_in_roots=false` all failed closed. Provider URLs/credentials are absent; Docker CLI is present but its daemon check is unusable, so no new local provider evidence can be claimed. |
| 2026-09-21 21:22–21:31 AEST | Corrected tracker evidence to the latest hosted policy job and added `docs/W08-operations-runbook.md` with deployment admission, incident response, backup/restore, rollback, observability handoff and D01–D09 timed drills; committed `ea01338`, reconciled concurrent changes and published `0e624d3`. | ~6 min | ~2 min remote fetch/merge/push wait | W08.7 implementation documentation is complete; all operational/provider drills remain explicitly unexecuted until named staging/production owners and systems are available. |
| 2026-09-21 21:32–21:39 AEST | Added W08.8 bounded TiDB/RustFS soak coverage with configurable workload parameters and p50/p95/p99/throughput markers; enabled 64-operation hosted composition coverage; ran syntax, explicit-skip and missing-gate rejection checks. | ~7 min | ~0 min | The soak implementation is ready for hosted qualification; local provider and production-capacity evidence remain unavailable. P06 stays open pending terminal markers and approved workload/resource targets. |
| 2026-09-21 21:44–21:58 AEST | Followed current-main CI run `35595664981`, extracted terminal `tidb-rustfs` job `106319766691` markers, committed the evidence update as `67e0938`, reconciled concurrent main changes and published the result at `077b6c2`; the final ledger-pointer sync was then published as `ffdcf1f`. | ~5 min | ~12 min hosted/remote wait | Both seed and reopen bounded soak phases passed with zero errors and recorded latency/throughput metrics. W08.8 bounded hosted qualification is complete; P06 remains open for production-shaped workload, resource/headroom, failover, multi-hour and SLO/cost evidence. |
| 2026-09-21 21:59–22:03 AEST | Checked the completed aggregate run and enumerated its unrelated failed jobs, then updated the ledger to preserve the W08 job-level PASS versus aggregate FAILURE boundary. | ~3 min | ~1 min hosted evidence lookup | W08 provider/TLS/native job results remain terminal successes; the aggregate is not release-green because other workstreams failed. |
| 2026-09-21 22:04–22:17 AEST | Added the production-facing HTTP health/readiness boundary (`/healthz`, `/readyz`), integration coverage and transport documentation; ran shared-target format, locked `mount-rs-http` tests and strict Clippy, then committed `30dec1d`, reconciled concurrent `origin/main` changes and published the implementation in merge tip `d6abcbe`. | ~10 min | ~4 min shared-target permission retry and remote fetch/merge/push | The local P05 implementation slice passed: 8 unit tests, 10 integration tests and strict Clippy. The probes remain process/configuration evidence only; collector, SLO, provider-health, paging and production alert gates remain open. |
| 2026-09-21 22:18–22:23 AEST | Ran the locked all-features `mount-rs-http` suite and strict Clippy with `-D warnings` against the shared Cargo target. | ~2 min | ~3 min build/test wait | 8 unit tests and 13 integration tests passed, including application telemetry and OTLP propagation; the local observability implementation gate is green, while deployed collector, provider-health, SLO and paging evidence remain external. |
| 2026-09-21 22:24–22:31 AEST | Added the hosted `http-observability` CI job, published it through concurrent main updates, and followed the superseding run `35599817215` at source `b26819e`; job `106333141914` reached terminal success after the earlier run was cancelled by main-branch concurrency. | ~4 min | ~3 min hosted queue/remote observation | Hosted all-features HTTP health/readiness, telemetry/OTLP and strict-Clippy evidence passed at the current revision. The aggregate workflow remains separate; production collector, provider-health, SLO, paging and alert-routing gates remain open. |
| 2026-09-21 22:32–22:47 AEST | Added response-header hardening (`Cache-Control: no-store`, `X-Content-Type-Options: nosniff`) with regression assertions; committed `2f7919a`, reconciled through `cc7bc47`, and reran the all-features HTTP suite and strict Clippy on the latest source. | ~8 min | ~7 min fetch/merge and repeated hosted-run observation | Local hardening passed. Six current-main CI candidates were cancelled or superseded before the new assertion-bearing job reached terminal execution; the subsequent stable run is recorded below. |
| 2026-09-21 22:48–22:56 AEST | Followed the stable queued run `35601990956` at source `f09fbe9`; its `http-observability` job `106340034907` reached terminal success, and ancestry verification confirmed `2f7919a` is included. | ~2 min | ~6 min hosted queue/remote observation | The response-header hardening is now terminally hosted-qualified at the repository contract level. Production collector, provider-aware health, SLO, paging and end-to-end alert gates remain open. |
| 2026-09-21 23:10–23:40 AEST (approx.) | Added the bounded W08.11 release identity/provenance verifier, pending/accepted/invalid fixtures and the hosted `w08-release-policy` job; ran Node/YAML/diff checks, committed `0c93eea`, reconciled concurrent `origin/main` pushes and published the implementation in `d5ede94`, then followed terminal policy job `106356402785` at source `fecec0e`. | ~20 min | ~10 min remote fetch/merge/push and hosted queue observation | The policy implementation is hosted-qualified. P09 remains open for real artifact checksums/provenance, signing, SBOM, target-package verification, canary, rollback and approval. |
| 2026-09-21 23:41–2026-09-22 00:04 AEST (approx.) | Added the W08.12 artifact-manifest generator and CLI preview workflow wiring; ran YAML/syntax/diff checks, built the locked CLI release binary, packaged a production-shaped tarball, verified its manifest/checksum/content, committed `fa1848d`, reconciled concurrent `origin/main` pushes and published in `1c0cf7a`, then followed hosted policy job `106363893748` at source `66544b5`. | ~25 min | ~15 min release build, remote fetch/merge/push and hosted queue observation | Actual artifact manifest wiring is locally and policy-hosted qualified. The tag-triggered release publication, signing/SBOM, target-platform acceptance, canary, rollback and approval remain open. |
| 2026-09-22 00:11–00:30 AEST (approx.) | Added W08.13's dedicated non-cancelling `.github/workflows/w08-release-policy.yml` and removed the duplicate cancellable policy job; ran local YAML/Node/diff checks, committed `a9f51e4`, reconciled concurrent `origin/main` pushes and published in `f432441`, then followed terminal hosted run `35611883547`, source `f432441`, job `106372777281`. | ~10 min | ~8 min remote fetch/merge/push and hosted queue observation | A real Ubuntu CLI artifact, checksum, artifact-derived manifest, direct/extracted runtime smoke path and policy fixtures passed in a stable hosted job. P09 remains open for approved tag publication, signing/SBOM, target-platform acceptance, canary, rollback and release approval. |
| 2026-09-22 00:30–00:51 AEST (approx.) | Added W08.14's locked-Cargo CycloneDX SBOM generator/verifier and wired it into the preview and dedicated release-policy workflows; ran real local artifact binding, license/graph checks, a wrong-source negative test, Node/YAML/diff checks, committed `9f6002c`, reconciled concurrent `origin/main` pushes and published it in `b93be19`, then followed hosted run `35614345209`, source `9c9d0e4`, job `106381893114`. | ~15 min | ~6 min remote fetch/merge/push and hosted queue observation | The hosted Ubuntu artifact path generated and verified a 288-component SBOM bound to the real tarball, while manifest controls remained pending for signature/SBOM attestation/canary. The tag-triggered publication, target-platform parity, cryptographic attestation, canary, rollback and approval remain open. |
| 2026-09-22 00:51–01:03 AEST (approx.) | Tightened W08.15 asset integrity so both release workflows checksum the tarball, manifest and SBOM after generation and verify all three; ran local three-entry checksum/tar/runtime checks, committed `306be46`, reconciled concurrent `origin/main` pushes and published it in `5116ded`, then followed hosted run `35615714935`, source `5116ded`, job `106386240669`. | ~5 min | ~7 min remote fetch/merge/push and hosted queue observation | All three hosted checksum entries passed. The tag-triggered publication, downloaded release inspection, cryptographic attestation, target-platform parity, canary, rollback and approval remain open. |
| 2026-09-22 01:03–01:19 AEST (approx.) | Added `.github/workflows/w08-release-targets.yml` for Linux x86_64/macOS arm64 build/test/package and uploaded-asset download verification; ran YAML/Bash checks and the local locked CLI suite, committed `6188755`, reconciled concurrent `origin/main` pushes and published it in `b0ca8a9`, then followed hosted run `35617415427`, source `b0ca8a9`, through four terminal-success jobs. | ~10 min | ~6 min remote fetch/merge/push and ~5 min hosted target-matrix observation | Both target builds and both downloaded asset sets passed manifest, 288-component SBOM, three-entry checksum, tar-content and target/runtime checks. Actual tag publication, cryptographic attestation, canary, rollback and approval remain open. |
| 2026-09-22 01:19–01:34 AEST (approx.) | Added W08.17's pinned GitHub Sigstore provenance and CycloneDX SBOM attestation wiring to the tag release workflow, plus an explicit `attest=true` target-matrix qualification path; added strict `gh attestation verify` identity constraints, finalized verified manifest controls and checksum regeneration; ran YAML parsing, embedded-Bash syntax, diff checks and the installed `gh` flag-surface check. The implementation was reconciled to current `origin/main` at `86f272c` before this chunk. | ~12 min | ~3 min documentation/static validation; hosted OIDC/attestation execution not yet run | The repository now has an executable hosted attestation path, but no tag release or manual attestation dispatch has created a Sigstore bundle. P09 canary, rollback and approval remain open. |
| 2026-09-22 01:34–01:41 AEST (approx.) | Dispatched W08 target attestation run `35620392878` after publishing the dependency correction; observed GitHub complete it as `cancelled` with zero jobs because concurrent `main` pushes occupied the shared pending concurrency group; inspected the live run list, synchronized to `origin/main` `00e7169`, and isolated manual dispatches from push events in the concurrency key. | ~5 min | ~3 min remote dispatch/status observation | No OIDC/Sigstore action ran and no provider evidence was created. The concurrency fix is ready to publish and a new manual dispatch is required. |
| 2026-09-22 01:41–01:56 AEST (approx.) | Retried the isolated target attestation run `35620932700` at source `11a7b22`; both target builds and both downloaded-asset verification jobs passed, but attestation jobs `106406881524` and `106406881625` failed at hosted setup because the action ref was shortened. Retrieved the exact GitHub error, corrected both workflows to the full `actions/attest@v4.2.2` SHA, and updated the evidence boundary. | ~8 min | ~10 min hosted build/download/attestation wait and log retrieval | Hosted build/download qualification passed. No attestation action step, OIDC token or Sigstore bundle was created; the full-SHA correction must be published and rerun. |
| 2026-09-22 01:56–02:00 AEST (approx.) | Recorded W08.19 as a separate correction chunk, updated the rollout/tracker boundary with the exact setup error and full SHA, and prepared the corrected workflows for publication. | ~3 min | ~1 min local diff/static inspection | The action-resolution defect is corrected locally; hosted attestation qualification remains open until the corrected workflow is pushed and rerun. |
| 2026-09-22 02:00–02:09 AEST (approx.) | Followed corrected run `35622899242` at source `2f43721`: both target builds, downloads, provenance attestations and CycloneDX SBOM attestations executed, but both final verifier steps failed because `gh attestation verify` rejects simultaneous `--signer-repo` and `--signer-workflow`. Removed the redundant `--signer-repo` flag from both workflows and recorded W08.20 for the next rerun. | ~5 min | ~8 min hosted build/download/attestation wait and log retrieval | Hosted attestation generation is proven; hosted identity verification remains open. No final attestation verification PASS or production signature result is claimed. |
| 2026-09-22 02:09–02:22 AEST (approx.) | Published W08.20 as `2ab3cf1` after reconciling concurrent `origin/main`, dispatched manual `attest=true` run `35624385556`, followed it to terminal success, and captured both target hashes, six terminal job IDs, four repository attestation records, four Rekor entries and the two `W08_RELEASE_TARGET_ATTESTATION_PASS` markers. Updated the tracker, production rollout and ledger with W08.21 evidence. | ~8 min | ~5 min hosted build/download/attestation wait plus ~4 min log/evidence retrieval and ~5 min documentation | Hosted target attestation is now terminally qualified for Linux x86_64 and macOS arm64. The approved tag release, release-registry acceptance, staged canary, rollback and owner approval remain open. |
| 2026-09-22 02:22–02:35 AEST (approx.) | Audited the P01–P09 handoff after W08.21, corrected the rollout P09 summary and evidence matrix so the terminal manual attestation is not reported as absent, committed `ff6fd91`, reconciled concurrent mainline changes and published the result in `a8d82ca`. | ~5 min | ~4 min remote fetch/merge/push | The documentation now consistently separates terminal manual target-attestation evidence from the still-unexecuted approved tag release, provider staging, observability, canary, rollback and release-owner gates. |
| 2026-09-22 02:35–02:42 AEST (approx.) | Ran the locked workspace test suite with all targets and then strict workspace Clippy with `-D warnings` on source `5cb96ad`; both completed successfully. Merged concurrent mainline changes at `b381b89` before recording the evidence. | ~5 min | ~1m37s test/lint execution plus ~5 min concurrent-mainline synchronization | Current workspace implementation remains test- and lint-clean on the tested source. Explicitly ignored provider/native rows remain external gates; no production or live-provider claim is added. |
| 2026-09-22 02:42–02:56 AEST | Followed current-main manual target-attestation run `35627761501` at source `50a33ac`, extracted its six terminal job IDs, target digests/sizes, four repository attestation records, four Rekor entries and both final PASS markers, then fetched and merged concurrent mainline tip `e87f0a6` before preparing this ledger update. | ~8 min | ~7 min hosted qualification wait plus ~5 min fetch/merge | The current-main-at-dispatch Linux/macOS target and attestation retest passed. It is retained as hosted qualification evidence only; approved tag publication, registry acceptance, canary, rollback, ownership and production approval remain open. |
| 2026-09-22 03:02–03:14 AEST | Reconciled shared mainline at `5aae52b`, ran `./scripts/cargo-shared test --workspace --all-targets --locked` and strict workspace Clippy with `-D warnings`; both exited 0. The first test attempt hit only the sandbox permission boundary on the configured shared Cargo target and was rerun with the required filesystem access. | ~5 min | ~53s test plus ~35s Clippy execution and ~5 min mainline synchronization | W08.23 source-level verification passed on the current shared source. Provider/native skips and all production P01–P09 gates remain explicitly open; no live-provider or production claim is added. |
| 2026-09-22 03:14–03:25 AEST | Dispatched and followed manual `attest=true` run `35631063978` from published source `cf75835`, then extracted its six terminal job IDs, target digests/sizes, four repository attestation records, four Rekor entries and both final PASS markers. | ~5 min | ~7 min hosted build/download/attestation wait plus log retrieval | W08.24 current published-main Linux/macOS target and attestation qualification passed. The approved tag release, registry acceptance, canary, rollback, ownership and production approval remain open. |
| 2026-09-22 03:25–03:38 AEST | Fetched and fast-forwarded to shared `origin/main` source `f16eec2`; two configured shared-target test attempts stopped before execution because dependency metadata/artifacts were missing, so the full locked workspace test and strict Clippy were rerun against fresh bounded target `/private/tmp/mount-rs-w08-current-cargo-target`. | ~8 min | ~2m36s test/lint execution plus compilation and target-cache diagnosis | W08.25 current-source implementation verification passed. The shared-target failures are recorded as infrastructure only; provider/native skips and all production P01–P09 gates remain explicitly open. |
| 2026-09-22 03:38–03:55 AEST | Dispatched and followed manual `attest=true` run `35633841116`, extracted the six terminal job IDs, Linux/macOS target hashes and sizes, four repository attestation records, four Rekor entries and both final PASS markers, then fetched concurrent mainline tip `eed3423` before preparing this update. | ~8 min | ~9 min hosted build/download/attestation wait plus log retrieval and mainline synchronization | W08.26 current-main Linux/macOS target and attestation qualification passed for source `56aae0c`; the approved tag release, registry acceptance, canary, rollback, ownership and production approval remain open. |
| 2026-09-22 03:55–03:59 AEST | Ran the full locked workspace test and strict workspace Clippy with `-D warnings` on latest integrated source `5c7cc705`, using bounded target `/private/tmp/mount-rs-w08-current-cargo-target`. | ~3 min | ~27.5s test/lint execution plus compilation/setup | W08.27 latest integrated source verification passed. Explicit provider/native skips and all production P01–P09 gates remain open; no live-provider or production claim is added. |
| 2026-09-22 04:08–04:21 AEST | Added W08.28's protected production-candidate release workflow, narrowed the existing preview tag trigger, ran Ruby YAML parsing, embedded Bash `bash -n` (3 preview + 5 candidate blocks) and `git diff --check`, committed `dff55858`, reconciled concurrent `origin/main`, and pushed the result in merge tip `6abc0535`. | ~8 min | ~0 min hosted/provider/native; ~1 min static validation; ~4 min fetch/merge/push | Candidate release admission is implementation-complete but unexecuted. Required environment reviewers/policies, candidate tag, registry publication, canary, rollback and owner approval remain external P09 gates. |
| 2026-09-22 04:21–04:35 AEST | Followed publication-side policy run `35638005702` to terminal success, classified push target run `35638005694` as cancelled with `jobs=[]` before execution, dispatched and followed `attest=true` target run `35638433010`, extracted its six terminal job IDs and target digests, downloaded both public bundles, independently verified checksums/manifests/288-component SBOMs/tar contents, and verified both SLSA and CycloneDX attestations with exact source identity. | ~14 min | ~5 min hosted build/download/attestation observation plus ~2 min artifact/attestation re-verification and ~2 min remote synchronization | W08.29 hosted target qualification passed. The cancellation created no evidence and remains separate; candidate-tag publication, protected environment approval, registry acceptance, canary, rollback and owner approval remain open. |
| 2026-09-22 04:35–04:47 AEST | Reconciled the W08.29 evidence commit with concurrent `origin/main`, pushed the public merge tip, synchronized the ledger and rollout notes to the publication baseline, reconciled one further concurrent mainline update, and verified the resulting public ref. | ~7 min | ~4 min remote fetch/merge/push wait | W08.29 evidence is publicly integrated; pointer-synchronized documentation is present in verified merge tip `3cd43779a8f440b47278e5112f9ec48b12f43e23`. Production candidate publication, canary, rollback and approval remain open. |
| 2026-09-22 04:47–04:51 AEST | Ran the locked offline workspace test and strict workspace Clippy with `-D warnings` on public source `76c2b1a863c23afe71c0591d0a480433e1b9078d`, using bounded target `/private/tmp/mount-rs-w08-final-cargo-target`. | ~0.15 engineer-day | ~46s test execution after compilation plus ~35s Clippy execution | W08.30 source verification passed. Explicit provider/native prerequisites and all production P01–P09 gates remain open; no live-provider or production claim is added. |
| 2026-09-22 04:51–05:11 AEST | Rechecked the production-config policy with a policy-only TLS URL: the positive fixture passed, the insecure fixture failed closed and the negative assertion passed. Followed hosted run `35641555767` to terminal success, downloaded both target bundles, independently verified checksums/manifests/288-component SBOMs/archive contents and the extracted macOS runtime, and ran exact-identity SLSA/CycloneDX attestation verification. | ~0.15 engineer-day | ~9 min hosted build/download/attestation execution plus queue/remote observation | W08.31 hosted target qualification passed. Candidate-tag publication, protected-environment approval, registry acceptance, staging/provider operations, canary, rollback, on-call and release-owner approval remain open. |
| 2026-09-22 05:11–05:34 AEST | Committed the W08.31 tracker, ledger and rollout evidence as `d293fe5b`, reconciled concurrent `origin/main` changes twice, handled one non-fast-forward push rejection without force, and published the reconciled result. | ~0.1 engineer-day | ~0.15 engineer-day concurrent-main fetch/merge/push wait | The W08.31 evidence is publicly integrated at `682d2441`; `HEAD` and `origin/main` match and the worktree is clean. |
| 2026-09-22 05:34–05:36 AEST | Added the exact public-pointer reference, committed the pointer sync as `7dbe0ce3`, pushed it, refreshed `origin/main` and verified the clean equal refs. | ~0.03 engineer-day | ~2 min remote push/fetch | The final W08.31 ledger pointer is public at `7dbe0ce3`; no production gate was changed or inferred as passed. |
| 2026-09-22 05:36–05:53 AEST | Added the fail-closed W08 rollout-ledger verifier/test and wired both controls into the dedicated hosted release-policy workflow; local syntax, baseline and six-case transition tests passed. Followed hosted run `35647096385` from source `2641962a6a65179abf4b8d785345fbe6af4be9b8` to terminal success, including job `106490142559`; classified cancelled run `35646848615` with `jobs=[]` as no evidence. | ~0.5 engineer-day | ~2m37s hosted policy execution plus queue/terminal observation; documentation synchronization and push remain separate | W08.32 implementation/static qualification and hosted tracking-control qualification are complete. The production decision remains NO-GO and P01–P09 remain external gates. |
| 2026-09-22 05:53–current | Added `--require-go` to the fail-closed rollout-ledger verifier, added production-admission negative/complete-GO coverage and wired a required admission job before the protected candidate build matrix; `node --check`, the seven-case suite, YAML parsing and `git diff --check` passed. | ~0.35 engineer-day | ~0 min hosted candidate admission by design; local qualification completed | W08.33 is an implementation safety control. The current production decision remains NO-GO; no candidate tag or production gate is claimed. |
| 2026-09-22 05:53–06:11 AEST | Followed the post-push W08.33 policy state: push run `35648755697` for source `86a87ed0` and manual-dispatch run `35648898876` (resolved source `987c593b`) both cancelled before job creation; no hosted admission result was promoted to evidence. Committed the correction as `969339b8`, reconciled concurrent mainline changes twice, and published the result in public ref `38a995f5`. | ~0.1 engineer-day | ~1 min hosted queue/cancellation observation plus ~4 min fetch/merge/push reconciliation | W08.33 remains locally qualified only. The cancellation boundary is external hosted scheduling state; the production decision remains NO-GO and no candidate tag or production gate is claimed. |
| 2026-09-22 06:13–06:28 AEST | Added the machine-readable NO-GO production-evidence packet, fail-closed validator/test and workflow admission wiring; local packet/rollout policy, ten-case evidence suite, seven-case rollout suite, YAML parsing and diff hygiene passed. Followed successful run `35650533691` and classified it as pre-W08.34, then classified current-tip run `35650626028` as cancelled with `jobs=[]`. Committed the boundary as `25ff75c1`, reconciled concurrent mainline changes twice, and published the pointer-synchronized result at `73b0936d`. | ~0.5 engineer-day | ~2m2s hosted run observation plus queue/cancellation boundary and ~5 min fetch/merge/push reconciliation; local qualification completed | W08.34 is implementation/static qualification only. No hosted packet-validator evidence is claimed; all P01–P09 remain open and the rollout decision remains NO-GO until actual production-like evidence and approval are supplied. |
| 2026-09-22 06:28–06:37 AEST | Followed current-tip hosted run `35651363875` at source `14f8c344`, job `106504528377`, to terminal success; the W08 policy and existing release-identity/provenance steps passed. Committed the hosted-success update as `aec7fbdd`, reconciled concurrent mainline changes three times, and published it in public ref `b649ed51`. | ~0.05 engineer-day | ~2m41s hosted job runtime plus queue/terminal observation and ~8 min fetch/merge/push reconciliation | W08.34 hosted implementation/static qualification passed. The packet remains NO-GO with zero production evidence records; P01–P09 and production approval remain open. |
| 2026-09-22 06:37–06:51 AEST | Rechecked current Docker/environment capability without exposing secrets; durable TiDB failed closed below the 10 GiB floor, and the supported single-node real-TiDB smoke entered PD startup before Docker returned `Bad response from Docker engine` and exited 125. | ~0.15 engineer-day | ~6 min container startup/diagnostic observation | No provider acceptance or production evidence is claimed. Restore a healthy Docker engine/capacity before retrying local component qualification; P01–P09 remain open. |
| 2026-09-22 06:55–07:03 AEST | Hardened W08.35 real TiDB Docker preflight to use one formatted server-info probe before resource creation; ran `sh -n scripts/test-tidb.sh` and the unavailable-daemon single-node check, which exited 2 with the new prerequisite-boundary message and no acceptance marker. Updated the tracker, rollout note, README and ledger to preserve the implementation/provider/production evidence boundary. | ~0.2 engineer-day | ~1–2 min local fail-closed validation; hosted qualification pending push | The harness correction is implementation/static evidence only. Durable TiDB, TiDB/RustFS composition and all P01–P09 production gates remain open until a healthy, adequately provisioned Docker/provider or approved production-like environment is available. |
| 2026-09-22 07:03–07:12 AEST | Committed W08.35 as `c4d1f8bd`, reconciled concurrent mainline changes and pushed merge tip `22f82dca` to `origin/main`. The automatic push run `35655178863` cancelled before jobs; manual credential-free run `35655299194`, job `106517465871`, resolved source `4efec58c`, completed successfully in 2m15s. Fetched public `origin/main` at `c45f3931` after concurrent threads advanced it. | ~0.1 engineer-day | ~2m15s hosted policy/artifact execution plus queue/cancellation and fetch/merge/push observation | W08.35 hosted implementation/static qualification passed. The production packet remains NO-GO with nine open gates and zero evidence records; no provider, candidate release, registry, canary, rollback or approval evidence is claimed. |
| 2026-09-22 07:16–07:24 AEST | Added W08.36 placeholder rejection, ran the local 36-item/11-case policy suite, committed implementation as `0855e2ab`, reconciled concurrent mainline changes and pushed merge tip `e0ab07d6`. Hosted run `35656592580`, job `106521925802`, completed successfully in 2m45s at source `e0ab07d6`; fetched public `origin/main` at `57ade44a` after concurrent threads advanced it. | ~0.2 engineer-day | ~2m45s hosted policy/artifact execution plus queue and fetch/merge/push observation | W08.36 hosted implementation/static qualification passed. The production packet remains NO-GO with nine open gates and zero evidence records; no provider, candidate release, registry, canary, rollback or approval evidence is claimed. |
| 2026-09-22 07:24–07:27 AEST | Reconciled the concurrent public changes, reran the 36-item rollout verifier, seven-case transition suite, NO-GO evidence validator and 11-case evidence suite, and pushed the W08.36 hosted-result ledger as public merge tip `08f3dd03`. | ~0.05 engineer-day | ~1–2 min fetch/merge/push reconciliation | The final public tip is clean and equal to `origin/main`; the W08 production packet remains NO-GO with nine open gates and zero evidence records. |
| 2026-09-22 07:27–07:29 AEST | Pushed the pointer-only ledger synchronization as `f1362442` and verified `HEAD == origin/main` with a clean worktree. | ~0.03 engineer-day | ~1 min final push/fetch verification | No W08 implementation or production-gate state changed; the packet remains NO-GO with nine open gates and zero evidence records. |
| 2026-09-22 07:29–07:36 AEST | Ran a read-only live production-boundary audit, fetched the concurrent `origin/main` tip, and recorded the P09 environment/tag/workflow findings in the ledger, rollout contract and tracker. | ~0.1 engineer-day | ~3 min GitHub API/CLI observation plus ~2 min mainline synchronization | The protected workflow file exists, but `w08-production`, its environment-secret surface, the candidate tag and production workflow runs are absent; only the CLI preview prerelease exists. P09 remains externally blocked and the rollout stays NO-GO. |
| 2026-09-22 07:36–07:49 AEST | Committed the audit documentation as `fd29a297`, reconciled concurrent `origin/main` changes, reran the 36-item/7-case/NO-GO/11-case W08 checks, and pushed the reconciled public merge tip `a40f5b8d`; then verified `HEAD == origin/main` and confirmed the audit commit is in remote ancestry. | ~0.05 engineer-day | ~2 min validator execution plus ~8 min fetch/merge/push retries caused by concurrent mainline updates | The live P09 boundary audit is publicly integrated; the packet remains NO-GO with nine open gates and zero evidence records. |
| 2026-09-22 07:49–08:06 AEST | Fast-forwarded to public source `6a19ff65`, re-ran all four W08 ledger/evidence validators, ran the full locked workspace test suite (exit 0) and strict workspace Clippy (exit 0), and prepared the current-source evidence update. | ~0.2 engineer-day | ~1 min mainline synchronization plus ~1 min test and ~54s Clippy execution | Source `6a19ff65` is test- and lint-clean; explicit provider/native skips and all nine production gates remain open. Subsequent unrelated merges were not promoted to this test result. |
| 2026-09-22 08:06–08:10 AEST | Committed the current-source verification as `4c6628ac`, reconciled concurrent mainline changes, reran the W08 policy suites, and pushed the public merge tip `ffc6efa5`; corrected the ledger wording to retain the exact tested source boundary. | ~0.05 engineer-day | ~2 min fetch/merge/push reconciliation | The verification evidence is publicly integrated, but the newer merge tip needs a fresh full workspace run before being called current-source verified. |
| 2026-09-22 08:10–08:13 AEST | Re-ran the full locked workspace test and strict Clippy on final public tip `49778fde`; both exited 0, then prepared this final-tip evidence update. | ~0.1 engineer-day | ~30s test execution plus ~1s Clippy execution | Final public source is test- and lint-clean; explicit provider/native skips and all nine production gates remain open. |
| 2026-09-22 08:13–08:16 AEST | Re-ran the full locked workspace test and strict Clippy on newly published tip `6e27d46c` after the concurrent NFS process-restart update; both exited 0. | ~0.1 engineer-day | ~40s test execution plus ~4s Clippy execution | Latest public source is test- and lint-clean; explicit provider/native skips and all nine production gates remain open. |
| 2026-09-22 08:16–08:20 AEST | Re-ran the full locked workspace test and strict Clippy on newly published tip `2d459e15` after concurrent 9P/N-API, chunked and Ozone updates; both exited 0. | ~0.1 engineer-day | ~50s test execution plus ~18s Clippy execution | Latest public source is test- and lint-clean; explicit provider/native skips and all nine production gates remain open. |
| 2026-09-22 08:20–08:23 AEST | Re-ran the full locked workspace test and strict Clippy on newly published tip `b27dd2bb` after concurrent FUSE fixes; both exited 0. | ~0.1 engineer-day | ~53s test execution plus ~26s Clippy execution | Latest public source is test- and lint-clean; explicit provider/native skips and all nine production gates remain open. |
| 2026-09-22 08:23–08:26 AEST | Committed the FUSE-tip qualification update as `efffd3d4`, reran the four W08 policy suites, reconciled and pushed it to `origin/main`; the direct remote check then observed concurrent public tip `f156bc0f`. Git could not refresh the local remote-tracking ref because the filesystem had only 121 MiB free, but the worktree remained clean. | ~0.05 engineer-day | ~2 min fetch/merge/push and disk-space observation | The W08 qualification commit was accepted by the push; `b27dd2bb` remains the exact tested source, and no claim is made about the newer untested remote tip. |
| 2026-09-22 08:26–08:31 AEST | Fetched and reconciled the moving mainline through `e39352d1`, reran the four W08 policy suites, pushed the synchronized ledger at `0ab9bf41`, and then ran the full locked workspace test and strict Clippy on that exact public tip; both exited 0. | ~0.15 engineer-day | ~1 min fetch/merge/push plus ~21s test and ~2s Clippy execution | Final public source `0ab9bf41` is test- and lint-clean; explicit provider/native skips and all nine production gates remain open. |
| 2026-09-22 08:31–08:33 AEST | Reconciled concurrent 9P/WebDAV changes and published the documentation at `2d840df9` after the `0ab9bf41` source run; preserved the exact tested-source boundary in the ledger. | ~0.05 engineer-day | ~1 min merge/push | W08 policy tracking remains green; the newer public source tip is explicitly awaiting a fresh full workspace run. |
| 2026-09-22 08:33–08:38 AEST | Fast-forwarded to `be721a20`, reran all W08 policy/evidence suites, ran the full locked workspace test and strict Clippy (both exit 0), and performed a fresh read-only production-boundary audit. | ~0.15 engineer-day | ~1 min mainline synchronization plus ~30s source checks and ~9s GitHub audit | Current source is test- and lint-clean; the production API/environment/tag/run boundary remains externally blocked, and P01–P09 remain open. |
| 2026-09-22 08:38–08:42 AEST | Published the reconciled W08 evidence refresh at `b7d77432`, then ran the full locked workspace test and strict Clippy on that exact public tip; both exited 0. | ~0.1 engineer-day | ~53s test execution plus ~2s Clippy execution | Latest public source is test- and lint-clean; explicit provider/native skips and all nine production gates remain open. |
| 2026-09-22 08:43–08:50 AEST | Fast-forwarded this checkout to fetched `origin/main` `229a9cd5`, reran the 36-item rollout verifier, seven-case transition suite, NO-GO evidence validator and 11-case evidence suite, then ran the full locked workspace test and strict workspace Clippy; all exited 0. Refreshed the read-only GitHub production-boundary audit at 08:49 AEST and recorded the API 404 boundary. | ~0.15 engineer-day | ~7s policy checks, ~18s Cargo test/Clippy execution, plus ~9s GitHub audit and mainline synchronization | `229a9cd5` is current integrated source-health evidence; provider/native opt-in rows and all nine production gates remain open. The production API/environment/tag/run boundary remains externally blocked. |
| 2026-09-22 08:50–08:59 AEST | Merged concurrent NFS/9P/FUSE mainline work, reran all four W08 policy/evidence suites, then reran the full locked workspace test and strict workspace Clippy on tested source `77114d4d`; both exited 0. A subsequent site-documentation-only merge produced `fc785237`. | ~0.15 engineer-day | ~30s policy checks plus ~30s Cargo test/Clippy execution and mainline reconciliation | `77114d4d` is the exact tested source and `fc785237` is source-equivalent; provider/native opt-in rows and all nine production gates remain open. |
| 2026-09-22 09:00–09:07 AEST | Merged the 9P parity update through `3c884bd8`, reran all four W08 policy/evidence suites, then ran the full locked workspace test and strict workspace Clippy on exact merge `7addaf1280bd64a4e54a1e9a0bf463253c67ac89`; all exited 0. | ~0.15 engineer-day | ~30s policy checks plus ~20s Cargo test/Clippy execution and mainline reconciliation | `7addaf12` is current integrated source-health evidence; provider/native opt-in rows and all nine production gates remain open. |
| 2026-09-22 09:07–09:10 AEST | Merged the bounded FUSE teardown update through `fb6f04c5`, reran all four W08 policy/evidence suites, then ran the full locked workspace test and strict workspace Clippy on exact merge `4b9787f9288e923dbe79e3aa56c4b9d6d9c1d85a`; all exited 0. | ~0.15 engineer-day | ~30s policy checks plus ~30s Cargo test/Clippy execution and mainline reconciliation | `4b9787f9` is current integrated source-health evidence; provider/native opt-in rows and all nine production gates remain open. |
| 2026-09-22 09:10–09:15 AEST | Fast-forwarded to public tip `9b2dabd7`, reran all W08 policy/evidence suites, syntax-checked the new WebDAV N-API parity test, and attempted its local runtime. | ~0.05 engineer-day | ~6s policy/syntax/runtime check and mainline synchronization | Syntax passed; runtime stopped before execution because the optional local N-API binding is absent. Hosted W08.4 evidence remains valid; no production-gate state changed. |
| 2026-09-22 09:15–09:23 AEST | Merged the published NFS restart-fencing and WebDAV parity updates through `656a8dab`, reran all four W08 policy/evidence suites, then ran the full locked workspace test and strict workspace Clippy on exact merge `7f964ca32110468d1d3eaa59a4067afd2ba651fb`; both exited 0. Merged the subsequent documentation-only public tip `b8d6a25b` as `d8d2b8dc`. | ~0.2 engineer-day | ~30s policy checks plus ~1m10s Cargo test/Clippy execution and mainline reconciliation | `7f964ca3` is the exact tested source and `d8d2b8dc` is source-equivalent; provider/native opt-in rows and all nine production gates remain open. |
| 2026-09-22 09:23–09:27 AEST | Merged the public 9P parity documentation through `2667bc72`, reran the four W08 policy/evidence suites, pushed the reconciled W08 chunk at `a6a86e40370d5ac0723becc062fe811cb7d917b7`, and refreshed the read-only production boundary at 09:25 AEST. | ~0.1 engineer-day | ~30s policy checks, ~4 min remote merge/push, ~13s GitHub audit | Public refs matched `a6a86e40` after push; the production repository/environment API remains 404, no candidate tag is present, and all nine production gates remain open. |
| 2026-09-22 09:27–09:31 AEST | Merged the published 9P constants/N-API support through `61b904c3`, reran all four W08 policy/evidence suites and JavaScript syntax checks for the changed files. | ~0.05 engineer-day | ~30s policy/syntax checks and mainline reconciliation | Current public N-API support is syntax-clean; full local runtime remains blocked by missing optional binding artifacts. The Rust source-health result at `7f964ca3` remains valid; no production-gate state changed. |
| 2026-09-22 09:34–09:44 AEST | Fetched and reconciled concurrent W05/W07/9P/NFS mainline work through source base `4268559c`, reran the full locked workspace test and strict Clippy, then reran the four W08 rollout/evidence policy suites and diff check. The initial test invocation hit the shared Cargo-target permission boundary and was retried with the prescribed external target access. | ~0.2 engineer-day | ~1 min fetch/merge reconciliation, ~53s test, ~35s Clippy, ~1 min policy/evidence checks | Current source base is test- and lint-clean; W08 tracking controls are green with 36 implementation items and 9 open production gates. The packet remains NO-GO with zero evidence records; provider/native, protected GitHub environment, candidate release, canary, rollback and approval gates remain external. |
| 2026-09-22 09:44–09:47 AEST | Merged concurrent FUSE/W07/W04/9P/NFS mainline work as `4b31858a`, reran the full locked workspace test and strict Clippy on that exact merged source, then reran all four W08 rollout/evidence policy suites. | ~0.1 engineer-day | ~17s test, ~16s Clippy, ~1s policy/evidence checks | The merged source is test- and lint-clean; W08 tracking controls remain green with 36 implementation items and 9 open production gates. The packet remains NO-GO with zero evidence records; provider/native, protected GitHub environment, candidate release, canary, rollback and approval gates remain external. |
| 2026-09-22 09:47–09:50 AEST | Merged concurrent WebDAV/NFS source and parity updates as `b5369394`, reran the full locked workspace test and strict Clippy on that exact merged source, then reran all four W08 rollout/evidence policy suites. | ~0.15 engineer-day | ~27s test, ~38s Clippy, ~1s policy/evidence checks | The merged source is test- and lint-clean; W08 tracking controls remain green with 36 implementation items and 9 open production gates. The packet remains NO-GO with zero evidence records; provider/native, protected GitHub environment, candidate release, canary, rollback and approval gates remain external. |
| 2026-09-22 09:50–09:54 AEST | Merged the concurrent WebDAV ledger and NFS fixture-isolation updates as `4c8dbfcb`, reran the full locked workspace test and strict Clippy on that exact merged source, then reran all four W08 rollout/evidence policy suites. | ~0.15 engineer-day | ~12s test, ~44s Clippy, ~1s policy/evidence checks | The merged source is test- and lint-clean; W08 tracking controls remain green with 36 implementation items and 9 open production gates. The packet remains NO-GO with zero evidence records; provider/native, protected GitHub environment, candidate release, canary, rollback and approval gates remain external. |
| 2026-09-22 09:54–09:58 AEST | Merged concurrent W26/WebDAV/9P/chunked updates as `97986b66`, reran the full locked workspace test and strict Clippy on that exact merged source, reran all four W08 rollout/evidence policy suites, and syntax-checked the changed N-API tests. | ~0.2 engineer-day | ~1m35s test, ~26s Clippy, ~1s policy/evidence/syntax checks | The merged source is test- and lint-clean; W08 tracking controls remain green with 36 implementation items and 9 open production gates. The packet remains NO-GO with zero evidence records; provider/native, protected GitHub environment, candidate release, canary, rollback and approval gates remain external. |
| 2026-09-22 09:58–10:01 AEST | Merged concurrent NFS completion-order, S3 workflow, W07 and site qualification updates as `916d1a77`, reran the full locked workspace test and strict Clippy on that exact merged source, then reran all four W08 rollout/evidence policy suites. | ~0.2 engineer-day | ~16s test, ~1s Clippy, ~1s policy/evidence checks plus remote reconciliation | The merged source is test- and lint-clean; W08 tracking controls remain green with 36 implementation items and 9 open production gates. The packet remains NO-GO with zero evidence records; provider/native, protected GitHub environment, candidate release, canary, rollback and approval gates remain external. |
| 2026-09-22 10:01–10:11 AEST | Published the reconciled W08 checkpoint as remote `5d9dad42`, verified `HEAD == origin/main == refs/heads/main`, then reran the full locked workspace test and strict Clippy on that exact pushed tip and reran all four W08 rollout/evidence policy suites. | ~0.2 engineer-day | ~1m test, ~27s Clippy, ~1s policy/evidence checks plus ~8 min fetch/merge/push retries caused by concurrent mainline commits | The published source is test- and lint-clean; the W08 tracking controls remain green with 36 implementation items and 9 open production gates. The packet remains NO-GO with zero evidence records; provider/native, protected GitHub environment, candidate release, canary, rollback and approval gates remain external. |
| 2026-09-22 10:11–10:14 AEST | Merged the latest WebDAV provider-concurrency and NFS close-waiter changes as `06b37182`, reran the full locked workspace test and strict Clippy on that exact merge, then reran all four W08 rollout/evidence policy suites. | ~0.2 engineer-day | ~30s test, ~21s Clippy, ~1s policy/evidence checks plus remote reconciliation | The merge is test- and lint-clean; the W08 tracking controls remain green with 36 implementation items and 9 open production gates. This source is pending the next normal push; the packet remains NO-GO with zero evidence records and all production/provider/native blockers unchanged. |
| 2026-09-22 10:14–10:18 AEST | Merged the FUSE forced-teardown and WebDAV hosted-status updates as `baaaad44`, reran the full locked workspace test and strict Clippy on that exact merge, then reran all four W08 rollout/evidence policy suites. | ~0.2 engineer-day | ~22s test, ~21s Clippy, ~1s policy/evidence checks plus remote reconciliation | The merge is test- and lint-clean; the W08 tracking controls remain green with 36 implementation items and 9 open production gates. This source is pending the next normal push; the packet remains NO-GO with zero evidence records and all production/provider/native blockers unchanged. |
| 2026-09-22 10:18–10:20 AEST | Merged the WebDAV provider-network and NFS in-flight-concurrency qualifications as `f4f672dd`, reran the full locked workspace test and strict Clippy on that exact merge, then reran all four W08 rollout/evidence policy suites. | ~0.2 engineer-day | ~8s test, ~1s Clippy, ~1s policy/evidence checks plus remote reconciliation | The merge is test- and lint-clean; the W08 tracking controls remain green with 36 implementation items and 9 open production gates. This source is pending the next normal push; the packet remains NO-GO with zero evidence records and all production/provider/native blockers unchanged. |
| 2026-09-22 10:20–10:22 AEST | Merged the 9P optional-absence/N-API server-view corrections as `27ac3c4b`, syntax-checked all changed JS files, and reran all four W08 rollout/evidence policy suites. | ~0.1 engineer-day | ~1s syntax/policy/evidence checks plus remote reconciliation | Static N-API checks pass; the optional native binding remains unavailable locally, so no runtime parity pass is claimed. The Rust baseline remains `f4f672dd` and W08 production remains NO-GO with nine open gates and zero evidence records. |
| 2026-09-22 10:22–10:25 AEST | Ran a fresh read-only GitHub/Git production-boundary audit and recorded the repository/environment/secrets/release/run 404s, missing candidate tag, and present protected workflow file. | ~0.05 engineer-day | ~12s API/Git observation | P09 remains externally blocked; no release, canary, rollback, approval or production evidence was created. The packet remains NO-GO with nine open gates and zero evidence records. |
| 2026-09-22 10:25–10:34 AEST | Reconciled the concurrent `origin/main` merge at `703aee1c`, syntax-checked the newly merged `postbuild.mjs` and related N-API JavaScript paths, reran the four W08 rollout/evidence validators, committed the ledger checkpoint as `2a99acdd`, merged concurrent mainline work, and pushed public merge tip `056e7a15`. | ~0.05 engineer-day | ~1s static/policy checks plus ~10 min fetch/merge/push reconciliation | The current JS/docs merge is clean and contains no Rust-source changes after tested baseline `f4f672dd`; `HEAD == origin/main == refs/heads/main` was verified. W08 remains functionally accepted but production `NO-GO` with nine open gates and zero evidence records. |
| 2026-09-22 10:34–10:38 AEST | Re-audited the live W08 hosted release/API boundary, checked both workflow histories, the protected environment, releases and candidate tags, and updated the tracker, rollout contract and ledger with the unchanged 404/no-tag result. | ~0.03 engineer-day | ~9s read-only GitHub/Git observation | No production mutation was attempted. Both workflow-list queries, the environment API and release surface remain unavailable; no candidate tag exists, so P09 remains externally blocked and the packet stays NO-GO with zero evidence records. |
| 2026-09-22 10:38–10:41 AEST | Merged concurrent `origin/main` source `9da45327` as exact tree `e88099a0`, ran the full locked workspace test and strict Clippy, syntax-checked changed N-API JavaScript files, and reran all four W08 rollout/evidence suites. | ~0.15 engineer-day | ~30s test/Clippy/policy execution plus merge reconciliation | The exact merged source is test- and lint-clean with provider/native prerequisites still explicit skips; W08 remains functionally accepted and production `NO-GO` with nine open gates and zero evidence records. The documentation update is pending normal publication. |
| 2026-09-22 10:41–10:49 AEST | Reconciled concurrent FUSE/W07/mainline updates through tested source `b944218c`, reran the full locked workspace test and strict Clippy, then merged the documentation-only public tip as source-equivalent `c178fa86`. | ~0.15 engineer-day | ~1m test/Clippy execution plus repeated mainline reconciliation | The tested source is clean and the following merge changes only W01/tracker documentation; W08 remains functionally accepted and production `NO-GO` with nine open gates and zero evidence records. This qualification is pending publication. |
| 2026-09-22 10:49–10:52 AEST | Reconciled concurrent N-API/S3-test/mainline updates through tested source `e8c60438`, reran the full locked workspace test and strict Clippy, and retained the following `6a35eb6c` merge as source-equivalent because it changed only W08 documentation after the tested tree. | ~0.15 engineer-day | ~25s test/Clippy execution plus mainline reconciliation | The current source-equivalent checkpoint is clean; W08 remains functionally accepted and production `NO-GO` with nine open gates and zero evidence records. This qualification is pending publication. |
| 2026-09-22 10:52–10:57 AEST | Reconciled the FUSE fix through tested source `eeb628da`, reran the full locked workspace test and strict Clippy, syntax-checked changed N-API JavaScript, reran all W08 policy/evidence suites, and merged the unrelated documentation-only tip as source-equivalent `ae67f691`. | ~0.15 engineer-day | ~30s test/Clippy/policy execution plus mainline reconciliation | The tested source and current source-equivalent tree are clean; W08 remains functionally accepted and production `NO-GO` with nine open gates and zero evidence records. This qualification is pending publication. |
| 2026-09-22 10:57–10:59 AEST | Committed the FUSE-qualified ledger update as `052dc184`, reconciled concurrent mainline documentation, pushed public merge tip `a921fe76`, and verified exact local/remote equality and ancestry. | ~0.05 engineer-day | ~6s fetch/merge/push verification | The latest W08 qualification is publicly integrated; the packet remains NO-GO with nine open gates and zero evidence records. No production mutation, candidate release, canary, rollback or approval was attempted. |
| 2026-09-22 10:59–11:01 AEST | Reconciled the public-pointer synchronization and concurrent site/W01 documentation, pushed public tip `ff29dad7`, and reverified exact local/remote equality, ancestry and W08 admission state. | ~0.03 engineer-day | ~8s fetch/merge/push/policy verification | The W08 ledger now points at the actual public tip; the packet remains NO-GO with nine open gates and zero evidence records. No production mutation, candidate release, canary, rollback or approval was attempted. |
| 2026-09-22 11:01–11:09 AEST | Fast-forwarded to current public source `a81a0827`, ran the full locked workspace test and strict Clippy, syntax-checked changed N-API JavaScript, parsed package JSON, reran all W08 policy/evidence suites, and refreshed the live production-boundary audit. | ~0.2 engineer-day | ~1m test/Clippy/policy execution plus ~9s hosted-boundary observation | Current source health is clean; provider/native prerequisites remain explicit skips. The audit still finds no accessible production workflow/environment/release or candidate tag, so P09 remains blocked and the packet stays NO-GO with zero evidence records. |
| 2026-09-22 11:09–11:17 AEST | Merged concurrent public tip `d0a67b22`, reran the full locked workspace test and strict Clippy on merged tip `4e0d0d73`, syntax-checked changed N-API JavaScript, parsed package JSON, reran all W08 policy/evidence suites, and ran diff checks. | ~0.15 engineer-day | ~53s test/Clippy/policy execution plus ~9s merge/static verification | The merged source tree is clean and production remains NO-GO; provider/native prerequisites remain explicit skips and P09 still lacks a reachable workflow/environment/release/candidate tag. This qualification is pending publication. |
| Prior goal phase before this ledger request | TiDB/RustFS harness hardening, native process-identity fix, TiDB/TiKV descriptor and bootstrap fixes, hosted-log analysis and repeated CI queue monitoring. | **Substantial; exact active split not instrumented** | Goal telemetry previously reported roughly 2 h 41 min elapsed, including tool/CI waits | Implementation chunks were committed and pushed; W08 functional acceptance is complete and production gates remain open. |

## Update protocol

For each subsequent W08 chunk, append or revise the relevant row with:

- exact commit and `origin/main` SHA;
- exact command or hosted job ID, including exit/conclusion;
- separate implementation, provider, native and hosted status;
- remaining actions and provisional active/wall-time estimates; and
- any new blocker without converting a skip or cancellation into a pass.

Commit and push each validated ledger or implementation chunk to `origin/main`.
W08 functional acceptance is complete only when the required W08.2, W08.3 and
W08.4b integration/provider/platform evidence is terminal and recorded in both
this ledger and `WORK_TRACKER.md`. Production rollout is complete only when
P01–P09 have terminal evidence, named ownership and an explicit GO decision;
demo approval, local tests, hosted job presence or a cancelled aggregate are
not substitutes.
