# W01-FUSE progress tracker

Parent roll-up: [W01 progress ledger](./W01_PROGRESS.md)

Owner: delegated W01-FUSE task `01a0c45c-4283-7473-9588-59c41cd1def0`  
Worktree: `/Users/andrewmcclenaghan/.codex/worktrees/a0af/mount-rs`  
Branch: `andymac4182/c/w01-fuse-main-20260922`

Last refreshed: **2026-09-22** (Australia/Brisbane)

Status: **In progress — production NO-GO**

This tracker owns the FUSE protocol, mount-free session, native mount,
callback, and lifecycle boundary. A focused codec or session pass does not
close native Linux mount, hosted CI, or root-callback acceptance; the
macOS/FSKit boundary is an explicit unsupported-scope decision recorded below.

| Gate | State | Required evidence |
| --- | --- | --- |
| Public FUSE exports and typed/raw protocol behavior | In progress | Pinned-oracle body/whole-message differentials and generated declarations |
| Rust-backed mount-free session | In progress | INIT, options, cache, lookup, flush, errors, handles, callbacks, and lifecycle coverage |
| Linux native FUSE | External gate | Hosted `/dev/fuse`/`fuse3` mount, read/write, unmount, fault and callback-event result |
| macOS/FSKit boundary | Accepted out of scope | Actual Darwin 27.0.0 arm64 host has no `/dev/fuse`; `mount::tests::mount_is_explicitly_unsupported_without_touching_the_path` passes, and this crate makes no FSKit or macFUSE FUSE-protocol claim |
| Errors, cancellation, concurrency, crash, restart and cleanup | Open | Deterministic and native lifecycle evidence with explicit failure classification |

## Current queue

- Complete the remaining applicable FUSE session/callback members and either
  implement or explicitly classify each native mount member; the current
  upstream option/member ledger is recorded in the crate README.
- Run hosted Linux native mount and transport-error event qualification.
- Qualify cancellation, close, crash/restart, concurrency, and durability
  behavior on every supported native runtime.
- Keep unsupported FSKit/platform results explicit; never convert skips into
  production passes.

## Evidence ledger

| Date | Chunk | Result | Remaining blocker |
| --- | --- | --- | --- |
| 2026-09-22 | Inherited W01 baseline | Current `origin/main` FUSE codec/session packets and existing root evidence remain available; privileged/native results are separate | Native Linux/FSKit, root callback events, remaining session parity, and lifecycle/race gates |
| 2026-09-22 | Rust session controls | Added public `FuseSessionOptions`, configurable inode identity, INIT preferences, cache/timeout policy, explicit flush modes, lifecycle/error observability, and regression tests; focused package tests and strict Clippy remain required before publication | Native mount/callback, hosted Linux, cancellation/concurrency, crash/restart and durability evidence |
| 2026-09-22 | Mount option boundary | Added fail-closed validation for caller-supplied native mount option tokens and transport-owned overrides; focused package tests and strict Clippy passed on macOS | Hosted Linux `/dev/fuse`, callback-event, crash/concurrency/durability, and signed/activated FSKit evidence |
| 2026-09-22 | N-API mount-free session facade | Added the Rust-backed `FuseSession` N-API class and public `./fuse` facade with typed options/defaults, negotiated state, inode views, request/reply/error counters, assertion/error callbacks, notification encoders, destroy-state readback, generated declarations, and raw INIT/LOOKUP/READLINK coverage | Native Linux callback events, complete operation/session parity, cancellation/concurrency, crash/restart and durability evidence |
| 2026-09-22 | Native FUSE terminal-error hooks | Added `FuseMountHooks`, owned `FuseTransportError` kinds, `mount_with_hooks`, exactly-once terminal reporting, callback-panic isolation, and a mount-free Unix-stream protocol-failure harness; current macOS FUSE target remains green and the Linux-only harness is target-gated | Hosted Linux `/dev/fuse` callback-event delivery, complete native lifecycle, cancellation/concurrency, crash/restart and durability evidence |
| 2026-09-22 | N-API automatic-mount callback bridge | Threaded `FuseMountHooks` through `mount-rs-auto` and exposed root `mount(..., { onTransportError })`; JavaScript callbacks are converted to an owned TSFN before the async mount future, with generated declarations and callback-safe teardown | Local Rust/N-API checks, Clippy, 16 N-API unit tests, debug addon build, generated typecheck, FUSE suite, and default facade test pass; hosted Linux callback-event delivery, FSKit, and full native lifecycle remain external |
| 2026-09-22 | Mount-free FUSE lock session | Added session-scoped `GETLK`/`SETLK` range conflict tracking, same-owner replacement/unlock, `RELEASE`/`DESTROY` cleanup, strict lock-body/flag validation, and explicit `EAGAIN` for `SETLKW` because the serialized session cannot safely block | Focused session target is 19/19, complete locked FUSE targets and strict Clippy pass; POSIX lock flags remain unadvertised for native mounts until a blocking/concurrent native path and hosted lock evidence exist |
| 2026-09-22 | Automatic mount callback bridge | Threaded one owned N-API `onTransportError` callback through the automatic FUSE/9P/NFS mount paths, with synchronous TSFN conversion, teardown release, generated declarations and native mount-hook forwarding; local auto/NFS/N-API tests and typecheck pass | Hosted native fault-event delivery, complete native lifecycle, cancellation/concurrency, crash/restart and durability evidence |
| 2026-09-22 | Native FUSE panic/cleanup boundary | Wrapped the Linux request loop and asynchronous session destroy in unwind isolation; a backend panic now becomes one owned `Task` transport error, closes the session, marks it inactive, and wakes lifecycle waiters; added a Linux-gated Unix-stream regression harness | macOS FUSE all-target tests (14 unit, 6 INIT, 6 notify/record, 11 protocol, 19 session, 3 sync-barrier), strict Clippy, formatting and Linux-target test type-check pass; hosted Linux native fault/crash/restart and callback-event execution remain external |
| 2026-09-22 | Stop-aware native FUSE cancellation | The Linux loop now observes `MountState::request_stop()` while a request is in flight, drops the cancellable backend future, runs session cleanup, and closes the lifecycle state; a Linux-gated blocking-driver harness verifies bounded cancellation without a transport-error callback | macOS FUSE all-target tests, strict Clippy, formatting and Linux-target test type-check pass; hosted Linux native close races, concurrent request behavior, crash/restart, callback events, locks and durability remain external |
| 2026-09-22 | N-API INIT capability honesty | Made the mount-free N-API session inherit the conservative native-session INIT defaults, so serialized dispatch does not advertise asynchronous direct I/O, parallel directory requests, or `SETXATTR_EXT` unless a caller explicitly supplies flags; added negotiated-flag regression assertions | Hosted Linux capability negotiation and native lifecycle/callback evidence remain external |
| 2026-09-22 | Bounded native FUSE read concurrency | Added up to 16 concurrent positional `FUSE_READ` workers with one serialized reply writer; stateful operations and writes remain serialized, and stop aborts plus drains read workers; a Linux-gated barrier driver proves two reads overlap | macOS FUSE all-target tests, strict Clippy, formatting and Linux-target test type-check pass; hosted Linux `/dev/fuse` execution, native close/crash/restart, write/mutation concurrency, callback events, locks and durability remain external |
| 2026-09-22 | Pinned FUSE oracle differential refresh | From `integrations/mount-rs-napi`, `MOUNTX_SOURCE=/private/tmp/mountx-source-w01-20260921 node test/fuse-codec.mjs` passed every listed FUSE protocol differential family, and the same oracle passed `node test/fuse-inodes.mjs`; this is focused mount-free codec/inode evidence, not the full package or native suite | The oracle is now available for this focused lane; hosted Linux `/dev/fuse`, native callback/lifecycle, remaining session parity, FSKit, cancellation/concurrency, crash/restart and durability remain external |
| 2026-09-22 | Unsupported operation body validation | The mount-free session now validates codec-backed `BMAP`, `SETXATTR`, `GETXATTR`, `LISTXATTR`, and `REMOVEXATTR` bodies with the negotiated protocol context before returning their explicit `ENOSYS` boundary; malformed legacy and `SETXATTR_EXT` forms return `EINVAL` without backend mutation | `./scripts/cargo-shared test -p mount-rs-fuse --all-targets --locked` passed 14 unit, 6 INIT, 6 notify/record, 11 protocol, 20 session, and 3 sync-barrier tests; strict warning-denied Clippy passed; native xattr support remains intentionally unadvertised |
| 2026-09-22 | FUSE_SYNCFS session barrier | Added the protocol 7.34 eight-byte `SYNCFS` body codec and native dispatch to `FsDriver::syncfs`; successful barriers, backend errors, malformed/trailing requests and empty replies are covered without changing the existing explicit boundaries for operations with no driver contract | Focused FUSE tests and host/Linux-target strict Clippy pass after the packet; hosted kernel syncfs behavior and the remaining native lifecycle/crash/durability gates remain external |
| 2026-09-22 | N-API FUSE_SYNCFS codec parity | Added `NativeFuseSyncfsIn`, Rust-backed `fuseDecodeSyncfsIn`/`fuseEncodeSyncfsIn`, explicit `./fuse` CommonJS/ESM aliases, generated declarations, and exact eight-byte/truncated/trailing N-API coverage; the release addon rebuild, codec test, generated typecheck, and locked N-API Rust check pass | The pinned mountx oracle still classifies `SYNCFS` as unimplemented, so no oracle differential is claimed for this operation; hosted kernel syncfs behavior and native lifecycle, callback, crash/restart and durability gates remain external |
| 2026-09-22 | Native forced-teardown closed state | Forced session-task cancellation now idempotently marks the native mount inactive/closed and wakes lifecycle waiters, covering bounded unmount timeout and post-runtime destructor fallback; a Linux-gated regression covers the `wait_closed()` contract | Host FUSE tests (14), host strict Clippy, Linux-target test check, Linux-target strict Clippy, formatting and diff checks pass; actual Linux `/dev/fuse` forced-unmount and crash/restart execution remain external |
| 2026-09-22 | In-flight native FUSE read interrupt | Registered positional read workers by request unique, aborts only a known worker for `FUSE_INTERRUPT`, preserves the existing `EAGAIN` reply for unknown targets, and drains read workers before the next serialized stateful request; a Linux-gated Unix-stream regression proves an interrupted read does not close the session and orderly stop remains callback-silent | Host all-target tests and strict Clippy, formatting/diff checks, and Linux-target strict Clippy pass; hosted `/dev/fuse` interrupt behavior, native mutation/write concurrency, close/crash/restart, callback events, locks and durability remain external |
| 2026-09-22 | Native Linux FUSE concurrency harness | Extended the ignored `/dev/fuse` harness to run eight concurrent blocking kernel clients, each writing, reading, renaming, and rereading a distinct file before asserting the mounted directory contains all eight results; this exercises the native session boundary without promoting local compile evidence to a runtime pass | Host harness compilation, Linux-target strict Clippy, formatting and diff checks pass; the hosted `native-fuse` job must execute the harness before native concurrency is accepted |
| 2026-09-22 | Native Linux FUSE callback fault harness | Added an ignored `/dev/fuse` scenario with a driver that panics only on a file read; the test requires the kernel read to fail, waits for session closure, asserts one owned `Task` transport callback, and completes bounded unmount/cleanup | Focused host tests, Linux-target strict Clippy, formatting and diff checks pass; the hosted `native-fuse` job must execute this case before native callback-event and panic/cleanup evidence is accepted |
| 2026-09-22 | Automatic named-FUSE callback fault harness | Extended the ignored automatic-FUSE harness with the same read-only backend panic through `AutoMountHooks.fuse`; it asserts one `Task` callback at the facade boundary, observes `active == false`, and completes bounded unmount/cleanup | Locked host harness compilation, Linux-target strict Clippy, formatting and diff checks pass; the hosted `native-fuse` job must execute this case before root automatic callback-event acceptance |
| 2026-09-22 | FUSE destroy/read-worker teardown boundary | The Linux request pump now treats `FUSE_DESTROY` as a terminal no-reply boundary, aborts and drains registered positional-read workers, and prevents a pending backend read from blocking session cleanup; a Linux-gated Unix-stream regression proves bounded close while a read is blocked | Host all-target tests, host/Linux-target strict Clippy, formatting and diff checks pass; hosted kernel unmount/close-race and crash/restart execution remain external |
| 2026-09-22 | Native blocked-read unmount harness | Added an ignored `/dev/fuse` scenario whose backend blocks one kernel `READ`; the harness waits until that request is active, calls bounded `Mounted::unmount`, requires the kernel read to finish with an error, and removes the mountpoint | Host all-target tests, host/Linux-target strict Clippy, formatting and diff checks pass; hosted execution must prove the kernel close race, then crash/restart and broader lifecycle evidence remain external |
| 2026-09-22 | macOS FUSE/FSKit scope boundary | On the actual Darwin 27.0.0 arm64 host, `/dev/fuse` is absent and the non-Linux mount path returns `UnsupportedPlatform` without touching the requested path; the supported native macOS path remains NFS, and no FSKit or macFUSE FUSE-protocol parity is claimed | This closes the macOS FUSE/FSKit decision as explicitly outside supported scope; Linux hosted `/dev/fuse`, callback, lifecycle, concurrency, crash/restart and durability gates remain open |
| 2026-09-22 | Native mount-object scope and async disposal (published as `4fd3e25e`) | Added `Mounted[Symbol.asyncDispose]()` to the root N-API lifecycle surface and covered it in the runtime/type declarations; source audit records the intentional boundary that the transport-neutral wrapper does not expose FUSE `session`, `/dev/fuse` `fd`, or invalidation methods because the native session/device remain owned by the serving task; final remote verification is `HEAD=origin/main=4fd3e25e` | FUSE-specific mount members remain outside the current N-API wrapper by design; exact-SHA hosted status is CI pending (`35646646162`), Fault injection in progress (`35646646113`), and no hosted Linux FUSE acceptance result is available; production remains NO-GO |
| 2026-09-22 | Forced-unmount phase deadline (published as `987c593bc08adfb161a55a7a9eee27ff82606310`) | The forced `umount`/lazy-detach ladder now shares one deadline with the final session-task drain, preventing a third full timeout after graceful unmount has expired; a Linux-gated stuck-helper regression checks the bounded result and closed/inactive state | Host FUSE all-target tests (14 unit, 6 INIT, 6 notify/record, 11 protocol, 20 session, 4 sync-barrier), host strict Clippy, Linux-target test check, Linux-target strict Clippy, formatting and diff checks pass; exact-SHA CI run `35648821996` and Fault injection run `35648821873` are pending, so the Linux-only timing test and actual hosted `/dev/fuse` forced-unmount, callback, crash/restart and durability gates remain external |
| 2026-09-22 | Native FUSE active-state transition (published as `8ddf48febaedbd78dc22d889e8f3c822a4e6ad45`) | FUSE mounts now become inactive when teardown starts, matching the other native transports and the upstream lifecycle contract; retryable graceful helper failures restore `active` while the kernel mount remains live; a Linux-gated synthetic helper regression covers the immediate transition and terminal cleanup | Host FUSE all-target tests (14 unit, 6 INIT, 6 notify/record, 11 protocol, 20 session, 4 sync-barrier), host/Linux-target strict Clippy, Linux-target test check, formatting and diff checks pass; exact-SHA CI run `35649715601` is pending and Fault injection run `35649715727` is queued, so the Linux-only runtime regression and hosted `/dev/fuse` close-race, callback, crash/restart and durability gates remain external |
| 2026-09-22 | Forced-teardown callback parity (published as `0d06abb10899f307ce83cd5fa198bab9c5f156a9`) | A graceful-unmount timeout now emits one owned `Task` transport error through the existing exactly-once, callback-panic-isolated hook before forced teardown; the Linux-gated stuck-helper regression asserts the callback kind and message alongside the bounded timeout and terminal state | Host FUSE all-target tests (14 unit, 6 INIT, 6 notify/record, 11 protocol, 20 session, 4 sync-barrier), host/Linux-target strict Clippy, Linux-target test check, formatting and diff checks pass; exact-SHA CI workflow-dispatch run `35650347479` is queued, push CI run `35650323951` was cancelled, and Fault injection run `35650324040` is in progress, so the Linux-only callback execution and hosted `/dev/fuse` forced-unmount/fault, crash/restart and durability evidence remain external |
| 2026-09-22 | FUSE mount-source parity (published as `2a979191d1ef5db37be3a9a3a4bbb2c3efe44457`) | The automatic mount facade now reports the configured FUSE `fsname` through its shared `source` property, matching the pinned mount contract; unsupported-platform FUSE objects return no source, and a Linux-gated Rust regression checks the configured value | FUSE host all-target tests (14 unit, 6 INIT, 0 native, 6 notify/record, 11 protocol, 20 session, 4 sync-barrier), automatic-facade host tests (5 unit, 0 native), host and Linux-target strict Clippy, Linux-target checks, formatting and diff checks pass; exact-SHA Fault injection run `35652258903`, CI run `35652258837`, and W08 release targets run `35652258840` are pending, while W04 production policy `35652258913` succeeded but is unrelated, so the Linux-only source regression and hosted native source/lifecycle execution remain external |
| 2026-09-22 | FUSE frame floor and upstream mount-member scope ledger | Native `max_frame` validation now requires the modern 7.12+ `FUSE_WRITE` header plus one page (`4176` bytes), preventing INIT from advertising a write frame larger than the device limit; the README records explicit decisions for `readers`, `signals`, `tap`, request `onError`, terminal `onTransportError`, root transport-specific members, and crash/restart ownership | Host FUSE all-target tests (14 unit, 6 INIT, 0 native, 6 notify/record, 11 protocol, 20 session, 4 sync-barrier), host strict Clippy, Linux-target check and strict Clippy, formatting, and diff checks pass; the Linux-only validation regression is compiled but not executable on Darwin, and hosted `/dev/fuse` callback, lifecycle, crash/restart and durability gates remain external |
| 2026-09-22 | Native forced-unmount cancellation ordering | Hosted native-FUSE run `35657075892`, job `106523259210`, proved the backend-panic callback case but failed both round-trip and blocked-read unmounts at the 15-second test bound; graceful `fusermount3 -u` could wait for an in-flight request before the session stop signal was sent. The forced path now requests session stop immediately after the graceful deadline and before lazy detach; a Linux-gated helper-ordering regression proves the lazy helper observes that stop request | Host FUSE all-target tests, host/Linux-target strict Clippy, Linux-target test check, formatting and diff checks pass; the Linux-gated ordering test is compile-checked but not executable on Darwin, and the corrected hosted native-FUSE run must still pass before unmount/callback/lifecycle evidence can be promoted |
| 2026-09-22 | Native forced-unmount descriptor drain | The corrected hosted run `35659287961`, native-FUSE job `106530560678`, still timed out both unmount cases: lazy detach was started while the session task still owned the FUSE descriptor. The forced path now stops and drains/aborts that task before invoking lazy detach, reusing one deadline for session drain and helper execution; the helper-ordering regression remains green in Linux-target compilation | Host FUSE all-target tests, host/Linux-target strict Clippy, Linux-target test check, formatting, and diff checks pass; the corrected hosted native-FUSE run must execute the new descriptor-drain path before the Linux unmount/callback/lifecycle gate can be accepted |
| 2026-09-22 | Published-tip FUSE verification | After the context-aware interrupt-validator repair, `fmt --check`, diff checks, locked FUSE/N-API all-target tests, isolated strict Clippy, and `x86_64-unknown-linux-gnu` FUSE test-target compilation passed; the rebuilt debug N-API addon, generated typecheck, mount-free session test, pinned codec oracle, and inode oracle also passed | Hosted CI run `35632382070` was cancelled by a later concurrent push, so it supplies no Linux `/dev/fuse` result; native mount, callback-event, close/crash/restart, mutation/write concurrency, locks, durability, and FSKit gates remain external |
| 2026-09-22 | Rebased callback-harness verification | On current `origin/main`, including the eight-client and callback-fault native harnesses, the locked FUSE target, strict Clippy, Linux test-target compile, locked N-API target, rebuilt debug addon, generated typecheck, mount-free FUSE session, pinned codec oracle, and inode oracle all passed | The native harnesses remain ignored and were not executed on this macOS host; hosted `/dev/fuse`, callback delivery, panic/cleanup, close/crash/restart, mutation/write concurrency, locks, durability, and FSKit remain external gates |
| 2026-09-22 | macOS FSKit boundary refresh | The locked FSKit Rust target passed 12 tests, formatting, strict Clippy, and an arm64 Rust bridge build; the standalone Swift delegate seam and in-process `MountRsXPCServiceLifecycleTests` passed; unsigned arm64 `xcodebuild` schemes `MountRsFSKit`, `MountRsXPCService`, and `MountRsHost` passed, and the embedded extension/XPC Mach-O artifacts were present | The diagnostic activation gate (mount attempt skipped) reported zero valid signing identities, an ad-hoc host bundle, and an unavailable `fskitd` connection, ending `FSKIT_ACTIVATION=BLOCKED`; no signed/installed/enabled FSKit extension or mounted read/write evidence is claimed, and Linux native/lifecycle gates remain external |
| 2026-09-22 | N-API public FUSE surface and dispatch | Closed the verified mount-free export gap: all 185 pinned wire constants are statically discoverable through CommonJS and ESM named imports with declarations, and the `./fuse` facade now exposes opcode body dispatch plus complete request/reply framing, extension validation, raw/unknown handling, and current `SYNCFS` support; `npm run build:debug`, oracle-enabled codec/session/inode tests, generated typecheck, and the locked N-API Rust target passed | The pinned oracle still classifies `SYNCFS` as unimplemented, so that current extension has no oracle differential; the package-wide oracle run reached the NFS phase but stopped on this macOS environment's `Operation not permitted` socket bind, and hosted Linux `/dev/fuse`, callback/lifecycle, close/crash/restart, concurrency, locks and durability remain external |
| 2026-09-22 | Hosted native-FUSE qualification attempt | CI run `35645669363` at `682d2441` passed checkout/toolchain/cache and the Linux FUSE prerequisite probe, then its `native-fuse` job `106485735709` was cancelled during `Exercise actual rootless kernel file operations`; no `/dev/fuse` mount, callback, lifecycle, or durability result is accepted from this run | Publish this branch and dispatch the non-canceling manual qualification workflow at the exact tip before promoting any native Linux result; W01 remains production NO-GO |
| 2026-09-22 | Hosted native-FUSE unmount failure and terminal-fix packet | [CI run `35647249560`](https://github.com/andymac4182/mount-rs/actions/runs/35647249560) at `68f775a53aae16832a5438b74fb21c06a36dd838` reached the native-FUSE scenarios after the prerequisite probe, but [native-fuse job `106491113021`](https://github.com/andymac4182/mount-rs/actions/runs/35647249560/jobs/106491113021) failed all three exercised cases at bounded `Mounted::unmount()` timeouts (ordinary round trip, blocked read, and backend read panic) before the overall run was later cancelled. This packet treats kernel `FUSE_DESTROY` as a terminal no-reply boundary, aborts and drains positional-read workers, and closes the session; host tests/Clippy and Linux-target test compilation pass | Re-run the exact published tip on a non-canceling Linux runner; no native mount/unmount, callback, close/crash/restart, concurrency, locks or durability gate is accepted from the failed/cancelled run, and W01 remains production NO-GO |
| 2026-09-22 | Hosted forced-teardown callback qualification | Manual [CI run `35650347479`](https://github.com/andymac4182/mount-rs/actions/runs/35650347479) at `0d06abb10899f307ce83cd5fa198bab9c5f156a9` passed the FUSE prerequisite probe; [native-fuse job `106500945410`](https://github.com/andymac4182/mount-rs/actions/runs/35650347479/jobs/106500945410) passed `native_backend_read_panic_reports_transport_error_and_closes`, but the ordinary round-trip and blocked-read cases still timed out in `Mounted::unmount()`. The overall matrix was still in progress with unrelated failures when this native job failed, so no hosted native acceptance is promoted; the current destroy-boundary packet is the unverified follow-up for those two cases | Re-run the current exact tip on a non-canceling Linux runner and prove terminal unmount, close-race, callback, crash/restart, concurrency, locks and durability before changing the NO-GO decision |
| 2026-09-22 | Bounded terminal read-worker cleanup | Terminal native teardown now aborts positional-read workers and bounds the final `JoinSet` drain to one second, so a backend future that does not cooperate with cancellation cannot keep the FUSE device open indefinitely; a Linux-gated blocking-worker regression covers the timeout boundary | Host FUSE tests, host/Linux-target strict Clippy, Linux-target test compilation, formatting and diff checks pass; exact-tip hosted unmount, callback, crash/restart, concurrency, locks and durability evidence remain external |
| 2026-09-22 | Bounded FUSE session cleanup | Kernel-initiated `FUSE_DESTROY` now bounds backend handle `close()` cleanup by the configured unmount timeout and reports a single owned `Task` transport error on timeout; terminal read-worker drain timeouts use the same error boundary, with a Linux-gated blocking-close regression | Host FUSE tests, host/Linux-target strict Clippy, Linux-target test compilation, formatting and diff checks pass; exact-tip hosted unmount, callback, crash/restart, concurrency, locks and durability evidence remain external |
| 2026-09-22 | Saturated native read control-plane packet | Replaced the unbounded wait for a positional-read permit with 16 bounded pending reads and `EAGAIN` overflow; `FUSE_DESTROY`/`FUSE_INTERRUPT` remain readable while all 16 active workers are blocked, with a Linux-gated datagram regression covering the saturated terminal path | Host FUSE tests, host/Linux-target strict Clippy, Linux-target test compilation, formatting and diff checks pass; exact-tip hosted unmount, callback, crash/restart, concurrency, locks and durability evidence remain external |
| 2026-09-22 | Targeted FUSE interrupt packet | `FUSE_INTERRUPT` now aborts/removes only its targeted positional-read worker instead of draining unrelated reads; the Linux-gated interrupt regression keeps a second blocked read active while the first is canceled and verifies the session stays open | Host FUSE tests, host/Linux-target strict Clippy, Linux-target test compilation, formatting and diff checks pass; exact-tip hosted interrupt, unmount, callback, crash/restart, concurrency, locks and durability evidence remain external |
| 2026-09-22 | Unconditional terminal read drain | Terminal cleanup now aborts and boundedly joins all registered read workers even when another worker already produced a transport/protocol failure, retaining the first owned error while preventing cleanup from racing outstanding workers | Host FUSE tests, host/Linux-target strict Clippy, Linux-target test compilation, formatting and diff checks pass; exact-tip hosted interrupt, unmount, callback, crash/restart, concurrency, locks and durability evidence remain external |
| 2026-09-22 | Forced-unmount mount-presence state | The forced `umount`/lazy-detach path now rechecks `/proc/self/mounts` before publishing terminal state: a still-present kernel mount remains `mounted=true`, `active=false`, and retryable after its session task is closed; a removed mount is marked absent as before. A Linux-gated stuck-helper regression covers the still-present `/` case | Host FUSE all-target tests (14 unit, 6 INIT, 0 native, 6 notify/record, 11 protocol, 20 session, 4 sync-barrier), host formatting/diff checks, and Linux-target strict Clippy pass; the Linux-only regression is compiled but not run on this macOS host, and hosted forced-unmount, callback, crash/restart, concurrency, locks and durability evidence remain external |
| 2026-09-22 | Forced-unmount grace before lazy detach | The hosted timeout also exposed a lifecycle-budget gap: a session blocked in `/dev/fuse` can never observe the stop notification while `read_frame()` is pending, so draining it for the full second unmount timeout delayed lazy detach beyond the native harness's 15-second bound. The forced path now gives a polling session a 250ms stop grace, aborts the owner task if it remains blocked (closing the device), then starts a fresh full forced-unmount deadline; host tests, formatting/diff checks, and Linux-target strict Clippy pass | Exact-tip hosted native-FUSE execution is still required to prove ordinary unmount, blocked-read cancellation, callback delivery, crash/restart, concurrency, locks and durability; W01-FUSE remains production NO-GO |
| 2026-09-22 | Hosted blocked-read unmount diagnosis and stop-notify fix | The current-main hosted run [35662346488](https://github.com/andymac4182/mount-rs/actions/runs/35662346488) at `b27dd2bbeaa8196052f526f4757ad6b7876b8ff7` passed the real round-trip and backend-panic callback cases but failed only `native_unmount_interrupts_a_blocked_read` in [native-fuse job `106540264683`](https://github.com/andymac4182/mount-rs/actions/runs/35662346488/jobs/106540264683): `native blocked-read unmount timed out: Err(Elapsed(()))` after 30.06s. The next fix retains one stop `Notify` permit in addition to waking current waiters, preventing a stop request from being lost while the request loop transitions back to `read_frame`; a Linux-gated blocked-positional-read stop regression covers the boundary | Host FUSE all-target tests (14 unit, 6 INIT, 0 native, 6 notify/record, 11 protocol, 20 session, 4 sync-barrier), formatting/diff checks, and Linux-target strict Clippy pass; the fix still requires exact-tip hosted native-FUSE rerun before any unmount/callback/lifecycle acceptance is promoted |
| 2026-09-22 | Native stopped-read terminal reply | A stopped positional-read worker now races the mount stop signal and emits a terminal `EIO` reply before the FUSE device is released; terminal `FUSE_DESTROY` cleanup requests stop, drains workers, and only aborts after the bounded drain expires. The Linux-gated Unix-stream regression now asserts the error reply and request unique | Host FUSE all-target tests (14 unit, 6 INIT, 0 native, 6 notify/record, 11 protocol, 20 session, 4 sync-barrier), host strict Clippy, Linux-target check/strict Clippy, formatting, and diff checks pass; manual hosted run `35662415701` / native-FUSE job `106540484337` passed ordinary round-trip and backend-panic close but still timed out the blocked-read unmount and kernel read, so the terminal reply fix needs a fresh exact-tip Linux rerun and W01 remains production NO-GO |
| 2026-09-22 | Hosted blocked-read forced-phase diagnosis | Non-canceling manual [CI run `35666436803`](https://github.com/andymac4182/mount-rs/actions/runs/35666436803) at `ac04a93` / native-FUSE job [106553144636](https://github.com/andymac4182/mount-rs/actions/runs/35666436803/job/106553144636) passed round-trip and backend-panic callback/close, but blocked-read unmount exceeded the 15s harness bound. The forced path now starts lazy detach concurrently with its 250ms session-stop grace, preserving the bounded public observation contract instead of serializing another full timeout; host tests, formatting/diff checks and Linux-target strict Clippy pass | Exact updated hosted rerun must prove the blocked kernel read and unmount complete within the 15s harness bound, then qualify callback/lifecycle, crash/restart, concurrency, locks and durability; W01-FUSE remains production NO-GO |
| 2026-09-22 | Hosted blocked-read rerun and early graceful-stop fix | Non-canceling manual [CI run `35668748366`](https://github.com/andymac4182/mount-rs/actions/runs/35668748366) at `dda0e9c` / native-FUSE job `106560234631` passed the real round-trip and backend-panic callback/close cases, but `native_unmount_interrupts_a_blocked_read` still timed out after 30.03s. The graceful helper was still allowed to consume its full 10s before forced teardown; the new fix stops and closes the serving task after the 250ms grace while graceful unmount remains pending, allowing the helper to release the blocked kernel request without paying a second full timeout. Focused host tests and formatting pass | Fresh exact-tip hosted rerun must prove blocked-read unmount and kernel-read failure within the 15s harness bound; callback/lifecycle, crash/restart, concurrency, locks and durability remain open, so W01-FUSE stays production NO-GO |

| 2026-09-22 | Native session boundary ledger correction | Reconciled the README with the current dispatch: session-scoped `GETLK`/`SETLK` are supported, `SETLKW` returns deliberate `EAGAIN`, and the Linux request pump handles `INTERRUPT` for registered positional-read workers while preserving `EAGAIN` for unknown targets; no hosted acceptance is inferred from this source/test evidence | Hosted `/dev/fuse` lock/interrupt behavior, close/crash/restart, callback, mutation/write concurrency and durability gates remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Deterministic lifecycle recheck | `CARGO_TARGET_DIR=/private/tmp/mount-rs-fuse-lifecycle-20260922 ./scripts/cargo-shared test -p mount-rs-fuse --lib --locked` passed all 14 library tests; the focused session suite passed all 20 tests, covering no-side-effect unsupported-platform behavior, option validation, interrupt classification, lock cleanup, and mount-free lifecycle/error paths | Linux-gated `/dev/fuse` close-race, callback, crash/restart, mutation/write concurrency, locks and durability tests remain unexecuted on this Darwin host; W01-FUSE stays production NO-GO |
| 2026-09-22 | Deterministic durability barrier recheck | `CARGO_TARGET_DIR=/private/tmp/mount-rs-fuse-durability-20260922 ./scripts/cargo-shared test -p mount-rs-fuse --test sync_barrier --locked` passed all 4 tests: `SYNCFS`, `FSYNCDIR`, durable/volatile `FLUSH`, and backend-error propagation | This is mount-free driver/barrier evidence, not kernel persistence, power-loss, crash/restart or hosted `/dev/fuse` durability acceptance; W01-FUSE stays production NO-GO |
| 2026-09-22 | Linux-target FUSE test check | `CARGO_TARGET_DIR=/private/tmp/mount-rs-fuse-linux-check-20260922 ./scripts/cargo-shared check -p mount-rs-fuse --tests --target x86_64-unknown-linux-gnu --locked` passed all library/integration-test compilation; the corresponding cross-target `test --no-run` could not link on Darwin because its native linker rejects Linux flags such as `--as-needed` and `-Bstatic` | This validates Linux-target Rust compilation only; hosted `/dev/fuse` execution, callback, close-race, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Complete current FUSE package suite | `CARGO_TARGET_DIR=/private/tmp/mount-rs-fuse-all-20260922 ./scripts/cargo-shared test -p mount-rs-fuse --all-targets --locked` passed 61 tests: 14 unit, 6 INIT, 0 native on Darwin, 6 notify/record, 11 protocol, 20 session, and 4 sync-barrier; examples compiled with no test cases | Darwin cannot execute the Linux-gated native mount harness; hosted `/dev/fuse` close-race, callback, crash/restart, concurrency, locks and durability evidence remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Warning-denied FUSE quality recheck | `CARGO_TARGET_DIR=/private/tmp/mount-rs-fuse-clippy-20260922 ./scripts/cargo-shared clippy -p mount-rs-fuse --all-targets --locked -- -D warnings` passed for the current FUSE library and all integration/example targets | This is static local quality evidence only; Linux native execution, callback/lifecycle, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Transport-error callback panic isolation | Added a Linux-gated regression proving a panicking `on_transport_error` hook is contained, invoked only once, and cannot replace the first stored transport failure; Darwin library tests passed 14/14, Linux-target test compilation and warning-denied Clippy passed | This verifies local callback state handling only; hosted Linux callback delivery, native lifecycle, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Concurrent unmount waiter convergence | Added a Linux-gated synthetic rootless-helper regression proving two simultaneous `FuseMount::unmount()` callers share one terminal result and publish inactive/closed/unmounted state; host library tests passed 14/14, Linux-target test checking and warning-denied Clippy passed | Darwin cannot execute the Linux-only helper test; hosted `/dev/fuse` close-race, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Retryable graceful-unmount state | Added a Linux-gated helper regression that fails the first graceful unmount, verifies the mount remains live/active and retryable, then succeeds on the second attempt and reaches closed/unmounted state; host library tests passed 14/14, Linux-target test checking and warning-denied Clippy passed | Darwin cannot execute the Linux-only helper transition; hosted `/dev/fuse` retry behavior, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Multi-observer `wait_closed` cleanup | Added a Linux-gated public-lifecycle regression proving multiple `wait_closed()` observers all wake when terminal cleanup publishes the closed state; host library tests passed 14/14, Linux-target test checking and warning-denied Clippy passed | Darwin cannot execute the Linux-only lifecycle test; hosted `/dev/fuse` close-race, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Native write-error callback boundary | Added a Linux-gated closed-peer regression that drives a valid INIT reply into a closed device peer and verifies one owned `Write` transport error plus terminal inactive/closed state; host library tests passed 14/14, Linux-target test checking and warning-denied Clippy passed | Darwin cannot execute the Linux-only device error test; hosted kernel callback delivery, close-race, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Native read-error callback boundary | Added a Linux-gated invalid-device regression using a write-only pipe that verifies one owned `Read` transport error plus terminal inactive/closed state; host library tests passed 14/14, Linux-target test checking and warning-denied Clippy passed | Darwin cannot execute the Linux-only device error test; hosted kernel callback delivery, close-race, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Native option fail-closed semantics | Added Linux-gated validation coverage proving public `max_read = 0`, `init_timeout = 0`, and `unmount_timeout = 0` options are rejected before native side effects; host library tests passed 14/14, Linux-target test checking and warning-denied Clippy passed | Darwin cannot execute the Linux-only validation test; hosted option negotiation, `/dev/fuse` lifecycle, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Mountpoint preflight boundary | Added Linux-gated validation coverage proving a regular file and an absent path are rejected as invalid mountpoints before native helper/device work; host library tests passed 14/14, Linux-target test checking and warning-denied Clippy passed | Darwin cannot execute the Linux-only validation test; hosted mountpoint/kernel lifecycle, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Public mount identity members | Extended the Linux-gated mount-object regression to assert the supported `FuseMount::mode()` and `FuseMount::mountpoint()` values alongside configured `source()`; host library tests passed 14/14, Linux-target test checking and warning-denied Clippy passed | Darwin cannot execute the Linux-only mount-object test; hosted `/dev/fuse` lifecycle, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Public mount-error source contract | Added error-surface coverage proving `MountError::Io` exposes its nested cause while option and timeout variants remain source-free; host library tests passed 15/15, Linux-target test checking and warning-denied Clippy passed | Hosted helper/kernel lifecycle, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Session options accessor contract | Extended the construction-options session regression to assert `FuseSession::options()` returns the configured inode, timeout, cache and flush policy; all 20 session tests, Linux-target test checking and warning-denied Clippy passed | This is mount-free policy readback only; hosted option negotiation, `/dev/fuse` lifecycle, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Sequential unmount idempotence | Added a Linux-gated lifecycle regression proving a later `FuseMount::unmount()` after terminal teardown returns success without reopening helper/device state; host library tests passed 15/15, Linux-target test checking and warning-denied Clippy passed | Darwin cannot execute the Linux-only helper test; hosted `/dev/fuse` lifecycle, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Negotiated session state contract | Extended the construction-options regression to assert the public `max_request` ceiling and `negotiated` INIT state after handshake; all 20 session tests, Linux-target test checking and warning-denied Clippy passed | This is mount-free negotiated-state evidence only; hosted INIT negotiation, `/dev/fuse` lifecycle, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Native mount default-options contract | Extended the default-options regression to assert `fsname`, subtype, permission flags, read-only/read-limit policy, helper options, frame ceiling, and ten-second lifecycle timeouts; host library tests passed 15/15, Linux-target test checking and warning-denied Clippy passed | This verifies local defaults only; hosted option negotiation, `/dev/fuse` lifecycle, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |
| 2026-09-22 | Mount mode selection contract | Added Linux-gated coverage proving explicit privileged mode and root `Auto` mode resolve without a rootless helper; host library tests passed 15/15, Linux-target test checking and warning-denied Clippy passed | Non-root helper discovery and hosted mode execution remain external; `/dev/fuse` lifecycle, callback, crash/restart, concurrency, locks and durability remain external; W01-FUSE stays production NO-GO |

## Completion rule

The owning task may mark W01-FUSE production-ready only when every applicable
gate above is PASS or explicitly accepted outside supported scope, the exact
commands and host prerequisites are recorded here, and this file is committed
with the implementation/test chunk. Until then the decision remains
**NO-GO**.
