# Formal verification and regression coverage

This inventory records the input domain and property of each locally executed proof. A passing bounded proof applies to its stated harness, not to the whole workspace. The full local runner passed all 30 harnesses with Kani 0.68.0 and CBMC 6.11.0 on 64-bit aarch64 macOS; each reported `VERIFICATION:- SUCCESSFUL` in its own invocation.

| Harness | Production code and input domain | Proved property | Bound and limits |
| --- | --- | --- | --- |
| `single_extent_validation` | Calls `src/storage.rs::validate_file_extents` directly with exactly one `BlockExtent`, a fixed nonempty `BlockId` (`"block"`), and four independent, arbitrary full-range `u64` values: file offset, block offset, extent length, and file size. | Every accepted extent has positive length, a checked file end, a checked block end, and a file end no greater than file size. Zero length, overflowing file or block ranges, and a file end beyond file size are rejected. Five cover cases are reached: acceptance, zero length, file overflow, block overflow, and file-size rejection. | `#[kani::unwind(16)]`; Kani 0.68.0 and CBMC 6.11.0. The fixed block ID excludes empty-ID rejection. A single extent cannot exercise ordering or overlap. `from_config` and the complete `validate_file_layout` call are outside this harness. |
| `two_extent_validation` (`mount-rs-core`) | Direct `validate_file_extents` call with exactly two fixed nonempty block IDs and seven arbitrary full-range `u64` values. | Acceptance exactly matches positive lengths, checked block/file ends, file-size bounds, and sorted nonoverlap. Covers accepted adjacent and sparse extents, overlap, and zero length. | Unwind 24; 0/371 failed checks and 5/5 covers. No block I/O or full namespace graph. |
| `namespace_path_boundaries` (`mount-rs-core`) | `has_component_prefix` with one symbolic choice among `/a`, `/a/x`, `/ab`, `/ab/x` against `/a`. | Exactly the first two are inside; both outcomes covered. | Unwind 8; 0/182 failed checks and 2/2 covers. Only four canonical paths; normalization closure/idempotence have ordinary tests. |
| `decoded_truncation_requires_write` (`mount-rs-core`) | All 64 combinations of the six `OpenFlags` booleans, calling `has_valid_truncate_access`. | Accepted truncation implies write; rejection implies truncation without write. Three covers. | 0/3 failed checks and 3/3 covers. Mutation and native numeric flag decoding are outside the harness. |
| `bounded_payload_range_stays_within_wire_input` (`mount-rs-9p`) | Direct `P9Reader::checked_end`, with symbolic bytes, input length 0–8, valid offset, and requested count 0–9. | Accepted range ends exactly at offset plus count and within input; rejection exactly matches out-of-range request. Exact-fit, rejection, and nonzero-offset covers. | Unwind 16; 0/49 failed checks and 3/3 covers. No byte copy, full parser, or concurrent transport proof. |
| `reply_frame_len_bounds` (`mount-rs-fuse`) | Pure `reply_frame_len` with message/used lengths at most `u32::MAX + 64` and arbitrary `usize` body offset. | Accepted frames use canonical header offset, remain in buffer, and fit `u32` wire length. Five boundary covers. | 0/55 failed checks and 5/5 covers. Allocation, Linux prepared read, and native mount are outside the harness. |
| `stateid_tokens_are_injective` (`mount-rs-nfs`) | `server_stateid` with arbitrary `u32` seed and distinct arbitrary `u64` tokens. | Encoded `other` bytes differ; one concrete pair cover. | 0/50 failed checks and 1/1 cover. No allocator, map lifetime, or restart proof. |
| `anonymous_access_uses_other_mode_bits` (`mount-rs-nfs`, v3) | Direct `allowed_access` call with arbitrary stats uid/gid, symbolic file/directory kind, and symbolic mode constrained to `0o000`–`0o777`; constructs AUTH_NONE with absent uid/gid and no groups, and AUTH_SYS with explicit uid/gid zero. | All six advertised v3 ACCESS rights for AUTH_NONE match the mode's other bits; explicit root retains read/modify/extend. Three cases cover denied anonymous root-owned access, explicit root access, and anonymous execute allowed by other bits. | No explicit unwind annotation; 0/299 failed checks (11 unreachable), 3/3 covers. RPC credential parsing, READ/WRITE/CREATE gating, and backend authorization are outside the proof. |
| `anonymous_access4_uses_other_mode_bits` (`mount-rs-nfs`, v4) | Direct `allowed_access4` call with the same bounded mode and symbolic stats uid/gid/file kind; constructs absent-identity AUTH_NONE and explicit-root AUTH_SYS, both with empty groups. | The full advertised v4 ACCESS bitmask for AUTH_NONE matches other mode bits; explicit root retains read/modify/extend. Three denied/root/other-execute covers. | No explicit unwind annotation; 0/294 failed checks (11 unreachable), 3/3 covers. RPC/TCP credential parsing, state-ID use, operation gating, and backend authorization are outside the proof. |
| `var_opaque_frame_bounds` (`mount-rs-nfs`) | Direct production `opaque_frame` decision called by `XdrReader::var_opaque`, with an arbitrary decoded `u32` length, arbitrary full-range `usize` caller maximum, and symbolic remaining-byte count constrained to 0–12. | An accepted aligned span is within the remaining input and both the caller and `XDR_MAX_ITEM` limits; excessive lengths or insufficient payload/padding reject. Five covers reach caller/global caps, missing payload, missing pad, and a complete frame. | No explicit unwind annotation; 0/19 failed checks and 5/5 covers. This decision proof excludes length-prefix decoding, byte extraction, error formatting, and complete NFS frames. |
| `var_opaque_short_wire_payload` (`mount-rs-nfs`) | Direct production `read_validated_opaque` extraction after assuming a consumed four-byte prefix and admitted padded span: arbitrary 12 wire bytes, symbolic wire size 4–12, and symbolic payload length 0–8. | Returned bytes equal exactly the declared payload, the reader advances by the aligned span, and its offset stays within the wire. Exact-fit and nonzero-padding covers. | Unwind 10; 0/267 failed checks (4 unreachable) and 2/2 covers. The admitted-span assumption and short wire exclude prefix/rejection paths; ordinary tests exercise `var_opaque` end to end. |
| `part_number_marker_boundary_does_not_wrap` (`mount-rs-s3`) | Production parser and checked conversion on `429496729[0-9]` with symbolic final ASCII digit. | Digits 0–5 convert exactly, 6–9 reject instead of wrapping; both outcomes covered. | Unwind 16; 0/319 failed checks and 2/2 covers. One ten-digit slice, no arbitrary marker or HTTP proof. |
| `sqlite_io_extent_matches_signed_range` (`mount-rs-sqlite-vfs`) | Direct numeric validator with arbitrary `c_int` amount, signed 64-bit offset, and pointer nullness. | Acceptance matches nonnegative length/offset, a nonnull pointer for positive lengths, and a checked signed end; converted values are exact. Zero length may use a null pointer. Four boundary covers. | 0/54 failed checks and 4/4 covers. External C buffer validity, ownership, aliasing, and callbacks are outside the proof. |
| `abi_request_slice_preconditions` (`mount-rs-fskit-bridge`) | Shared dispatch validator for both C entrypoints, with arbitrary full-range `usize` request length/response capacity and three pointer-presence booleans. | An accepted request is at most 1 MiB + 20 bytes; nonzero request length and response capacity require the respective pointers, and the response-length output is always present. Six zero/exact/oversize/missing-pointer covers. | 0/5 failed checks and 6/6 covers. Pointer validity, alignment, non-aliasing, worker lifetime, and native FSKit activation are external obligations documented in the C header. |
| `writer_takeover_is_fenced_and_checked` (`mount-rs-foundationdb`, `foundationdb` feature) | Production numeric writer-acquire planner with arbitrary full-range clock/TTL/current fence+expiry/last fence and presence flags; assumes positive admitted clock/TTL and positive persisted records when present. | A grant advances every observed fence and checked expiry; a live lease or overflow rejects. Four grant/live/fence-overflow/expiry-overflow covers. | Unwind 12; 0/236 failed checks and 4/4 covers. No FoundationDB transaction atomicity, lease-oracle trust, commit acknowledgement, or multiwriter CAS proof. |

The following 15 proofs extend that inventory. Each harness calls a decision used by production code and has its own executed Kani result. A symbolic decision proof does not establish allocation, I/O, or scheduler behavior in its caller.

| Harness | Production code and input domain | Proved property | Bound and limits |
| --- | --- | --- | --- |
| `three_extent_validation` (`mount-rs-core`) | Direct `validate_file_extents` with exactly three extents, fixed nonempty block IDs, and ten arbitrary full-range `u64` values: three file offsets, block offsets, and lengths, plus file size. | Acceptance matches an independent `u128` oracle for positive lengths, checked file/block ends, file-size bounds, and sorted nonoverlap. Adjacent and sparse layouts, either overlap, and a zero third length are covered. | Unwind 32; 0/304 failed checks (4 unreachable), 6/6 covers. No fourth extent, block read, or arbitrary namespace graph. |
| `directory_link_count_boundaries` (`mount-rs-core`) | Production `validate_directory_link_counts`, called by `Namespace::validate`, with symbolic root flag and arbitrary full-range `u64` parent count, child count, and `nlink`. | Root requires zero parents, nonroot requires one, and `nlink` exactly equals checked `child_count + 2`. Acceptance and wrong-parent, overflow, and wrong-link-count rejection match an independent `u128` oracle. | Unwind 16; 0/262 failed checks (4 unreachable), 5/5 covers. The graph walk that obtains these counts remains a finite regression, not a symbolic proof. |
| `linux_wire_open_bits_preserve_access_and_truncate_guard` (`mount-rs-core`) | Direct `OpenFlags::from_bits` and shared truncation guard with arbitrary full-range `u64` Linux numeric flag bits. | Access mode 0/1/2 maps to read/write exactly, mode 3 grants neither; create/exclusive/truncate/append map to their bits; unknown bits grant nothing and cannot bypass read-only truncate rejection. | 0/80 failed checks, 4/4 covers. No HostFs OS open, N-API callback, or Node string-flag parser proof. |
| `read_lease_ttl_milliseconds_are_checked` (`mount-rs-core`) | Direct `ReadLeaseRequest::validate` with fixed nonempty owner, arbitrary full-range `u64` seconds, and symbolic nanoseconds constrained to `0..1_000_000_000`. | Milliseconds match independent `u128` conversion; submillisecond zero rejects with `EINVAL`, values beyond `u64` reject with `EOVERFLOW`, and the exact `u64::MAX` boundary admits. | Unwind 8; 0/241 failed checks (6 unreachable), 4/4 covers. Owner parsing, provider clocks, provider `i64` conversions, and lease I/O remain outside. |
| `bounded_frame_size_classification_matches_wire_bounds` (`mount-rs-9p`) | Production frame-size decision shared by extraction, version scans, and stream parsing; arbitrary `u32` declared size, received prefix 0–12 bytes, and negotiated limit 7–12 bytes. | Classifies sizes below the seven-byte header, above the limit, incomplete, and complete in the correct order; four cases including exact fit are covered. | Unwind 4; 0/17 failed checks, 4/4 covers. No allocation, body decoder, or complete stream schedule. |
| `nonroot_access_selects_owner_primary_supplemental_and_other` (`mount-rs-nfs`, v3) | Direct `allowed_access` for regular files/directories, octal mode `0..0o777`, nonroot user/file UID and primary/supplemental/file GID each 1–3, one modeled supplemental group, and requested mask 0–63. | Advisory rights select owner before primary/supplemental group before other, using independent octal masks; granted bits stay within the request. Seven covers reach priority, group allowance/denial, other, and file/directory execute. | 0/397 failed checks, 7/7 covers. Requested masking is modeled after the helper; async ACCESS handling, credential provenance, and operation authorization require separate wire/backend evidence. |
| `nonroot_access4_selects_owner_primary_supplemental_and_other` (`mount-rs-nfs`, v4) | The same bounded nonroot identities, mode, file kind, and one supplemental group; requested mask 0–63, calling `allowed_access4` and the supported-mask decision. | Advisory v4 rights match independent owner/group/other octal masks; grants stay within supported/requested rights. Seven owner, group, other, lookup, and execute covers. | 0/397 failed checks, 7/7 covers. The proof does not verify RPC authentication, async handler state IDs, or permission enforcement by the driver. |
| `supported_access4_mask_respects_request_and_object_kind` (`mount-rs-nfs`, v4) | Production supported-mask decision with every `u32` requested mask and symbolic regular-file/directory kind. | Supported rights are a subset of the request and implemented rights; a regular file never advertises DELETE, while a directory can, following [RFC 8881 §18.1](https://www.rfc-editor.org/rfc/rfc8881.html#section-18.1). Both kinds and unknown request bits are covered. | 0/4 failed checks, 3/3 covers. This proves ACCESS reply advertisement only; DELETE procedure authorization remains separate. |
| `var_opaque_short_wire_decision` (`mount-rs-nfs`) | Production XDR opaque wire decision with payload length and caller limit each 0–4, wire size 0–8, and four symbolic tail bytes. | Missing prefix, caller cap, missing payload/padding, zero-length completion, and nonzero padding completion match wire bounds. | Unwind 6; 0/109 failed checks, 6/6 covers. This allocation-free decision excludes extracted bytes and formatted errors; a 450-case public-reader regression checks those paths. |
| `var_opaque_all_wire_prefixes` (`mount-rs-nfs`) | The same production decision with every symbolic four-byte `u32` big-endian length prefix, arbitrary full-range `usize` caller limit, and tail size 0–4. | Caller/global XDR caps, aligned-span truncation, and zero/four-byte completion match an independently decoded wire length and padded-span oracle. | Unwind 6; 0/130 failed checks, 6/6 covers. Tail is bounded; full `var_opaque` allocation/error formatting and nested frames are unproved. |
| `decoded_chunk_total_never_wraps_or_exceeds_limits` (`mount-rs-s3`) | Production decoded-byte checker called by streaming and buffered decoders, with arbitrary full-range `u64` current/additional/declared counts, arbitrary full-range `usize` body limit, and symbolic declared-length presence. | Acceptance equals exact `u128` sum within `u64`, body, and optional declaration bounds; overflow/body and declaration rejection have distinct error classes. | Unwind 2; 0/105 failed checks, 4/4 covers. Numeric checker only; HTTP/signatures/full streaming state and buffering behavior remain separate. |
| `chunk_payload_and_footer_stay_within_body` (`mount-rs-s3`) | Production buffered chunk-frame checker with arbitrary full-range `usize` cursor/body length and `u64` payload size. | Accepted payload and CRLF footer fit the body exactly; size/offset overflow and truncation reject according to an independent `u128` oracle. | Unwind 2; 0/101 failed checks, 3/3 covers. No signed-chunk parsing, body allocation, or split network stream proof. |
| `reacquired_writer_fence_is_exactly_next` (`mount-rs-chunked`) | Production expired-writer reacquisition guard with arbitrary full-range `u64` prior and acquired fences. | Only the strict next token admits; wrap, equal, and skipped tokens fence the writer. | 0/23 failed checks, 4/4 covers. Provider compare-and-swap, async cancellation, and lease expiry remain runtime/provider contracts. |
| `timeout_and_remaining_are_wide_before_clamping` (`mount-rs-webdav`) | Production expiration and remaining-time helpers with arbitrary full-range `u64` timeout seconds and symbolic `i64` clock constrained to -1000..=1000 ms. | Independent `i128` arithmetic verifies saturation at `i64::MAX`, no backward expiry, and exact floor-to-seconds remaining time. | 0/281 failed checks (4 unreachable), 3/3 covers. Extreme `i64` clocks have finite regressions; HashMap lock conflicts, path normalization, and async schedule are not symbolically proved. |
| `writer_renewal_requires_exact_live_fence_and_checked_expiry` (`mount-rs-foundationdb`, `foundationdb` feature) | Production renewal planner and shared lease matcher with arbitrary full-range `u64` clock/TTL/current and requested fences/expiries plus owner-match flag; positive TTL and decoded/input token fields are assumed, but clock zero is admitted. | Renewal accepts only the exact live owner/fence/expiry token and checked new expiry. Wrong owner/fence/expiry, expired token, and expiry overflow reject with the expected error; zero-clock grant is covered. | Unwind 16; 0/279 failed checks (4 unreachable), 8/8 covers. FoundationDB transactions, lease-oracle trust, and lost commit acknowledgements remain outside. |

After installing and setting up Kani, check that `./scripts/cargo-shared kani --version` reports 0.68.0, then run the local proof set from the repository root with:

```sh
./scripts/verify-formal
```

The script invokes the repository-required `./scripts/cargo-shared` wrapper and runs every harness separately so its failed checks and cover properties stay attributable to one proof. FSKit has an independent Cargo workspace, so the script enters `transports/mount-rs-fskit` for its proof. The FoundationDB proof enables the `foundationdb` feature and needs that feature's native build dependencies.

Kani emitted unsupported-construct warnings in standard-library allocation/panic internals and for some core caller-location/foreign-function paths. The passing harnesses do not establish behavior on paths that reach those constructs; the inventory above identifies the bounded production decisions actually checked.

## Executable regression coverage

The added Rust and JavaScript tests exercise production wiring and specific interleavings. They are finite regressions; their passing results do not extend the symbolic input domain or prove transport, backend, native-pointer, or concurrent behavior generally.

| Area | Exercised behavior | Remaining boundary |
| --- | --- | --- |
| Core metadata and paths | `Namespace::validate` connects chunker configuration and one-, two-, and three-extent validation; tests cover adjacent, sparse, overlap, overflow, normalization, component prefixes, and ID/lineage decoding. A 200-case independent integer matrix checks root/nonroot directory link counts. A separate three-present-directory regression checks all 512 directed edge masks through `Namespace::validate` against an independent rooted-tree oracle. | The three-extent and link-count harnesses prove arithmetic and local counts, not an arbitrary graph walk or block read. The topology matrix is exhaustive only for its fixed three-directory shape. |
| Decoded open flags | Memfs and KV tests enumerate all 64 masks. A normal Rust test checks Linux numeric access modes, read-only truncate rejection, and unknown-bit masking; the Kani proof covers every `u64` Linux bit pattern. HostFs rejects read-only truncate before an OS open, preserving existing bytes and leaving an invalid create path absent. The N-API structural-driver regression rejects that mask before its JavaScript `open` callback and permits write-capable truncate. | Host OS effects, arbitrary JavaScript callbacks, and Node string-flag alias semantics remain executable integration or future proof boundaries. |
| KV listings and persist waiters | KV tests enumerate a small keyspace's subsets and bounds, including lexical siblings, unrepresentable names, and truncation of a provider page before filtering. Persist tests poll bounded waiter registration, cancellation, stale wake, and unlock schedules. | These finite matrices do not prove arbitrary provider keyspaces, scheduler fairness, or cross-thread execution. |
| 9P, FUSE, NFS | 9P tests exercise split/version framing, declared `u32::MAX` size rejection, old-task cancellation, pending queue caps, duplicate/reused tags, and failed response writes. FUSE tests cover frame bounds, notification decoding, and inode/handle identity. NFS v3/v4 wire ACCESS tests exercise anonymous/root plus nonroot owner, primary, later supplemental, and other identities, a narrowed requested mask, and regular-file DELETE `supported` suppression. A 450-case XDR reader matrix checks prefix, payload, padding, absolute offsets, and error text. | Numeric 9P frame/payload, FUSE length, NFS XDR wire decisions and short extraction, and advisory ACCESS calculations are proved under their listed bounds. Full parsers, kernel mounts, and socket schedules are not. NFS ACCESS advertises permission bits; it does not gate READ/WRITE/CREATE procedures or establish backend authorization. |
| S3 and WebDAV | S3 tests cover checked multipart markers, segmented staging, signed chunks/trailers, tampering, direct `consume_payload` counter overflow without buffer/state mutation, buffered declared-length refusal, 190 two-cut partitions, and truncated terminal chunks. WebDAV regressions caught `u64::MAX` timeout wraparound and extreme remaining-time subtraction overflow; tests also cover lock scopes, token parsing, refresh of a shared parent during member expiry, and cancellation cleanup. | S3 numeric totals/frame bounds and WebDAV bounded clock arithmetic are proved. HTTP peers, signature verification, full decoder schedules, and symbolic HashMap/path lock conflicts remain outside those harnesses. |
| Chunked and versioned filesystems | Tests control canceled publication acknowledgements and follower responses; exact lease-release retry; reacquired fence gaps/wraps; checked read-lease TTL submillisecond, exact-maximum, and overflow; view, handle, and operation pin lifetimes; lost renewal replies; same-ID publication replay and snapshot identity. A concurrent SQLite regression exercises both directions of a metadata volume paired with different block databases: each writer acknowledges its own file, and the other cannot resolve its block. | Fixed-mode open cancellation after lease acquisition and lost lease-renewal acknowledgements can retain a writer lease until TTL. Dropped view/handle cleanup needs a live Tokio runtime; lost pin-open/renewal responses need provider-owned retry IDs. Existing concurrent provider modes do not yet implement the new core block-backing authority contract: mounts must use the same block database or shared remote backing, and separate preflight checks cannot enforce that relationship. |
| SQLite and PGlite metadata | Tests corrupt persisted head/parent/historical rows and check full decoding, pin/token validation, publication rejection, and pin/delete order. SQLite tests use separate connections for finite concurrent outcomes. | Embedded PGlite socket behavior may serialize clients; its test does not establish independent cross-client row-lock behavior. Neither suite proves remote PostgreSQL schedules or crash recovery. |
| FoundationDB and object-store blocks | FoundationDB feature-on tests call production numeric writer-acquire and exact-token renewal planners; object-store tests exercise 16 conditional-create replay combinations and scoped in-memory reconciliation. | The proofs omit FoundationDB transaction atomicity and commit acknowledgement. No live FoundationDB server or remote object-store consistency is covered. |
| SQLite VFS and FSKit | SQLite VFS tests exercise signed callback range checks. FSKit's separate Rust workspace tests both dispatch entrypoints and oversized request rejection; its C header states caller buffer and worker-lifetime contracts. | Numeric proofs and Rust tests cannot establish externally supplied C buffer validity, alignment, disjointness, ownership, or native FSKit activation. |
| Mastra virtual filesystem | Branch-built native-library tests exercise adapter lifecycle, exclusive writes, hardlink-first moves and copy fallback, replaced source/destination races, partial copy failures, symlink moves, replacement during `deleteFile`/`rmdir`, overwrite-move shutdown, and stale-mtime checks. No-overwrite directory moves fail with `ENOTSUP` because the backend lacks an atomic no-replace directory move. | Hardlink and copy moves expose intermediate states; a failed copy may leave an incomplete destination to avoid unsafe path-based cleanup. A backend replacement after an identity check can race with path-based unlink or rmdir. Strict cross-client moves need atomic no-replace or conditional deletion; `expectedMtime` needs backend compare-and-write. |

Linux prepared-read and native mount tests could not run on this macOS host.

## Publication schema compatibility

Versioned publications now persist caller-supplied namespaces as `NamespacePublication`, reserving `Snapshot` for coordinator live capture. Exact-payload retries of old explicit publications stored as `Snapshot` still reconcile and return their immutable legacy kind; a different payload returns `EEXIST`. An old explicit row cannot be distinguished from a true old snapshot by kind alone, so a reused ID can still be ambiguous for `snapshot_with_publication_id`.

SQLite and PGlite keep the versioning schema at version 1 on ordinary open/reopen and Snapshot-only writes. The transaction that first inserts a `NamespacePublication` also activates schema version 2; the SQLite failure regression verifies that an uncommitted insertion leaves both the row and the version unchanged. A version-1 reader can reopen before that commit and rejects a version-2 store on a new connection afterward. Already connected old readers are not re-gated, so they must be drained before using the new kind. Rollback after activation requires a compatible reader, and downstream exhaustive `VersionKind` matches must add the new variant.

## Planned proof slices

Each future harness needs its own property, input assumptions, bound, and result before it enters the proved inventory.

| Candidate | Proposed input assumptions | Limits to report |
| --- | --- | --- |
| Namespace graph | Extend file layouts beyond the proved one-, two-, and three-extent shapes. Model a small directory adjacency matrix separately from `BTreeMap` traversal, then connect root, cycle, multiple-parent, unreachable, and local link-count results to `Namespace::validate`. | The direct symbolic `BTreeMap` attempts were stopped without results. The existing 512-mask three-directory regression and local link-count proof do not prove arbitrary graph traversal or storage. |
| Publication and lease state machine | Model flush, metadata CAS, conflict, ambiguous commit, canceled acknowledgement, and exact-token lease transitions. | Real SQLite, PGlite, FoundationDB, object-store, and cross-host behavior require runtime gates. |
| Concurrent backing and path identity | Implement the new core `ConcurrentBackingId`/MRC2 metadata and block-store contract in providers and the coordinator, then bind a metadata volume to one durable authority before accepting concurrent writers; atomically open the local backing checked by the CLI path guard. | Current provider preflights run independently and cannot compare backing identities. The CLI checks a pathname before opening the provider, so a symlink swap between those steps can change the backing. Static alias tests do not exercise this race; closing it needs persisted provider binding and descriptor-based opening. |
| Caller-owned pin/lease retry IDs | Reconcile provider commits whose pin open, pin renewal, or writer acquisition/renewal response is lost. | Needs provider API and persisted idempotency records; current TTL bounds some abandoned leases. Never treat a present stale-token `ESTALE` as proof of pin removal. |
| Native pointer and ownership contracts | Specify FSKit, N-API, and SQLite VFS pointer lengths, capacities, lifetime, and ownership transitions. | C/kernel caller validity, aliasing, platform APIs, and data-race freedom need separate evidence. |
| Open-flag dispatch boundaries | Connect the proved Linux numeric decoder and six-boolean truncation guard to HostFs and N-API pre-mutation dispatch; check native host constants and accepted Node string aliases at their ingress boundaries. | Host OS open effects and arbitrary JavaScript callbacks still require executable integration tests. |
| NFS operation authorization | Add a verified caller-principal contract to `FsDriver` or guarded operations, propagate it through v3/v4 READ/WRITE/CREATE/REMOVE and v4 state-ID use, then model bounded permission and handle transitions with fail-closed wire tests. | AUTH_SYS credentials can be spoofed without an external trust boundary. Transport-only stat/mode checks can race; ACCESS reply calculations alone cannot enforce operations or backend policy. |
| Full NFS XDR reader | Join the proved length/span decisions and admitted payload extraction into a bounded `var_opaque` proof of formatted errors, offsets, and rejection paths; extend array nesting separately. | A direct public-reader harness timed out through formatting/allocation and was removed. The 450-case reader matrix is finite; complete NFS frames and peers remain outside. |
| WebDAV lock scope and state | Model a small canonical path/lock set and conflict/refresh transitions, then connect to `DavLockTable` without importing unbounded path normalization or HashMap allocation into the proof. | Direct symbolic lock-state/scope attempts timed out in standard-library string and map code and were removed; the existing finite path matrix remains executable evidence. |
| Full protocol/parser and backend state | Connect the proved 9P/NFS/S3 numeric frame decisions to bounded body parsers, S3 chunk partitions/signatures, WebDAV lock schedules, listings, and provider restart schedules. | Network peers, allocation, cryptographic signatures, and remote backend behavior remain outside the current numeric harnesses. |
