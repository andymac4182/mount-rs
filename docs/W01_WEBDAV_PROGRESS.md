# W01-WebDAV progress tracker

Parent roll-up: [W01 progress ledger](./W01_PROGRESS.md) and the current
repository ledger in [`WORK_TRACKER.md`](../WORK_TRACKER.md).

Owner: delegated W01-WebDAV task

Last refreshed: **2026-09-22** (Australia/Brisbane)

Status: **In progress — production NO-GO**

This tracker owns WebDAV protocol/XML/locks/authentication, session and
streaming APIs, connection faults, lifecycle, and provider/native boundaries.

| Gate | State | Required evidence |
| --- | --- | --- |
| Public WebDAV barrel and pure protocol behavior | Local PASS; pinned pure oracle differential and 16-case pinned HTTP differential PASS; broader N-API session/server parity OPEN | `./webdav` barrel, generated declarations, pinned constants/path/header/XML/lock/document differential, and authenticated TypeScript-vs-Rust loopback HTTP behavior; the pure differential passed with `MOUNTX_SOURCE=/private/tmp/mountx-source-w01-20260921` at oracle `85361a8212ff9bff8e69f62fa8993ef2c2ec51e8`, and the HTTP differential passed 40 paired S3+WebDAV cases, 16 of them WebDAV |
| Session, lock, auth and streaming objects | Buffered/session, active-lock-owner, direct-method, direct-streaming, live HTTP streaming, live auth, live request-error callback, and Map-shaped stats slices PASS; three oracle-only controls are explicitly outside the supported N-API scope; broader member parity OPEN | N-API session view, driver/options/lock policy, scalar and recursive owner lock records, class 1/2/3 methods, auth challenge/acceptance, streamed bodies, cancellation, typed errors, request-level `onError` including an exact-once live `PATCH` refusal callback, and `stats.methods: Map<string, number>`; Rust-only injected-clock expiry is deterministic and tested, while N-API does not claim the oracle's injectable `now`, `onAssertion`, or live `DavLockTable` |
| Connection and server lifecycle | Rust lifecycle serialization, immediate-close wakeup, bounded close-timeout/retry/relisten state, tracked forced connection cancellation, N-API wrapper lifecycle serialization and retry-after-failure semantics, focused live HTTP network concurrency/auth, local host-enabled N-API listener/fault, local macOS native, and hosted macOS/Linux native round-trip slices PASS; restart/hosted session lifecycle OPEN | Rust HTTP lifecycle and connection-fault tests, including concurrent `listen()` serialization, immediate `listen()`/`close()` wakeup, stalled-request timeout, and abort-handle cancellation; rebuilt N-API wrapper race, close-timeout retry, forced-cancellation, 16-pair network-concurrency, and live Basic-auth challenge/acceptance coverage; local N-API listen/close and peer-reset evidence; the explicit macOS `mount_webdav` round-trip and hosted `native-webdav` jobs passed; crash/restart and hosted session/member lifecycle evidence remain required |
| Live/provider/native qualification | Local and hosted macOS/Linux native WebDAV I/O PASS for the recorded packet; local SQLite orderly-reopen and process-crash recovery/process-local-lock classification PASS; live provider and other native environments external | Fresh supported backend and hosted/native results where applicable; native job success and SQLite recovery are not live-provider or full hosted session acceptance |
| Faults, concurrency and durability | Local peer-fault/lock-conflict/expiry/cancellation/close, listener lifecycle serialization, bounded close timeout/retry and forced connection cancellation through Rust and N-API, same-session independent-request and 16-pair live HTTP network concurrency slices, and local SQLite process-crash recovery PASS; broader power-loss/live-provider durability, hosted concurrency, and wider ordering OPEN | Peer-fault callback, lock conflicts/expiry, cancellation/close, concurrent listener calls, stalled-request close timeout/retry, direct-session and live network concurrency on supported live backends, process-crash/provider durability, and wider concurrency matrices |

## Current queue

- Bind the remaining applicable WebDAV session/server members and complete the
  broader direct method/member differential. The N-API boundary for
  `now`, `onAssertion`, and the live `DavLockTable` is explicitly outside the
  supported surface: the Rust transport retains deterministic clock injection,
  while N-API exposes serializable options and read-only lock snapshots.
- Extend concurrency evidence beyond the eight independent direct-session and
  16-pair live HTTP requests, including hosted and power-loss/live-provider
  boundaries where applicable.
- Exercise power-loss and live-provider restart behavior through the N-API
  boundary; the local SQLite process-crash recovery probe now passes, while
  orderly close-timeout retry and forced connection cancellation are covered.
- Keep the N-API wrapper lifecycle race regression green; the package-wide
  server harness still has an unrelated NFS prerequisite failure before its
  WebDAV phase, so it is not promoted to WebDAV acceptance.
- Use the opt-in `MOUNT_RS_SERVER_PHASE=webdav` selector when qualifying the
  WebDAV N-API network/fault/restart matrix independently of that NFS gate.
- Keep the pinned pure oracle differential green and add the broader
  N-API session/member differential when that harness is available; the
  pinned TypeScript-vs-Rust loopback HTTP differential is now evidenced below.
- Qualify supported provider/hosted paths and remaining native environments;
  the local SQLite provider/reopen classification is recorded below, while
  crash/power-loss and live-provider behavior remain separate; keep unavailable
  rows explicitly classified.

## Evidence ledger

| Date | Chunk | Result | Remaining blocker |
| --- | --- | --- | --- |
| 2026-09-22 | WebDAV pure barrel and buffered N-API session | `./scripts/cargo-shared test -p mount-rs-webdav --locked`: 13 passed, native mount probe explicitly ignored; isolated locked N-API check passed; release N-API build and generated declarations passed; direct buffered session/options/driver/auth check passed | Broader session/server parity, loopback N-API listener in this sandbox (`Operation not permitted` without host execution), streaming, peer-fault, restart/durability, provider, and native/hosted gates remain open |
| 2026-09-22 | WebDAV pinned pure oracle differential | `MOUNTX_SOURCE=/private/tmp/mountx-source-w01-20260921 node test/webdav-codec.mjs` passed the complete pure constants/path/header/XML/lock/document differential at oracle `85361a8212ff9bff8e69f62fa8993ef2c2ec51e8`; the same source-backed host-enabled `node test/servers.mjs` also passed | Full session/server member differential, native/hosted lifecycle, provider qualification, crash/power-loss durability, and broader concurrency remain open |
| 2026-09-22 | WebDAV N-API streamed request/response bodies | Isolated N-API check and release addon build passed; generated declarations expose `handleRequestStream`; direct N-API probe passed three-chunk PUT, multi-chunk GET, early response-iterator return, and deliberate request-body failure mapping; the WebDAV stream facade accepts async iterables and Web ReadableStreams | Complete session/member parity, direct listener lifecycle on a host that permits loopback binds, peer-fault injection, oracle differential, restart/durability, provider, and native/hosted gates remain open |
| 2026-09-22 | WebDAV N-API active lock records | Added generated `WebdavLockView` records and `WebdavSession.locks`; `CARGO_TARGET_DIR=/private/tmp/mount-rs-w01-webdav-lock-test-target ./scripts/cargo-shared test -p mount-rs-webdav --locked` passed 13/13 with the native mount probe explicitly ignored, `CARGO_TARGET_DIR=/private/tmp/mount-rs-w01-webdav-lock-check-target ./scripts/cargo-shared check -p mount-rs-napi --locked`, `CARGO_TARGET_DIR=/private/tmp/mount-rs-w01-webdav-lock-napi-target pnpm build`, `node test/servers.mjs`, and `node test/typecheck.mjs` passed; direct LOCK exposed token/path/depth/exclusive/timeout state and UNLOCK reduced the session view to zero | Complete session/member parity, direct listener lifecycle on a host that permits loopback binds, peer-fault injection, oracle differential, restart/durability, provider, and native/hosted gates remain open |
| 2026-09-22 | WebDAV N-API lock-owner and XML entity parity | The rebuilt N-API view now preserves recursive `WebdavXmlNode` owner trees; host-enabled local and pinned structural-driver `node test/servers.mjs` phases accepted a namespaced owner containing `A&amp;B` and returned the expected owner tree. `CARGO_TARGET_DIR=/private/tmp/mount-rs-w01-webdav-xml-entities-test-target ./scripts/cargo-shared test -p mount-rs-webdav --locked` passed 13/13 with predefined/numeric entity tests; `CARGO_TARGET_DIR=/private/tmp/mount-rs-w01-webdav-lock-owner-clippy-target ./scripts/cargo-shared clippy -p mount-rs-webdav -p mount-rs-napi --lib --locked -- -D warnings`, `node test/typecheck.mjs`, and the release addon build passed | Complete session/member parity beyond the owner view, listener/native/hosted lifecycle, provider qualification, crash/power-loss durability, and broader concurrency remain open |
| 2026-09-22 | WebDAV direct N-API method matrix | Host-enabled `node test/servers.mjs` passed direct `OPTIONS`/`MKCOL`/`PUT`/`HEAD`/`GET`, `PROPFIND`/`PROPPATCH`, `COPY`/`MOVE`, `LOCK`/`UNLOCK`, `DELETE`, and explicit `PATCH` refusal at the session boundary | Complete session/member parity, direct listener lifecycle on a host that permits loopback binds, peer-fault injection, oracle differential, restart/durability, provider, and native/hosted gates remain open |
| 2026-09-22 | WebDAV N-API peer-fault callback | Host-enabled `node test/servers.mjs` drove a large-response Node socket reset after reply readiness; exactly one typed transport callback carried the accepted `127.0.0.1:<port>` peer and server cleanup completed | Malformed-connection, lock conflict/expiry, restart/durability, complete session/member parity, oracle differential, provider, and native/hosted gates remain open |
| 2026-09-22 | WebDAV N-API malformed connection | Host-enabled `node test/servers.mjs` isolated a malformed HTTP request on a second WebDAV server and received one typed transport callback with the accepted `127.0.0.1:<port>` peer; the malformed socket and server then closed cleanly | Lock conflict/expiry, restart/durability, complete session/member parity, oracle differential, provider, and native/hosted gates remain open |
| 2026-09-22 | WebDAV same-driver restart classification | Host-enabled `node test/servers.mjs` wrote a file and an active lock, closed the seed server, recreated the server over the same driver, read the file successfully, and observed zero replacement-session locks; this is local same-process recreation evidence only | Crash/power-loss restart, durable lock policy, provider/native/hosted lifecycle, complete session/member parity, oracle differential, concurrency, and durability remain open |
| 2026-09-22 | WebDAV direct-session concurrency classification | Host-enabled `node test/servers.mjs` created `/concurrent` and completed eight parallel unique-file `PUT` requests followed by eight parallel `GET` requests through one `WebdavSession.handleRequest`; every response succeeded and every body matched | This is in-process same-driver evidence only; network-client, native, hosted, crash/power-loss restart, durable-provider behavior, complete session/member parity, and oracle differential remain open |
| 2026-09-22 | WebDAV N-API lock conflict and expiry | Host-enabled `node test/servers.mjs` created a direct exclusive lock, observed an unsubmitted-token `PUT` return `423`, then verified a `Second-1` lock expired and `WebdavSession.locks` returned to zero | Malformed-connection, restart/durability, complete session/member parity, oracle differential, provider, and native/hosted gates remain open |
| 2026-09-22 | WebDAV N-API supported-scope classification | The earlier classification recorded the pre-callback checkpoint: the local N-API surface exposed `driver`, scalar `options`, `stats`, `assertions`, `lockCount`, and snapshot `locks: WebdavLockView[]`, while the pinned oracle also exposed injectable `options.now`, `onError`, `onAssertion`, a live `DavLockTable`, and `stats.methods: Map<string, number>` | The current request-error and Map parity rows supersede those two gaps; injectable `now`, `onAssertion`, live `DavLockTable`, full session/member parity, native/hosted lifecycle, provider, crash/power-loss durability, and broader concurrency remain OPEN |
| 2026-09-22 | WebDAV N-API stats method-map parity | The WebDAV postlude now normalizes the native method-counter snapshot to the oracle's `Map<string, number>` shape; post-build declarations, `node test/typecheck.mjs`, the rebuilt direct N-API `PUT` probe, and `MOUNTX_SOURCE=/private/tmp/mountx-source-w01-20260921 node test/webdav-codec.mjs` passed. The package-wide server harness still stopped in the unrelated parent 9P relisten phase before reaching WebDAV | The injectable `now`, `onAssertion`, live `DavLockTable`, full session/member parity, native/hosted lifecycle, provider, crash/power-loss durability, and broader concurrency remain OPEN |
| 2026-09-22 | WebDAV N-API request-error callback parity | Added a request-level `onError(error, head)` hook without changing existing transport-hook literals; Rust session tests passed unsupported-method and authenticated-401 callback cases exactly once, the release addon regenerated `WebdavServerOptions.onError`, `node test/typecheck.mjs` passed, and an isolated rebuilt N-API PATCH probe received the typed error and request head | The injectable `now`, `onAssertion`, live `DavLockTable`, full session/member parity, native/hosted lifecycle, provider, crash/power-loss durability, and broader concurrency remain OPEN |
| 2026-09-22 | WebDAV N-API supported-scope decision for clock, assertions, and lock table | `./scripts/cargo-shared test -p mount-rs-webdav --test webdav --locked injected_session_clock_controls_lock_expiry_deterministically` passed 1/1 (14 filtered); the Rust `WebdavSessionOptions.now` hook controls lock expiry at an exact injected millisecond, while the N-API binding intentionally has no synchronous JavaScript clock callback, no externally triggerable transport assertion site, and exposes `WebdavLockView[]` snapshots rather than out-of-band lock-table mutators | These three oracle-only controls are outside the supported N-API scope with evidence and rationale recorded; broader session/server differential, hosted/native lifecycle, provider, crash/power-loss durability, and wider concurrency remain OPEN |
| 2026-09-22 | WebDAV pinned TypeScript/Rust HTTP differential | `CARGO=./scripts/cargo-shared MOUNTX_SOURCE=/private/tmp/mountx-source-w01-20260921 node --experimental-strip-types scripts/check-http-parity.mjs` passed `mount-rs HTTP TypeScript/Rust differential: PASS (S3 + WebDAV, 40 paired cases)`, including 16 authenticated WebDAV loopback cases covering streaming PUT, XML property update, GET/HEAD/range/conditional behavior, PROPFIND, COPY/MOVE, refusal, missing-resource, and DELETE responses | This is local pinned-oracle loopback evidence; N-API member parity, hosted/native lifecycle, provider, crash/power-loss durability, and wider network/native concurrency remain OPEN |
| 2026-09-22 | Hosted native WebDAV jobs | At exact scope-packet SHA `e13c52bea3485fada8be03ed62fba9a107255507`, [CI run 35640746296](https://github.com/andymac4182/mount-rs/actions/runs/35640746296) reported [macOS native-WebDAV job 106469172312](https://github.com/andymac4182/mount-rs/actions/runs/35640746296/job/106469172312) and [Ubuntu native-WebDAV job 106469172419](https://github.com/andymac4182/mount-rs/actions/runs/35640746296/job/106469172419) completed `success`; the workflow's ignored `native_mount` harness uses `mount_webdav` on macOS and `davfs2`/FUSE on Ubuntu | Hosted macOS/Linux native WebDAV execution is now evidenced for this packet, but the overall CI run was still in progress with unrelated lanes; full hosted session/member, provider, crash/power-loss durability, and broader concurrency acceptance remain OPEN |
| 2026-09-22 | N-API SQLite WebDAV provider/reopen classification | From `integrations/mount-rs-napi`, `node test/typecheck.mjs && node test/webdav-sqlite.mjs` passed; the test wrote bytes and an exclusive lock through a SQLite-backed WebDAV session, shut down the server/provider, reopened the same database, read the exact bytes, and observed zero replacement-session locks | This proves local SQLite byte persistence and classifies WebDAV locks as process-local on orderly recreation; it does not prove crash/power-loss recovery, durable locks, live remote-provider behavior, or hosted acceptance |
| 2026-09-22 | N-API SQLite WebDAV process-crash recovery | `node test/webdav-sqlite-crash.mjs` spawned a child N-API WebDAV server, wrote bytes and an exclusive lock through the session, terminated the child with `SIGKILL`, reopened the same SQLite database in a new N-API WebDAV server, read the exact bytes, and observed zero replacement-session locks | This proves local SQLite recovery after abrupt process death and retains the process-local lock classification; it does not prove power-loss ordering, live remote-provider behavior, durable locks, or hosted acceptance |
| 2026-09-22 | WebDAV Rust listener lifecycle serialization | `./scripts/cargo-shared fmt --all -- --check` passed, and `./scripts/cargo-shared test -p mount-rs-webdav --test webdav --locked` passed 17/17, including a current-thread immediate `listen()`/`close()` lost-wakeup regression and 16 concurrent `listen()` calls sharing one fixed loopback port | The transport lifecycle race slice is locally qualified; N-API network/hosted concurrency, crash/power-loss restart, provider durability, and broader hosted session/member lifecycle remain OPEN |
| 2026-09-22 | WebDAV N-API wrapper lifecycle serialization | Rebuilt the release addon with `CARGO_TARGET_DIR=/Users/andrewmcclenaghan/Library/Caches/mount-rs/cargo-target pnpm build`; `node test/typecheck.mjs`, `node test/webdav-lifecycle.mjs`, and `node test/webdav-sqlite.mjs` passed. The dedicated lifecycle test ran 40 alternating close-first/listen-first races, exercised real loopback binds, and verified no fulfilled listen left its port reachable after close | This qualifies the local N-API wrapper boundary only; the package-wide `node test/servers.mjs` stopped in the unrelated NFS phase at `0 !== 1` before WebDAV, and hosted/N-API network concurrency, crash/power-loss restart, provider durability, and broader session/member parity remain OPEN |
| 2026-09-22 | WebDAV N-API close-timeout retry | Rebuilt the release addon with `CI=true CARGO_TARGET_DIR=/private/tmp/mount-rs-w01-webdav-napi-close-target pnpm build`; `node test/typecheck.mjs`, `node test/webdav-lifecycle.mjs`, `node test/webdav-sqlite.mjs`, `node test/shutdown-lifecycle.mjs`, and the host-enabled `MOUNT_RS_SERVER_PHASE=webdav node test/servers.mjs` passed. The dedicated lifecycle test ran 40 alternating close-first/listen-first races, exercised real loopback binds, verified no fulfilled listen left its port reachable after close, and drives a stalled partial PUT through timed-out close, forced connection cancellation, peer exit, and successful retry after the failed close promise is cleared | This qualifies orderly local N-API timeout/retry and forced-cancellation behavior; the package-wide default harness remains blocked in the unrelated NFS phase before WebDAV, while hosted/N-API network concurrency, crash/power-loss restart, provider durability, and broader session/member parity remain OPEN |
| 2026-09-22 | WebDAV N-API network/fault/restart matrix | With the default all-transport harness unchanged, `MOUNT_RS_SERVER_PHASE=webdav node test/servers.mjs` passed the WebDAV-only phase through the real loopback listener; its assertions cover direct class 1/2/3 methods, streamed PUT/GET and cancellation/error mapping, peer reset and malformed HTTP callbacks, auth, parallel requests, same-driver restart, and lifecycle cleanup | This is local host-enabled N-API evidence; hosted network concurrency, crash/power-loss restart, live-provider durability, and complete session/member parity remain OPEN |
| 2026-09-22 | WebDAV N-API live network concurrency, auth, streaming and request errors | `node test/webdav-network-concurrency.mjs` passed a real loopback WebDAV listener with one `MKCOL`, 16 concurrent 64 KiB-plus HTTP `PUT`s, 16 concurrent HTTP `GET`s, and a chunked `ReadableStream` `PUT`/`GET`; every response and body matched, method counters reported 17 PUTs and 17 GETs, an unsupported live `PATCH` returned 405 and invoked `onError` exactly once with its request head, and a second listener returned a Basic `401` challenge then `200` for valid credentials | This qualifies local N-API network-client concurrency, authentication, streaming, and request-error delivery only; hosted network concurrency, power-loss/live-provider durability, and broader ordering remain OPEN |
| 2026-09-22 | WebDAV bounded close-timeout lifecycle | `CARGO_TARGET_DIR=/private/tmp/mount-rs-w01-webdav-close-target ./scripts/cargo-shared test -p mount-rs-webdav --test webdav --locked` passed 18/18 with host networking; strict warning-denied WebDAV Clippy and formatting passed. A stalled partial `PUT` now keeps a timed-out server in a terminal closing state, aborts the tracked connection task, rejects relisten, and allows a close retry after the forced drain completes | This qualifies the Rust transport boundary only; hosted network concurrency, crash/power-loss restart, provider durability, and complete session/member parity remain OPEN |
| 2026-09-22 | WebDAV local macOS native round-trip | `MOUNT_RS_WEBDAV_NATIVE_TEST=1 ./scripts/cargo-shared test -p mount-rs-webdav --test native_mount --locked -- --ignored --nocapture` passed 1/1 on Darwin 27.0 arm64 as uid 501 using `/sbin/mount_webdav` and `/sbin/umount`; the explicit native harness mounted, performed the round-trip, and cleaned up successfully | This is local macOS native evidence only; hosted macOS/Linux, provider, crash/power-loss restart, broader concurrency, and full session/member parity remain OPEN |
| 2026-09-22 | Published-tip hosted workflow status | A read-only status check for published commit `9e8e4592cd8d4fe5b42c2734621ac1cd1bce02b5` found [CI run 35631845088](https://github.com/andymac4182/mount-rs/actions/runs/35631845088) and [fault-injection run 35631845044](https://github.com/andymac4182/mount-rs/actions/runs/35631845044) cancelled, while [Live Cloudflare R2 run 35631845090](https://github.com/andymac4182/mount-rs/actions/runs/35631845090) failed; the unrelated [W04 production-policy run 35631845034](https://github.com/andymac4182/mount-rs/actions/runs/35631845034) succeeded | No hosted WebDAV PASS is claimable from this tip; hosted/native lifecycle, provider, crash/power-loss durability, and broader concurrency remain OPEN, so local and source-backed runs do not change the production NO-GO decision |
| 2026-09-22 | Current published-tip hosted workflow status | A read-only status check for docs-only tip `f76a637fdc6d62f400b75505579628facb3cc871` found [CI run 35633305914](https://github.com/andymac4182/mount-rs/actions/runs/35633305914) and [fault-injection run 35633305962](https://github.com/andymac4182/mount-rs/actions/runs/35633305962) cancelled, while the unrelated [W04 production-policy run 35633305932](https://github.com/andymac4182/mount-rs/actions/runs/35633305932) succeeded; no fresh Live Cloudflare R2 run was listed for this tip | No hosted WebDAV PASS is claimable from the current tip; the prior implementation-tip R2 failure and all hosted/native lifecycle, provider, crash/power-loss durability, and broader concurrency OPEN gates remain in force |
| 2026-09-22 | Latest published-tip hosted workflow snapshot | A read-only status check for tracker tip `4b103b1ab9be142b15638a9679999bfd43d3bd80` found [CI run 35633547228](https://github.com/andymac4182/mount-rs/actions/runs/35633547228) pending and [fault-injection run 35633547086](https://github.com/andymac4182/mount-rs/actions/runs/35633547086) in progress; the unrelated [W04 production-policy run 35633547306](https://github.com/andymac4182/mount-rs/actions/runs/35633547306) succeeded | Pending/in-progress workflows are not hosted WebDAV acceptance; no WebDAV PASS is claimable, and the hosted/native lifecycle, provider, crash/power-loss durability, and broader concurrency gates remain OPEN |
| 2026-09-22 | Package integration boundaries | `node test/webdav-codec.mjs`: explicit SKIP because `MOUNTX_SOURCE` is unset; package-wide `node test/servers.mjs` reached the unrelated NFS lane first and hit its host-permission prerequisite | Do not promote the package-wide NFS failure or the skipped oracle row into WebDAV PASS evidence |

## Completion rule

The owning task may mark W01-WebDAV production-ready only when every
applicable gate above is PASS or explicitly accepted outside supported scope,
exact commands and prerequisites are recorded here, and this file is committed
with each implementation/test chunk. Until then the decision remains
**NO-GO**.
