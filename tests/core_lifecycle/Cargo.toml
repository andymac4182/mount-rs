[package]
name = "mount-rs-core-lifecycle"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
publish = false

[workspace]

[dependencies]
mount-rs-memfs = { path = "../../filesystems/mount-rs-memfs" }
mount-rs-core = { path = "../.." }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
