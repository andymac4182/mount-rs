[workspace]

[package]
name = "mount-rs-sqlite-matrix"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
publish = false

[dependencies]
mount-rs-chunked = { path = "../../integrations/mount-rs-chunked" }
mount-rs-core = { path = "../.." }
mount-rs-fault-injection = { path = "../../integrations/mount-rs-fault-injection" }
mount-rs-sqlite = { path = "../../integrations/mount-rs-sqlite" }
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
